**Vorgang:** slice-138
**Fund:** `TestRangeAndFileAt` meldete `keep.md: M erwartet, got ok=false` bei gleich langer Neuschreibung derselben Sekunde; der unmittelbar folgende `make fullbuild` war grün.
