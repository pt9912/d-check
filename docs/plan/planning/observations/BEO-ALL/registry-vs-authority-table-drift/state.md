**Stand:** gestrichen (2026-08-16) — für die eingetretene Instanz mechanisiert: die dritte `planning`-Fähigkeit (slice-102, `wave-unregistered`) prüft die Richtung „Ergebnisnotiz ⇒ Register-Zeile" scharf in `make gates`, genau die Tabelle, an der die Beobachtung zweimal eintrat. Rest bleibt benannt, nicht gezählt: ADR-Index und Konventionsspeicher-Index tragen dieselbe Bauform und sind weiter ungeprüft; träte die Klasse dort ein, wäre ein `registry`-Modul (opt-in, ein Verzeichnis-Muster je Register plus Autoritäts-Datei, eine Richtung, ein Grund-Code) die vorgezeichnete Form.
