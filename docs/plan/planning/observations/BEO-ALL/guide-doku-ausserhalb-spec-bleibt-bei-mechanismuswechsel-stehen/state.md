**Stand:** offen — 2×. Gefunden und behoben in `slice-219`, während der
CO-001-Auflösung (nicht in `slice-220` selbst, wo der Mechanismus wechselte —
das ist der Kern der Beobachtung), und erneut in `slice-226`, diesmal direkt
im selben Slice, das den Mechanismus wechselte — dort aber durch den
unabhängigen Review gefunden, nicht durch den Implementer-Schritt 7 selbst.
