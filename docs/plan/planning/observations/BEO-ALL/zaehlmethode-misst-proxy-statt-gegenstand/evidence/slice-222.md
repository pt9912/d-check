**Vorgang:** slice-222
**Fund:** Zwei Messungen im selben Slice zählten einen Proxy. **(1)** Die Frozen-Klassen-Tabelle wies „299 Vorkommen" aus und summierte sich selbst auf 300; gemessen sind es **315** Vorkommen auf **302** Zeilen — gezählt worden waren teils Zeilen, teils Vorkommen, und die Spalten-Aufteilung stimmte in vier von sechs Klassen nicht, weil das Muster `.harness/baseline/v6.5.0` die fünf relativen Schreibweisen übersieht. **(2)** Aus „33 eingefrorene Pfad-Verweise" schloss der Plan auf den Umfang des `ignore-refs`-Ventils. Das ist ein Schluss über die falsche Menge: Ein Verweis erzeugt einen Befund, wenn ein **Modul ihn auflöst** — 24 der 27 in `done/` sind `d-check:cite`-Direktiven in Verzeichnissen, die `citations.scope` ausnimmt, die sieben in `docs/reviews/` stehen in Inline-Code und Fences. Gemessen (Lauf ohne den Eintrag): **4** Befunde. Beide Male lieferte der Proxy eine plausible Zahl. Gefunden vom unabhängigen Review (F-3); beim Nachmessen ist die Klassifikation dann ein zweites Mal still gescheitert, weil `grep -rl … .` die Pfade **ohne** `./`-Präfix ausgibt und die `case`-Muster eines annahmen.
