**Vorgang:** slice-207
**Fund:** Zweimal im selben Slice, und beide Male sah die Zahl plausibel aus.
**(1)** Die Delta-Messung einer Baseline-Hebung zählte **geänderte Zeilen**
und sagte über **Regel-Änderungen** aus: Mit dem Filter des Vorgängers
(`diff -I` über Versionen und Daten) standen 27 Dateien als geändert da; erst
`-w -B` zeigt, dass fünfzehn davon reines Tabellen-Padding waren und die
größte Einzeldatei bei 81 geänderten Zeilen genau **eine** inhaltliche trägt.
**(2)** Die Register-Zählung *„35 Verzeichnisse — nachgezählt"* zählte nur
`BEO-ALL/`; das Register führt ein zweites Kürzel, es sind 36. Beide Male war
der gezählte Teilbaum plausibel genug, um nicht aufzufallen — der erste fiel
beim Klassifizieren auf, der zweite erst im unabhängigen Review.

**Nachtrag:** Dieser Beleg fehlte zunächst. Die Closure-Notiz von slice-207
nannte ihn, die Datei wurde nicht geschrieben — die Register-Paarung war damit
gebrochen, und kein Sensor hält sie. Gefunden hat es Review-Runde 1 des
Folge-Slice beim Nachzählen des Zähler-Stands.
