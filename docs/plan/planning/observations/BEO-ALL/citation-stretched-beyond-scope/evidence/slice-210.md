**Vorgang:** slice-210
**Fund:** Der Plan führte den Geschwister-Eintrag
[`eigene-menge-gemessen-fremde-behauptet`](../../eigene-menge-gemessen-fremde-behauptet/observation.md)
an zwei Stellen als *„(10×, **verkörpert**)"*. Sein `state.md` beginnt mit
*„gemischt, differenziert nach Instanz"* und weist **zwei** Instanzen
ausdrücklich als *kein formgültiger Ausgang* aus. Der Ableiter dieses Eintrags
— **das Feld lesen, nicht den Titel** — ist hier nicht auf eine fremde Quelle
anzuwenden, sondern auf ein Nachbar-Artefakt desselben Registers, und die
`state.md` war in diesem Lauf bereits geöffnet worden. Verschärfend: Der Plan
nennt genau diesen Eintrag in §8 als eines seiner benannten Risiken. **Der
unabhängige Review hat es gefunden.**
