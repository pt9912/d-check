**Vorgang:** slice-176
**Fund:** Eine `.claude/rules/`-Regel mit `paths`-Frontmatter lädt im Auto-Modus nur bei Zugriff über dedizierte Werkzeuge, nicht bei Shell-Zugriff (`cat`, `sed`, `awk`) — Antwort: Regeln ohne `paths` laden beim Sitzungsstart.
