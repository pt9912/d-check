# Beobachtungs-Register

**Status:** Aktiv. **Letzte Änderung:** 2026-08-22 (slice-112-Closure: **BEO-002** erneut eingetreten — verkörpert, nicht weitergezählt; der fehlende Ableiter „grep nach dem alten Wortlaut" ist jetzt in [`MR-025`](../../../harness/conventions.md#mr-025) benannt). Davor 2026-08-21 (slice-111-Closure: **BEO-009** neu bei 2 — eine Commit-Botschaft behauptet eine Probe oder Änderung, die nicht stattfand; **BEO-007 auf 3 und VERKÖRPERT** — der `pre-commit`-Hook trägt jetzt den vollen `doc-check`).

Regeln dieses Registers: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer schreibt (Slice-Closure), wer liest (Welle-Closure =
Lese-Schritt bei 3×; Slice-Planung = Sichtungs-Schritt darunter), wann gestrichen wird,
welche Form ein Beleg hat (Slice-Kennung, so viele wie der Zähler, in `done/`), und dass
eine leere Tabelle `— keine —` trägt statt zu verschwinden.

| Kennung | Beobachtung | Sub-Area | Zähler | Belege | Stand |
|---|---|---|---|---|---|
| BEO-009 | **Eine Commit-Botschaft behauptet eine Probe oder Änderung, die nicht stattfand.** Die Botschaft wird vor oder während der Arbeit formuliert und beschreibt den beabsichtigten Zustand; schlägt eine Probe fehl oder greift ein Edit still nicht, reist die Behauptung trotzdem mit — als falscher Beleg in der Historie. Kein Gate hält Botschaft gegen Diff oder gegen die zitierte Probe (`commits` prüft nur die Kennung) | `*` | 2 | [slice-111](done/slice-111-wave-drift-zwei-haelften.md) | offen, Zähler **2** — beide am selben Tag, ein Beleg-Slice (die Beleg-Form verlangt Slice-Kennungen; die Klasse ist dichter als der Zähler). Erstes Eintreten: eine Digest-Backfill-Botschaft behauptete „--version liefert v0.62.0", die Probe war mit „flag not defined" fehlgeschlagen — **gepusht**, korrigiert im Folge-Commit samt echter Verifikation (OCI-Label). Zweites Eintreten Minuten später: „Index-Zeile gespiegelt", aber der Edit hatte den Wortlaut verfehlt und nichts geändert — vor dem Push per Amend geheilt. Gegenmittel ab jetzt: Botschaften über Proben erst **nach** deren Exit formulieren, und vor dem Commit `git diff --stat` gegen jede Botschafts-Zeile halten (dieselbe Deckungs-Frage wie BEO-006, andere Richtung: dort reist Undeklariertes mit, hier fehlt Deklariertes). Die mechanische Form bei 3× wäre ein Hook, der benannte Datei-Pfade der Botschaft gegen den Staged-Diff hält — dieselbe Heuristik-Grenze wie bei BEO-006 benannt |
| BEO-008 | **Die Spiegel einer Pin-Hebung sind drei Klassen, gehoben wird nur die grep-bare.** `baseline/<tag>`-Pfad-Verweise sind gate-gedeckt und werden beim Bump vollständig retargetet; Release-/Tree-**URLs** mit dem Tag (`releases/tag/`, `releases/download/`, `tree/`) und **Prosa-/Ellipsen-Pins** (`…/vX.Y.Z/…`, „Stand"-Angaben in MR-Körpern) deckt kein Gate und kein Pfad-grep — sie bleiben stehen, teils in sich widersprüchlich in derselben Zeile (Link-Ziel neu, Quell-URL alt) | `*` | 2 | [slice-106](done/slice-106-baseline-v560-vendoring.md), [slice-110](done/slice-110-baseline-v570-bump.md) | offen, Zähler **2**. Beide Male fand erst der unabhängige Review die Klasse, beide Male als Auflage (slice-106 F-1/F-3: Tag-/Tree-URLs; slice-110 F-1: Download-URLs + [`MR-021`](../../../harness/conventions.md#mr-021)-Ellipse — die Wiederholung eine Hebung später, trotz geheiltem Vorgänger). Gegenmittel jetzt: der **Hebungs-Zensus als Drei-Klassen-Checkliste** in der [`MR-028`](../../../harness/conventions.md#mr-028)-Prozedur — der Nachfolger-Eintrag kopiert sie mit. Die mechanische Form bei 3× wäre ein Abgleich der Tag-Muster in URLs/Prosa gegen den §Baseline-Pin (dieselbe Bauform wie der ghcr-Präfix-Abgleich des `versions`-Moduls) |
| BEO-007 | **Ein Gate-Aufruf hinter einer Pipe meldet den Exit des letzten Pipe-Glieds.** `make gates 2>&1 \| tail` liefert den Exit von `tail` — die `&&`-Kette läuft weiter, ein roter Lauf wird als grün behandelt, und die Botschaft behauptet Gates, die nie bestanden wurden. Der Stop-Hook fängt nur das Sitzungs-Ende, nicht den Commit dazwischen | `*` | 3 | [slice-107](done/slice-107-baseline-v560-delta-audit.md), [slice-108](done/slice-108-roadmap-offene-wellen.md), [slice-111](done/slice-111-wave-drift-zwei-haelften.md) | **Schwelle erreicht, VERKÖRPERT in welle-79.** Zweimal am 2026-08-21-Vormittag eingetreten (ein Body-Commit ging mit rotem doc-check durch und wurde gepusht — Heilung als Nachzug-Commit; ein C-1-Commit ebenso — Heilung per Amend). **Drittes Eintreten am selben Abend, trotz gelebter Arbeitsregel:** der Gate-Exit stand explizit in der Ausgabe (`GATES-EXIT=2`), aber die Shell-Kette verband Gate und Commit mit `;` statt `&&` — der rote Stand wurde committet und vor dem Push per Amend geheilt. Die Regel „Exit explizit prüfen" trägt nicht, wenn das Prüfen den Commit nicht **bindet**. Verkörpert als die im Register benannte mechanische Form, an der Stelle mit Biss: der `pre-commit`-Hook läuft jetzt zusätzlich den **vollen `doc-check`** — ein roter Doku-Stand ist damit uncommitbar, unabhängig davon, wie die Shell-Kette gebaut ist. Der Zähler bleibt bei 3 — die Verkörperung beendet das Zählen, nicht die Klasse (Gates jenseits von doc-check bleiben Kür der Arbeitsregel) |
| BEO-006 | **Ein pfad-selektiver Commit nimmt still mit, was früher gestagt wurde.** `git rm`/`git mv` stagen sofort; ein späterer Commit, der nur weitere Pfade addet, trägt den gesamten Index — die Botschaft beschreibt einen Teil des Diffs, der Rest reist undeklariert mit, und der Zwischenstand kann gate-rot sein. Kein Gate sieht das: `commits` prüft die Message-**Form** (Kennung), keines die Deckung Botschaft ↔ Diff | `*` | 2 | [slice-106](done/slice-106-baseline-v560-vendoring.md), [slice-108](done/slice-108-roadmap-offene-wellen.md) | offen, Zähler **2**. Erstes Eintreten bei der v5.6.0-Hebung: die 6 283-Zeilen-Baum-Löschung steckte im Skript-Fix-Commit, dessen Botschaft nur das Skript nannte; gefunden vom unabhängigen Review (F-2) über eine Worktree-Gegenprobe, geheilt durch Neuschnitt der unpushed Kette. Gegenmittel heute: Arbeitsregel (vor pfad-selektiven Commits `git status` auf schon Gestagtes prüfen; Moves/Löschungen erst unmittelbar vor ihrem Commit stagen). **Zweites Eintreten in slice-108, Stunden später und trotz frischer Arbeitsregel:** ein `git add -A`-Commit nahm den ungetrackten Review-Report mit, die Botschaft nannte ihn nicht — vor dem Push per Amend deklariert; die Regel allein trägt nicht. Eine mechanische Form gäbe es (Hook, der Botschaft-Pfad-Nennungen gegen den Staged-Diff hält), sie wäre aber ein Heuristik-Wächter — bei 3× zu entscheiden |
| BEO-004 | **Eine Modul-Grenze wird nur auf der Quell-Achse gedacht.** Ein Modul prüft, was es scannt — liest aber Dateien, die es nie scannt: Zieldateien außerhalb der Scan-Wurzeln (`matrix` den Status, `anchors`/`codepaths` die Slugs, `diagrams`/`versions` ihre Quellen, `pins` die gehashte Heading-Section), Post-Pässe ueber selbst benannte Verzeichnisse und **git-Revisionen** (`vcs` rechnet auf Blobs). In diesen Eingaben gilt keine Zusage, die das scannende Modul gibt — und bei `pins` ist die Folge **still** | `*` | 3 | [slice-101](done/slice-101-fence-unbalanciert.md) | offen, **verkörpert in welle-73**. Dreimal in DREI Review-Runden desselben Slice gefunden (Erst-Review F-3: Ziel-Achse; Re-Review N-2: `pins`/`codepaths` fehlten in der Aufzählung; dritte Runde R-7: git-Revisionen als dritte Achse) — die Beleg-Form verlangt Slice-Kennungen, darum **eine** Kennung bei Zähler 3; die Klasse ist dichter als der Zähler. **Verkörpert, nicht weitergezählt:** die vorgeschlagene Form — eine Frage im Slice-Template — gibt es in diesem Repo nicht ([`MR-018`](../../../harness/conventions.md#mr-018--d-check-verkörpert-als-producer-self-hoster-keine-templates): keine Templates). Gewählt wurde die Stelle, an der die Klasse dreimal **tatsächlich** gefunden worden ist: ein MEDIUM-Anker im Reviewer-Skill („welche Eingaben liest dieses Modul, die es nicht scannt?“, `reviewer.md` 1.4.0). Der Zähler bleibt bei 3 — die Verkörperung beendet das Zählen, nicht die Klasse. **Wirkung belegt in welle-76:** die Re-Review zu [slice-095](done/slice-095-links-resolve-from.md) fand über genau diesen Anker die Abdeckungs-Stille der `resolve-from`-Gruppen-Orte (Orte außerhalb des wirksamen Scan-Bereichs sind still keine Quellen) — als benannte Grenze in den Vertrag übernommen |
| BEO-003 | **Eine geteilte Lexik driftet an den Rändern, weil jeder Konsument sie selbst vorbereitet.** Nicht die Lexik selbst war das Problem, sondern dass fünf Stellen dieselbe Zeile verschieden trimmten (`TrimSpace` unicode-weit gegen `TrimLeft` auf Space/Tab) und zwei Automaten dieselbe Frage verschieden beantworteten. Jede Stelle für sich vertretbar — zusammen ein stiller Grün-Pfad, den kein Gate sieht, weil jedes Modul für sich konsistent ist | `*` | 3 | [slice-101](done/slice-101-fence-unbalanciert.md), [slice-099](done/slice-099-structure-modul.md), [slice-103](done/slice-103-geteilte-lexik-raender.md) | **verkörpert in welle-74**. In slice-101 dreimal eingetreten, in drei aufeinanderfolgenden Review-Runden, jedes Mal an einer neuen Stelle (spans; dann `planning`, also genau dort, wo der Anlassfall gemessen wird; dann drei unassertierte Konsumenten). Fuer die **Fence**-Lexik geschlossen: ein geteiltes Trimm-Prädikat, beide Schluss-Lesarten geteilt, je Konsument eine Assertion. Fuer andere Lexiken offen — `citations` bildet Absätze selbst, `headingSection` beantwortet die Anker-Frage roh; dafür liegt [slice-103](done/slice-103-geteilte-lexik-raender.md) in Arbeit. **Bei 3x verkörpern:** ein geteiltes Prädikat allein genügt nicht — es braucht je Konsument eine Assertion gegen Wieder-Divergenz, sonst ist die Invariante nur ein Kommentar. **Zweites Eintreten in slice-099:** die geteilte Abschnitts-Mechanik trägt **zwei Wort-Begriffe** — die Marke unicode-weit, die Floskel ASCII —, und nur einer stand im Vertrag; beobachtbar an derselben Eingabe (ein Umlaut setzt eine Marke fort und beendet eine Floskel, ein Unterstrich umgekehrt). Diesmal war die Divergenz **gewollt**: verschiedene Gegenstände, verschiedene Grenzen. Die Klasse ist damit weiter gefasst als „dieselbe Frage verschieden beantwortet“ — sie umfasst auch „verschiedene Antworten, nur eine im Vertrag“. **Drittes Eintreten in slice-103** — und dort gleich dreifach: die Absatzbildung in `citations`, der Anker-Zweig in `versions`/`pins` (zweimal halb repariert, erst in der dritten Runde ganz) und der Aktiv-Status-Guard in `planning` (zwei belegte Falsch-Rot). **Verkörpert, nicht weitergezählt:** zweimal hintereinander hat eine Aufzählung, die „vollständig“ hieß, eine Stelle nicht gekannt, und beide Male fand ein Review sie in Minuten — die Liste ist als Methode widerlegt. Die Form ist ein **Kopplungs-Test** (`TestAnkerFrageHatEineAntwort`): dieselbe Eingabe durch **alle** Konsumenten derselben Frage, Fehlschlag bei abweichender Antwort. Er prüft die **Übereinstimmung**, nicht den Aufruf — eine Liste kann eine Stelle vergessen, eine Kopplung nicht. Offen bleibt die Klasse dort, wo noch kein Kopplungs-Test existiert (Absatz- und Überschriften-Achse) |
| BEO-002 | **Eine Semantik-Änderung wird nur im Dokumentkörper nachgezogen, ihre Ränder bleiben stehen** — Historie-Zeilen, Index-Einträge, Konsequenz-Abschnitte, Entscheidungs-Protokolle. Sie sind Prosa und darum gate-unsichtbar: `links`/`anchors`/`ids` prüfen die Auflösung, nicht die Aussage. Der Rand referiert danach eine Fassung, die es nicht mehr gibt, und wirkt dabei so belastbar wie der Körper | `*` | 3 | [slice-093](done/slice-093-closure-note-gate.md), [slice-096](done/slice-096-structure-modul-analyse.md), [slice-104](done/slice-104-floskel-wortgrenze.md) | **Schwelle erreicht.** Erstmals belegt bei slice-093 (die Befund-Provenance wurde beim neuen `--config`-Flag im Kern nachgezogen, in `sources.go` nicht — ein Review fand es). In slice-096 **zweimal** eingetreten (zählt als **ein** Beleg, weil die Beleg-Form Slice-Kennungen verlangt — die Klasse ist dichter als der Zähler): „erster Treffer" blieb nach der Mehrdeutigkeits-Härtung an drei Stellen stehen, und die Überarbeitung nach dem Review korrigierte Anforderung und Algorithmus, ließ aber Spezifikations-Historie, ADR-Index, ADR-Konsequenzen und das Entscheidungs-Protokoll auf der widerrufenen Fassung. **Bei 3× verkörpern statt weiterzählen:** die naheliegende Form ist eine Regel „vor dem Ändern einer Semantik ihre Spiegel auflisten" in der Autoritäts-Doku — dieselbe Bauform wie die Release-Prep-Aufgabenregel, die eine andere still wachsende Klasse geschlossen hat **Drittes Eintreten in slice-104 (welle-72):** nach der Zähl-Angleichung standen **drei** Vertragsflächen auf der widerrufenen Fassung und waren gegen den Lauf falsifizierbar — die Aussage, die Zählung sehe Inline-Code weiterhin; das Akzeptanzkriterium, das einen Teilstring-Treffer verlangte; §2-Schema und §4-Tabelle. Alle drei fand erst der unabhängige Review. **Verkörpern statt weiterzählen:** die Klasse ist dreimal aufgetreten und jedes Mal von einem Menschen bzw. Reviewer gefunden, nie von einem Gate — die naheliegende Form ist die Regel „vor dem Ändern einer Semantik ihre Spiegel auflisten" in der Autoritäts-Doku, mit der Liste der Spiegel (Anforderung · Algorithmus · §2-Schema · §4-Tabelle · Klartexte · print-config-Vorlage · Handbuch · ADR). **Verkörpert vor slice-099** als [`MR-025`](../../../harness/conventions.md#mr-025--semantik-änderung-die-spiegel-vor-dem-editieren-auflisten) — und dort zum ersten Mal angewandt. Sie hat gewirkt (drei Spiegel standen auf der Liste und wären sonst stehengeblieben) und war zugleich **selbst lückenhaft**: vier Lücken fand der Review, eine fünfte die Re-Review. Die fünfte hat die Regel geschärft, statt sie zu bestätigen — sie lag in genau der Datei, die ohnehin bearbeitet wurde, also dort, wo kein Datei-`grep` sucht. Antwort: wo eine Aufzählung eine Menge spiegelt, gehört sie an ihre Quelle **gebunden**; die Liste ist der Notbehelf für den Rest. **Bleibt offen** — eine Regel für Menschen kann weiter verfehlt werden. **Erneut eingetreten in [slice-112](done/slice-112-wellen-invariante-kennungs-menge.md)** (verkörpert, nicht weitergezählt): eine Wortlaut-Präzisierung ließ drei Stellen mit der alten Lesart stehen — in drei Dateien, die der Slice ohnehin bearbeitete (Lastenheft-Prosa, Spezifikation-§2-Schema-Zeile, Handbuch-§6-Vorsatz); die [`MR-025`](../../../harness/conventions.md#mr-025)-Liste stand im Slice, war aber aus dem Gedächtnis geschrieben. Gefunden vom unabhängigen Review (zwei MEDIUM). Gegenmittel: der Ableiter für Wortlaut-Änderungen ist das `grep` nach dem **alten** Wortlaut — jetzt in [`MR-025`](../../../harness/conventions.md#mr-025) benannt |

## Gestrichene Einträge

Regeln dieser Sektion: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer eine Zeile still löscht, macht sie ununterscheidbar von
einer, die es nie gab; darum hierher verschieben (mit Begründung), nicht löschen.

| Kennung | Beobachtung | Gestrichen am | Warum sie nicht mehr auftreten kann |
|---|---|---|---|
| BEO-001 | Ein Datei-Register und seine Autoritäts-Tabelle driften **unbemerkt** — kein Gate deckte die Richtung „Artefakt ⇒ registriert“; zweimal eingetreten ([slice-087](done/slice-087-spec-historie-referenzrichtung.md), [slice-099](done/slice-099-structure-modul.md): drei Wellen-Closures ohne Register-Zeile) | 2026-08-16 | **Für die eingetretene Instanz mechanisiert:** die dritte `planning`-Fähigkeit ([slice-102](done/slice-102-wellen-lifecycle-invariante.md), `wave-unregistered`) prüft die Richtung „Ergebnisnotiz ⇒ Register-Zeile“ und läuft im eigenen Baum scharf in `make gates` — genau die Tabelle, an der die Beobachtung zweimal eingetreten ist, kann nicht mehr still unvollständig sein. **Rest bleibt benannt, nicht gezählt:** ADR-Index und Konventionsspeicher-Index tragen dieselbe Bauform und sind weiter ungeprüft; tritt die Klasse dort ein, ist der `registry`-Modul-Vorschlag aus dem ursprünglichen Eintrag die vorgezeichnete Form (opt-in, ein Verzeichnis-Muster je Register plus Autoritäts-Datei, **eine** Richtung, ein Grund-Code) |
| BEO-005 | Eine chronologische Tabelle kippt still ihre Richtung — aus „unten anhängen“ wird „oben einfügen“, danach führt dieselbe Tabelle zwei gegenläufige Blöcke; kein Gate las Reihenfolge. Am selben Tag an **drei** Tabellen eingetreten ([slice-099](done/slice-099-structure-modul.md)); die mechanische Form stand im Eintrag vorgezeichnet (typisierter Monotonie-Vergleich der Schlüsselspalte, drei Entscheidungen, die keine Details sind) | 2026-08-21 | **Für die eingetretene Instanz mechanisiert:** die siebte `structure`-Bedingung ([slice-105](done/slice-105-tabellen-monotonie.md), `table-order`/`table-column`, v0.61.0) prüft die sechs eigenen chronologischen Bestandstabellen scharf in `make gates` — genau die Tabellen, an denen die Beobachtung eingetreten ist, können nicht mehr still kippen. Der Retro-Beleg lief mit dem **Produkt** (27 Befunde am Stand vor der welle-73-Heilung, exakt die 14 · 6 · 7 der Skript-Messung; heutiger Bestand null), und die Release-Prep der Streich-Welle war der erste gefangene Fall (Handbuch-§11-Zeile im ersten Anlauf falsch herum). **Rest bleibt benannt, nicht gezählt:** chronologische Tabellen außerhalb der sechs aktivierten Regeln, in **zwei** Tabellen gesplittete Ordnungen und fremde Repos ohne Aktivierung deckt die Mechanisierung nicht — tritt die Klasse dort ein, ist die vorgezeichnete Form eine weitere `structure`-Regel bzw. der Grenz-Trigger aus [ADR-0057](../adr/0057-structure-tabellen-monotonie.md) |
