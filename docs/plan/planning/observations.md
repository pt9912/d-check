# Beobachtungs-Register

**Status:** Aktiv. **Letzte Änderung:** 2026-08-15.

Regeln dieses Registers: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer schreibt (Slice-Closure), wer liest (Welle-Closure =
Lese-Schritt bei 3×; Slice-Planung = Sichtungs-Schritt darunter), wann gestrichen wird,
welche Form ein Beleg hat (Slice-Kennung, so viele wie der Zähler, in `done/`), und dass
eine leere Tabelle `— keine —` trägt statt zu verschwinden.

| Kennung | Beobachtung | Sub-Area | Zähler | Belege | Stand |
|---|---|---|---|---|---|
| BEO-005 | **Eine chronologische Tabelle kippt still ihre Richtung.** Wer eine Zeile anfügt, schaut auf die Zeile daneben statt auf die Regel: aus „unten anhängen“ wird irgendwann „oben einfügen“, und danach führt dieselbe Tabelle zwei gegenläufige Blöcke. Sichtbar wird das erst, wenn jemand nach einem Datum sucht — kein Gate liest Reihenfolge, und beide Fassungen sind für sich plausibel | `*` | 1 | [slice-099](done/slice-099-structure-modul.md) | offen. Am selben Tag an **drei** Tabellen gefunden: §7-Historie der Spezifikation (71 Zeilen, Bruch nach 28), §Historie des Lastenhefts (76 Zeilen, Bruch im Tail) und die Drift-Tabelle der Roadmap (49 Zeilen, drei Blöcke). Der Bruch ist jedes Mal die Stelle, an der die Pflege-Gewohnheit wechselte. Alle drei sind jetzt absteigend; die Klasse bleibt offen, weil nichts sie am Wiederauftreten hindert. **Maschinell entscheidbar, aber nicht geschenkt.** Gemessen statt behauptet: ein **typisierter** Monotonie-Vergleich der Schlüsselspalte findet am Stand **vor** der Heilung alle drei Tabellen (14 · 6 · 7 Verletzungen) und am Stand danach null — 6 Tabellen, 318 Zeilen. Ein **naiver** String-Vergleich dagegen meldet drei korrekt sortierte Tabellen rot (`0.10.0 → 0.9.0`, `v0.10.0 → v0.9.0`, `1.9 → 1.10`), der Typ ist also Pflicht, kein Komfort. Die Form wäre eine **siebte Bedingung** im Modul `structure` (dieselbe Eingabe-Achse, Schnitt-Kriterium [ADR-0044](../adr/0044-geteiltes-referenz-ventil-quell-skopus.md) ⇒ kein neues Modul) — mit **drei** Entscheidungen, die keine Details sind: (a) sie müsste die **rohen** Abschnitts-Zeilen lesen, weil die Bereinigung aus Schritt 5 Inline-Code leert und die Schlüsselzelle von `version.md` genau dort steht — eine ausdrückliche Ausnahme von „alle Bedingungen arbeiten auf diesem Text“; (b) sie braucht als einzige Bedingung eine **Zell-Adresse** (Tabelle wählen, Spalte indizieren, Kopf-/Trennzeile überspringen), und die Tabellen-Lexik existiert heute nur als `^|`-Test im Modul `targets` — sie ein zweites Mal zu bauen wäre BEO-003 in Reinform; (c) Richtung je Regel (das Handbuch ist als einzige Tabelle aufsteigend) und ein Befund statt eines stillen Übersprings für eine untypisierbare Zelle. **Beweislage schwächer als bei den drei aktivierten Regeln:** der Nutzen ist Prävention, der Beleg ein Retro-Lauf, der Ist-Bestand grün |
| BEO-004 | **Eine Modul-Grenze wird nur auf der Quell-Achse gedacht.** Ein Modul prüft, was es scannt — liest aber Dateien, die es nie scannt: Zieldateien außerhalb der Scan-Wurzeln (`matrix` den Status, `anchors`/`codepaths` die Slugs, `diagrams`/`versions` ihre Quellen, `pins` die gehashte Heading-Section), Post-Pässe ueber selbst benannte Verzeichnisse und **git-Revisionen** (`vcs` rechnet auf Blobs). In diesen Eingaben gilt keine Zusage, die das scannende Modul gibt — und bei `pins` ist die Folge **still** | `*` | 3 | [slice-101](done/slice-101-fence-unbalanciert.md) | offen, **verkörpert in welle-73**. Dreimal in DREI Review-Runden desselben Slice gefunden (Erst-Review F-3: Ziel-Achse; Re-Review N-2: `pins`/`codepaths` fehlten in der Aufzählung; dritte Runde R-7: git-Revisionen als dritte Achse) — die Beleg-Form verlangt Slice-Kennungen, darum **eine** Kennung bei Zähler 3; die Klasse ist dichter als der Zähler. **Verkörpert, nicht weitergezählt:** die vorgeschlagene Form — eine Frage im Slice-Template — gibt es in diesem Repo nicht ([`MR-018`](../../../harness/conventions.md#mr-018--d-check-verkörpert-als-producer-self-hoster-keine-templates): keine Templates). Gewählt wurde die Stelle, an der die Klasse dreimal **tatsächlich** gefunden worden ist: ein MEDIUM-Anker im Reviewer-Skill („welche Eingaben liest dieses Modul, die es nicht scannt?“, `reviewer.md` 1.4.0). Der Zähler bleibt bei 3 — die Verkörperung beendet das Zählen, nicht die Klasse |
| BEO-003 | **Eine geteilte Lexik driftet an den Rändern, weil jeder Konsument sie selbst vorbereitet.** Nicht die Lexik selbst war das Problem, sondern dass fünf Stellen dieselbe Zeile verschieden trimmten (`TrimSpace` unicode-weit gegen `TrimLeft` auf Space/Tab) und zwei Automaten dieselbe Frage verschieden beantworteten. Jede Stelle für sich vertretbar — zusammen ein stiller Grün-Pfad, den kein Gate sieht, weil jedes Modul für sich konsistent ist | `*` | 2 | [slice-101](done/slice-101-fence-unbalanciert.md), [slice-099](done/slice-099-structure-modul.md) | offen. In slice-101 dreimal eingetreten, in drei aufeinanderfolgenden Review-Runden, jedes Mal an einer neuen Stelle (spans; dann `planning`, also genau dort, wo der Anlassfall gemessen wird; dann drei unassertierte Konsumenten). Fuer die **Fence**-Lexik geschlossen: ein geteiltes Trimm-Prädikat, beide Schluss-Lesarten geteilt, je Konsument eine Assertion. Fuer andere Lexiken offen — `citations` bildet Absätze selbst, `headingSection` beantwortet die Anker-Frage roh; dafür liegt [slice-103](in-progress/slice-103-geteilte-lexik-raender.md) in Arbeit. **Bei 3x verkörpern:** ein geteiltes Prädikat allein genügt nicht — es braucht je Konsument eine Assertion gegen Wieder-Divergenz, sonst ist die Invariante nur ein Kommentar. **Zweites Eintreten in slice-099:** die geteilte Abschnitts-Mechanik trägt **zwei Wort-Begriffe** — die Marke unicode-weit, die Floskel ASCII —, und nur einer stand im Vertrag; beobachtbar an derselben Eingabe (ein Umlaut setzt eine Marke fort und beendet eine Floskel, ein Unterstrich umgekehrt). Diesmal war die Divergenz **gewollt**: verschiedene Gegenstände, verschiedene Grenzen. Die Klasse ist damit weiter gefasst als „dieselbe Frage verschieden beantwortet“ — sie umfasst auch „verschiedene Antworten, nur eine im Vertrag“ |
| BEO-002 | **Eine Semantik-Änderung wird nur im Dokumentkörper nachgezogen, ihre Ränder bleiben stehen** — Historie-Zeilen, Index-Einträge, Konsequenz-Abschnitte, Entscheidungs-Protokolle. Sie sind Prosa und darum gate-unsichtbar: `links`/`anchors`/`ids` prüfen die Auflösung, nicht die Aussage. Der Rand referiert danach eine Fassung, die es nicht mehr gibt, und wirkt dabei so belastbar wie der Körper | `*` | 3 | [slice-093](done/slice-093-closure-note-gate.md), [slice-096](done/slice-096-structure-modul-analyse.md), [slice-104](done/slice-104-floskel-wortgrenze.md) | **Schwelle erreicht.** Erstmals belegt bei slice-093 (die Befund-Provenance wurde beim neuen `--config`-Flag im Kern nachgezogen, in `sources.go` nicht — ein Review fand es). In slice-096 **zweimal** eingetreten (zählt als **ein** Beleg, weil die Beleg-Form Slice-Kennungen verlangt — die Klasse ist dichter als der Zähler): „erster Treffer" blieb nach der Mehrdeutigkeits-Härtung an drei Stellen stehen, und die Überarbeitung nach dem Review korrigierte Anforderung und Algorithmus, ließ aber Spezifikations-Historie, ADR-Index, ADR-Konsequenzen und das Entscheidungs-Protokoll auf der widerrufenen Fassung. **Bei 3× verkörpern statt weiterzählen:** die naheliegende Form ist eine Regel „vor dem Ändern einer Semantik ihre Spiegel auflisten" in der Autoritäts-Doku — dieselbe Bauform wie die Release-Prep-Aufgabenregel, die eine andere still wachsende Klasse geschlossen hat **Drittes Eintreten in slice-104 (welle-72):** nach der Zähl-Angleichung standen **drei** Vertragsflächen auf der widerrufenen Fassung und waren gegen den Lauf falsifizierbar — die Aussage, die Zählung sehe Inline-Code weiterhin; das Akzeptanzkriterium, das einen Teilstring-Treffer verlangte; §2-Schema und §4-Tabelle. Alle drei fand erst der unabhängige Review. **Verkörpern statt weiterzählen:** die Klasse ist dreimal aufgetreten und jedes Mal von einem Menschen bzw. Reviewer gefunden, nie von einem Gate — die naheliegende Form ist die Regel „vor dem Ändern einer Semantik ihre Spiegel auflisten" in der Autoritäts-Doku, mit der Liste der Spiegel (Anforderung · Algorithmus · §2-Schema · §4-Tabelle · Klartexte · print-config-Vorlage · Handbuch · ADR). **Verkörpert vor slice-099** als [`MR-025`](../../../harness/conventions.md#mr-025--semantik-änderung-die-spiegel-vor-dem-editieren-auflisten) — und dort zum ersten Mal angewandt. Sie hat gewirkt (drei Spiegel standen auf der Liste und wären sonst stehengeblieben) und war zugleich **selbst lückenhaft**: vier Lücken fand der Review, eine fünfte die Re-Review. Die fünfte hat die Regel geschärft, statt sie zu bestätigen — sie lag in genau der Datei, die ohnehin bearbeitet wurde, also dort, wo kein Datei-`grep` sucht. Antwort: wo eine Aufzählung eine Menge spiegelt, gehört sie an ihre Quelle **gebunden**; die Liste ist der Notbehelf für den Rest. **Bleibt offen** — eine Regel für Menschen kann weiter verfehlt werden |
| BEO-001 | Ein Datei-Register und seine Autoritäts-Tabelle driften **unbemerkt** auseinander: [ADR-0047](../adr/0047-matrix-spec-historie-nicht-provenance-exempt.md) war sieben Tage `Accepted`, ohne im [ADR-Index](../adr/README.md) zu stehen (`AGENTS.md` §5 verlangt die Zeile). Kein Gate deckt die Richtung „Artefakt ⇒ registriert" ab — `links` prüft nur die Gegenrichtung (Index-Zeile ⇒ Datei existiert). **Dieselbe Bauform** tragen der Konventionsspeicher-Index und das Wellen-Register der Roadmap; beide sind heute konsistent, aber ebenso ungeprüft | `*` | 2 | [slice-087](done/slice-087-spec-historie-referenzrichtung.md), [slice-099](done/slice-099-structure-modul.md) | **eingetreten wie vorausgesagt.** Gefunden 2026-08-09 während slice-093, **bewusst nicht** dorthin gezogen (WIP-Limit, und slice-093 ist bereits Produkt-Code + Release). Vorschlag für einen späteren Slice: opt-in Modul (Arbeitsname `registry`) mit `[{dir, file-pattern, authority}]`, **eine** Richtung, ein Grund-Code — Größenordnung `tracked`, deckt alle drei Register mit einem Config-Block. Abgrenzung: **kein** Ausbau von `targets` (dessen Eingabe-Achse sind Makefile-Regeln; ein Datei-Register wäre ein zweiter, disjunkter Eingabe-Modus). **Zweites Eintreten in slice-099, genau an der benannten Stelle:** die Beobachtung nannte das Wellen-Register der Roadmap ausdrücklich als „heute konsistent, aber ebenso ungeprüft“ — drei Wellen-Closures (70, 71, 72) haben ihre Zeile in §Abgeschlossene Wellen nie bekommen, obwohl alle drei ihre Ergebnisnotiz geschrieben haben. Nachgetragen. Der `registry`-Vorschlag ist damit **gemessen** statt hypothetisch: die Richtung „Artefakt ⇒ registriert“ fehlt, und ihr Fehlen kostet |

## Gestrichene Einträge

Regeln dieser Sektion: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer eine Zeile still löscht, macht sie ununterscheidbar von
einer, die es nie gab; darum hierher verschieben (mit Begründung), nicht löschen.

| Kennung | Beobachtung | Gestrichen am | Warum sie nicht mehr auftreten kann |
|---|---|---|---|
| — keine — | | | |
