# Beobachtungs-Register

Regeln dieses Registers: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer schreibt (Slice-Closure), wer liest (Welle-Closure =
Lese-Schritt bei 3×; Slice-Planung = Sichtungs-Schritt darunter), wann gestrichen wird,
welche Form ein Beleg hat (Slice-Kennung, so viele wie der Zähler, in `done/`), und dass
eine leere Tabelle `— keine —` trägt statt zu verschwinden.

| Kennung | Beobachtung | Sub-Area | Zähler | Belege | Stand |
|---|---|---|---|---|---|
| BEO-025 | **Ein Liefer-Punkt landet im Commit eines FREMDEN Slice.** Die Arbeit ist getan und geprüft, aber die Botschaft trägt eine andere Kennung — die Spur bricht genau dort, wo sie gebraucht wird: `git log --grep=slice-<NNN>` findet den eigenen Liefer-Punkt nicht mehr, und wer die Closure gegenprüft, sieht eine Zusage ohne Commit. **Der Unterschied zu [`BEO-009`](observations.md):** dort behauptet eine Botschaft zu viel, hier ist sie inhaltlich richtig und nur am falschen Vorgang befestigt. **Nicht reparierbar, sobald gepusht** — und kein Gate sieht es: `commits` prüft, dass eine Kennung dasteht, nicht ob es die richtige ist. **Ableiter:** vor dem Editieren jede Änderung ihrem Commit zuordnen, und beim Wechsel zwischen zwei Slices in einer Sitzung zuerst den offenen Stand committen | `*` | 1 | [slice-174](done/slice-174-register-deckung.md) | **Erstauftreten.** Handbuch §4.20 und die `planning`-Modultabellenzeile — beide Liefer-Punkte von [slice-174](done/slice-174-register-deckung.md) — liegen in `a4bc209`, dem Commit, der [slice-187](done/slice-187-handbuch-url-beide-formen.md) schneidet; die Botschaft nennt weder `slice-174` noch seine Anforderung. Gefunden hat es die **Verifikation**, nicht der Schreibende, und sie nennt die Folge: der Slice hat einen Liefer-Punkt ohne eigene Commit-Spur. Anlass war ein Kontext-Wechsel mitten in der Arbeit — ein eingehender Hinweis, der einen zweiten Slice schnitt, während der erste offen lag. Die Klasse steht schon als Erfahrung im Gedächtnis („vor dem Editieren jede Änderung ihrem Commit zuordnen") und ist trotzdem eingetreten; deshalb bekommt sie eine Kennung |
| BEO-024 | **Ein Zustell-Kanal hängt an der ARBEITSWEISE, die Regel aber am Inhalt.** Eine Anweisung wird an eine Bedingung geknüpft, die nach dem Gegenstand klingt — *„lädt, wenn mit passenden Dateien gearbeitet wird"* —, tatsächlich aber am **Werkzeugweg** hängt: sie greift nur, wenn die Datei über bestimmte Werkzeuge angefasst wird, und schweigt bei jedem anderen Zugriff. Wer die Bedingung liest, hört den Gegenstand; wer sie misst, findet den Weg. **Von außen ist beides grün** — der Kanal existiert, die Datei ist da, nichts meldet. **Der Unterschied zu `BEO-022`:** dort fehlt die Zustellung ganz, hier gibt es sie und sie feuert nur manchmal. **Der Unterschied zu `BEO-023`:** dort behauptet ein Wächter eine Prüfung, hier behauptet ein Kanal eine Reichweite — dieselbe Gestalt eine Ebene höher. **Die Klasse ist nicht auf Werkzeug-Konfiguration beschränkt:** jede Bedingung, die nach dem WAS klingt und am WIE hängt, gehört hierher — ein Gate, das nur bei bestimmten Commit-Formen läuft, eine Regel, die nur im interaktiven Modus greift | `*` | 2 | [slice-176](done/slice-176-planning-rule-pilot.md), [slice-186](done/slice-186-vcs-rename-im-range.md) | **Erste Instanz, vor dem Bau erkannt — und das ist die interessante Hälfte.** [slice-176](done/slice-176-planning-rule-pilot.md) war auf `.claude/rules/` mit `paths`-Frontmatter geschnitten, in der Annahme, die Regel lade beim Arbeiten an passenden Dateien. Tatsächlich lädt sie im Auto-Modus nur beim Zugriff über die **dedizierten** Werkzeuge; jeder Shell-Zugriff (`cat`, `sed`, `awk`) geht daran vorbei. **Dieses Repo trifft das besonders**, weil der Auto-Modus zur Shell rät und die Planungs-Dateien überwiegend so geschrieben werden — im Anlassfall hätte der Kanal geschwiegen. **Antwort, gemessen statt vermutet:** nicht Hooks, sondern **Regeln ohne `paths`** — ein Eintrag ohne diese Bedingung lädt beim Sitzungsstart und hängt an keinem Werkzeugweg. [slice-176](done/slice-176-planning-rule-pilot.md) liefert so — vier Symlinks, beim Sitzungsstart geladen —, der Hook bleibt der nachrangige Ausweg und hat seit der Closure ein Kriterium statt einer Meinung: er lohnt, wenn der Preis drückt, und der ist mit **29,4k Token** beziffert. **Der Befund dieser Klasse steht damit nicht anders da:** die Bedingung war falsch verstanden, und dass die Abhilfe schließlich im selben Kanal lag, ändert nichts an der Frage, die zu stellen war. **Prozedur, daraus abgeleitet:** wer einen Zustell- oder Prüf-Kanal wählt, fragt nicht „wann greift er?", sondern „**wovon** hängt sein Greifen ab — vom Gegenstand oder vom Weg dorthin?", und misst die Antwort, bevor er baut. **Kein Gate fängt das** und keines kann: der Kanal ist korrekt konfiguriert, nur seine Reichweite ist enger als ihr Name. **Eintritts-Bedingung für die zweite Instanz:** eine Zusage, deren Greifen sich mit der Arbeitsweise ändert, ohne dass die Zusage das sagt **Zweite Instanz, [slice-186](done/slice-186-vcs-rename-im-range.md) — und sie kam von außen:** ein Adopter meldete, dass [`DC-FA-VCS-001`](../../../spec/lastenheft.md#dc-fa-vcs-001--git-diff-immutabilität-des-core-über-eine-commit-range-modul-vcs-opt-in) den Befund für die umbenannte immutable Datei über `--staged` liefert und über `--range` nicht — reproduziert, alle vier Fälle. Das ist der Satz dieses Eintrags als Code: *ein Gate, das nur bei bestimmten Commit-Formen läuft*. **Neu an dieser Instanz sind zwei Dinge.** Erstens die Ebene: die Reichweite hing nicht an einer Konfiguration, sondern an der **Diff-Übersetzung** unter dem Modul — zwei Modi, zwei Übersetzungen, und nur eine löste die Zusage ein. Zweitens der Weg zur Fehl-Einschätzung: **die Zugehörigkeit zu diesem Eintrag wurde zunächst verneint**, weil er nach seinem Titel (*Zustell-Kanal*) beurteilt wurde statt nach seinem Geltungssatz, der die Klasse ausdrücklich über Werkzeug-Konfiguration hinaus öffnet — `BEO-012` im Akt der Beurteilung einer anderen Kennung. **Abgrenzung, wie der Eintrag sie selbst zieht:** `BEO-023` wäre *ein Wächter behauptet eine Prüfung*; hier behauptet die **Reichweite** eines Moduls mehr, als sein Aufruf-Modus hält. Gezählt wird deshalb hier und nicht dort. **Ableiter:** wo eine Prüfung mehrere Eingabe-Modi kennt, ist die Gleichheit ihrer Antworten eine **gehaltene** Eigenschaft — sie gehört gemessen, nicht angenommen. |
| BEO-023 | **Ein Wächter, der nie fangen konnte, liest sich wie einer, der fängt.** Ein Test trägt in Name und Kommentar eine Zusage und misst sie nicht: die Prüfung steht als Protokoll-Ausgabe statt als Assertion, oder die Fixture erzeugt gar nicht den Zustand, über den der Name spricht. Beides ist **grün** und damit von einem echten Wächter nicht zu unterscheiden — schlimmer als kein Test, weil er die Lücke besetzt. **Der Unterschied zu `BEO-013`:** dort hört ein Wächter auf zu fangen, hier hat er nie gefangen; und der Unterschied zu `BEO-019`: dort ist der **Prüfling** nichtdeterministisch, hier ist die **Probe** wirkungslos. **Die Klasse ist nicht auf Tests beschränkt** — sie trifft jede Prüfung, deren Erfolgsfall der Normalzustand ist: eine Regel, die auf nichts zeigt, ein `forbid-pattern` mit einem Anker, der nie greift, ein Gate über eine leere Menge. Dieses Repo hat für die Regel-Seite bereits Antworten (`section-missing` bei leerer Kandidaten-Menge, fail-closed bei leerer Prüfmenge); für die **Test**-Seite gab es keine | `*` | 7 | [slice-177](done/slice-177-structure-hint.md), [slice-178](done/slice-178-offene-tasks-roh.md), [slice-179](done/slice-179-strukture-teilmenge.md), [slice-180](done/slice-180-closure-profil-spans.md), [slice-181](done/slice-181-handbuch-link-in-cli.md), [slice-182](done/slice-182-erklaerte-leermenge.md), [slice-178](done/slice-178-offene-tasks-roh.md) | **Erste Instanz, gemessen — und die Messung ist zugleich die Antwort.** Ein Test namens `…OhneErlaeuterungDreiSpalten` protokollierte die Spaltenzahl mit `t.Logf` statt sie zu prüfen, und seine Fixture lieferte real **vier** Spalten. Gefunden hat es nicht das Lesen, sondern eine **Mutationsprobe** der Verifikation: die geprüfte Bedingung wurde durch `if true` ersetzt, und die gesamte Suite blieb grün. **Prozedur, daraus abgeleitet:** wer einen Wächter für eine benannte Zusage schreibt, dreht die Zusage **einmal um** und belegt, dass genau ein Test rot wird — die Probe kostet einen Lauf und ist der einzige Beleg, dass der Wächter beißt. In slice-177 nachgeholt: drei Mutationen, je ein roter Test. **Kein Gate fängt das** und keines kann — ein Test ohne Assertion ist syntaktisch tadellos; Coverage misst Ausführung, nicht Behauptung. **Eintritts-Bedingung für die zweite Instanz:** ein Test, dessen Umkehrung grün bleibt. **Zweite Instanz, [slice-178](done/slice-178-offene-tasks-roh.md) — und sie liegt im Wächter gegen diese Klasse selbst, einen Tag nach ihrer Registrierung.** Ein Test namens `…UeberlebtUngeradeBacktickParitaet` trug ein Fixture mit `` `erledigt ` `` — **zwei** Backticks, also eine wohlgeformte Spanne. Nachgemessen: der **Vorgänger-Mechanismus** findet in genau dieser Datei den offenen Haken. Der Test belegte „Befund auf der Item-Zeile", nicht „überlebt die Blindstelle"; für den tatsächlich blinden Fall gab es keinen Test, nur eine Ad-hoc-Messung in der Commit-Botschaft. **Neu an dieser Instanz:** die Umkehr-Probe war gefahren und grün — sie prüft, ob ein Test **beißt**, nicht ob er das **Richtige** beißt. Ein Test mit falschem Fixture überlebt jede Mutation seines Prüflings, weil er ihn gar nicht trifft. **Prozedur, ergänzt:** neben „dreh die Zusage um" gehört „zeig, dass der **Vorgänger** an diesem Fixture scheitert" — ein Regressions-Test ohne belegte Regression ist keiner. Zweite Lücke derselben Instanz: die zugesagte Eingrenzung „im Abschnitt" hielt kein Test; die Mutation „über die ganze Datei zählen" lief grün durch. **Dritte Instanz, [slice-179](done/slice-179-strukture-teilmenge.md) — Schwelle erreicht, Zähler 3:** der Test `TestExemptSectionPattern_FenceTreu` richtete sein Ausnahme-Muster auf die **gefencte** Überschrift. Damit war *„nie gewählt"* von *„gewählt und dann ausgenommen"* am Ergebnis nicht unterscheidbar, und eine Fence-Blindheits-Mutation lief grün durch. **Neu und bitter an dieser Instanz:** der Kommentar über genau diesem Test schrieb *„eine geerbte Zusage ohne Probe ist eine Erinnerung"* — die Klasse war beim Schreiben benannt und wurde im selben Atemzug begangen; die ergänzte Prozedur der zweiten Instanz (*zeig, dass der Vorgänger scheitert*) war notiert und nicht angewandt. **Prozedur, ergänzt:** eine Probe für eine **Ausschluss**-Zusage muss so gebaut sein, dass der ausgeschlossene Fall im Ergebnis **sichtbar** würde — zielt der Filter auf denselben Gegenstand, den der Ausschluss entfernt, hebt sich die Prüfung auf. Die Reparatur war eine Zeile (Muster auf die **echte** Überschrift), und die Mutation macht sie jetzt rot. **Vierte Instanz, [slice-180](done/slice-180-closure-profil-spans.md), diesmal als ANTWORT statt als Defekt:** die erste Vorher/Nachher-Probe war schon **vorher** rot und belegte damit nichts; verworfen und durch eine diskriminierende ersetzt (echter `done/`-Slice, vorher grün/Exit 0, nachher rot/Exit 1). Die Verifikation hat sie um eine **Kontrolle** erweitert, die zeigt, dass das Vorher-Grün nicht leer ist. **Prozedur, bestätigt:** eine Probe belegt eine neue Deckung nur, wenn der Vorher-Lauf aus dem **richtigen** Grund grün ist — und das zeigt erst die dritte Datei neben Vorher und Nachher. **Verkörperung steht aus** und fällt an die Closure von [welle-86](welle-86-closure-uebergang-durchsetzen.md) (Lese-Schritt, Baseline-Regelwerk `modul-06`)  **Fünfte Instanz, [slice-181](done/slice-181-handbuch-link-in-cli.md):** ein Test, dessen benannte Hälfte **still schwächer werden kann**. Die „im Kopf"-Zusage schnitt die Ausgabe an einem Anker ab; fehlte der Anker, blieb die ganze Ausgabe stehen und die Zusage kollabierte kommentarlos auf die schwächere Prüfung darüber. Der Review hat es mit einer eigenen Probe belegt (Anker umbenannt, URL ans Ende ⇒ **PASS**). **Neu an dieser Instanz:** nicht der Test war blind, sondern sein **Ausweich-Pfad** — er beißt heute und hörte bei einer harmlosen Umbenennung auf, ohne rot zu werden. **Prozedur, ergänzt:** ein Anker, an dem eine Assertion hängt, gehört selbst in ein `Fatalf` — wer auf die schwächere Form zurückfällt, prüft weiter und sagt nicht, dass er weniger prüft. |
| BEO-022 | **Eine Regel tritt in Kraft, bevor ihre Zustellung existiert.** Der Text ist geschrieben, steht an genau einer Stelle und ist richtig — nur der Ort, an dem sein Adressat ihn suchen würde, schweigt. Die Regel gilt damit für alle und erreicht niemanden; wer nur das Briefing liest, arbeitet weiter nach der alten Form und tut nichts falsch. **Der Unterschied zu `BEO-013`:** dort fängt ein vorhandener Wächter nichts mehr, hier gibt es keinen Lesepfad zum Adressaten — eine Stufe früher als der taube Sensor, und eine andere Frage als „kein Gate": ein Gate hätte die Regel erzwungen, eine Zustellung macht sie **bekannt**. Beides fehlt, aber nur eines davon ist billig. **Die Klasse ist nicht auf Konventionen beschränkt** — sie trifft jede Aussage mit einem benannten Adressaten: eine Adaption, einen Skill-Anker, eine Grenze in einer ADR | `*` | 2 | [slice-171](done/slice-171-vorpruefungen-belegen.md), [slice-177](done/slice-177-structure-hint.md) | **Erste Instanz, gemessen.** [`MR-054`](../../../harness/conventions.md#mr-054) galt ab seinem Commit für jeden neu angelegten Slice-Plan. Erreichbar war er nur über den Index in `harness/conventions.md`; [`AGENTS.md`](../../../AGENTS.md) §5 zählt die drei Vorprüfungen auf, verlinkte für die dritte [`MR-053`](../../../harness/conventions.md#mr-053) und schwieg zu den beiden anderen — genau denen, die die neue Regel betrifft. Gefunden hat es der zweite Leser, nicht der Schreibende. **Behoben** in derselben Closure (Zeiger in `AGENTS.md` §5 und `harness/README.md`), aber die **Reihenfolge** bleibt der Befund: erst galt sie, dann war sie zustellbar. **Kein Gate fängt das** und keines kann — eine Regel ohne Leser ist von einer Regel ohne Anlass nicht unterscheidbar. **Prozedur:** wer eine Adaption schreibt, benennt in derselben Änderung ihren **Adressaten und dessen Lesepfad**; steht der Pfad nicht, ist die Adaption nicht fertig. **Eintritts-Bedingung für die zweite Instanz:** eine Regel wird wirksam, ohne dass der Ort, den ihr Adressat ohnehin liest, sie nennt. **Die Beobachtung zur Zustellung ist beantwortet:** [slice-176](done/slice-176-planning-rule-pilot.md) liefert **nicht** pfad-gebunden, sondern beim Sitzungsstart — vier Regelwerk-Module stehen seither im Kontext jeder Hauptsitzung, und `AGENTS.md` §1 nennt sie. Die Klasse ist damit **verschoben, nicht getilgt**: die Zustellung erreicht **keinen Subagenten**-Kontext, also genau die Review- und Verifikations-Rollen, die beide bisherigen Instanzen gefunden haben. **Zweite Instanz, [slice-177](done/slice-177-structure-hint.md):** der neue Konfigurations-Schlüssel `structure[].hint` war lauffähig, dokumentiert und getestet — und fehlte im Gerüst, das `--print-config` ausgibt, also an dem Ort, an dem ein Adopter nach den verfügbaren Schlüsseln sieht. **Neu an dieser Instanz:** der Adressat ist nicht der Lauf, sondern der **Adopter**, und der Lesepfad ist eine **Produkt-Ausgabe**, kein Dokument. Der Slice hatte BEO-022 in seiner Sichtung geführt und die Lücke trotzdem gelassen — die Sichtung schützt nicht, sie erinnert |
| BEO-021 | **Eine Kennung, die einen KANAL ausweist, wird als Bezug auf den INHALT gelesen.** Die Dependabot-Commits tragen `[ADR-0067]` im Präfix, weil das Traceability-Gate von jeder Botschaft eine Kennung verlangt. Die Kennung sagt: *„dieser Commit entsteht aus dem Kanal, den [ADR-0067](../adr/0067-dependabot-als-hebender-kanal.md) erlaubt"* — sie sagt **nicht**, dass die gehobene Bibliothek etwas mit jener Entscheidung zu tun hat. Wer die Kennung wie jede andere liest, folgert einen sachlichen Bezug, den es nicht gibt. **Die Klasse ist allgemeiner als der Anlass:** überall, wo eine ID ein **Verfahren** ausweist statt eines **Gegenstands**, lädt sie zu dieser Fehl-Lesung ein — dieselbe Bewegung wie bei `BEO-012`, nur von der Lese- statt von der Schreib-Seite. **Kein Gate fängt das**, und keines kann: die Kennung ist formal korrekt, nur ihre Reichweite ist enger als üblich. Was dagegen steht, ist geschrieben und nicht gebaut — der Satz *„die Kennung gilt dem Kanal, nicht dem Inhalt des einzelnen Bumps"* in [`AGENTS.md`](../../../AGENTS.md) §5 und in der ADR | `*` | 0 | — | **Noch nicht eingetreten**, als Wachposten geführt: der Ausgang des §5-Punktes von [slice-167](done/slice-167-dependabot.md) war *weiter offen*, und der Kanon verlangt dafür diesen Eintrag. **Eintritts-Bedingung, beobachtbar:** ein Commit, ein Review-Befund oder eine Doku-Zeile begründet etwas mit `[ADR-0067]`, das über *„stammt aus dem Dependabot-Kanal"* hinausgeht. **Zu streichen**, sobald die Dependabot-Commits einen anderen Traceability-Weg haben — dann entfällt der Anlass | <!-- d-check:ignore (die beiden `[ADR-0067]`-Literale sind Praefix-Werte, keine Verweise) -->
| BEO-020 | **Gemessen wird die eigene Menge, ausgesagt wird über die fremde.** Die Frage war, ob ein gepinnter Fremd-Bestand sich bewegt. Gezählt wurde, wie viele verschiedene Werte der Pin **in unserer Historie** trug — also wie oft **wir** gehoben haben. Das Ergebnis („Action-Pins: null Bewegungen", „`distroless`: nie bewegt") las sich wie eine Aussage über upstream und trug eine Entscheidung: eine Achse wurde damit abgelehnt. Die **direkte** Messung — upstream fragen — ergab bei sechs von zwölf Pins Rückstand, darunter beide Action-Pins. **Die Klasse ist nicht „zu wenig gezählt"**: die Zählung war vollständig und richtig, nur über die falsche Grundgesamtheit. Ein Bestand, den nur **wir** ändern, kann über einen Bestand, den nur **andere** ändern, nichts sagen. **Test vor jeder Messung:** wer ändert die Menge, die ich zähle — und wer ändert die, über die ich rede? Sind das zwei verschiedene, ist die Messung kein Beleg. Kein Gate fängt das; die Zahl ist echt, nur ihr Gegenstand ist ein anderer als der behauptete. **Verwandt, aber nicht dasselbe:** `BEO-011` trifft eine Aussage, die aus **zu wenig** Bestand gezogen ist — hier ist genug gezählt, aber das Falsche | `*` | 6 | [slice-142](done/slice-142-freshness-weitere-achsen.md), [slice-160](done/slice-160-reviewer-skill-kontextlast.md), [slice-179](done/slice-179-strukture-teilmenge.md), [slice-180](done/slice-180-closure-profil-spans.md), [slice-182](done/slice-182-erklaerte-leermenge.md), [slice-172](done/slice-172-closure-uebergang-waechtern.md) | Die zweite Instanz ist eine **Variante**: dort war die verwechselte Menge eine **fremde** (unsere Pin-Historie gegen upstream), hier eine **eigene Teilmenge** — gezählt wurden `befund:`-Felder, die nur 104 von 214 Reports führen, ausgesagt wurde über alle Befunde aller Reports. Das `quelle`-Feld, in dem der gesuchte Anker-Name tatsächlich steht, war nicht im Scan, und die handgezählte Vergleichsmenge trug gar kein `befund:`-Feld — die beiden Zahlen, die als Beleg gegeneinanderstanden, hatten **keine Schnittmenge**. **Der Test aus der Erstfassung greift auch hier, aber nur in verschärfter Form:** „wer ändert die Menge" trennt eigene von fremden Beständen und lässt die Teilmengen-Verwechslung durch. Der Zusatz-Test ist billiger: **die gezählte Menge benennen, bevor die Zahl fällt** — nicht „687 Befund-Zeilen", sondern „687 Zeilen mit `befund:`-Feld aus den 104 Reports, die eines führen". Wer den Nenner ausschreiben muss, sieht die Lücke beim Schreiben statt im Review. Zähler **2**, unter der Schwelle — als **Sichtungs-Schritt** der Slice-Planung geführt. **Prozedur, sofort wirksam:** wo eine Entscheidung an „bewegt sich das" hängt, wird der fremde Bestand **gefragt**, nicht die eigene Historie gelesen; wo das nicht geht, trägt die Entscheidung die Grenze offen. Umgesetzt: die zwölf Achsen des Nachtlaufs fragen upstream direkt, und der gemessene Rückstand ist als [slice-161](done/slice-161-sechs-pins-heben.md) geschnitten. **Dritte Instanz, [slice-179](done/slice-179-strukture-teilmenge.md) — Schwelle erreicht, Zähler 3:** eine Vergleichs-Tabelle stellte *„frei 13 Treffer"* neben *„verankert 26 Treffer"* unter die Überschrift „über dieselben 444 Items", maß aber **zwei verschiedene Ausdrücke**. Ein `^` kann die Treffermenge nur verkleinern — die Zeile war damit nicht ungenau, sondern **arithmetisch unmöglich**, und niemandem beim Schreiben aufgefallen. **Neu an dieser Instanz:** die Zahl wanderte in eine **immutable** ADR, in beide Spec-Straten, ins Handbuch und in ein **ausgehendes** Dokument, das einen fremden Adopter zum Umschreiben seiner Konfiguration auffordert — die Reichweite war größer als bei den beiden Vorgängern zusammen. Gefunden von Review und Verifikation **unabhängig voneinander**, keiner der beiden vom Schreibenden. **Prozedur, ergänzt:** eine Tabelle, die zwei Messungen nebeneinanderstellt, benennt in jeder Zeile den **Ausdruck**, nicht nur seine Form — und wer zwei Zeilen als „dasselbe, einmal anders" liest, prüft die Richtung: kann die zweite Zeile größer sein als die erste? **Verkörperung steht aus** und fällt an die Closure von [welle-86](welle-86-closure-uebergang-durchsetzen.md) (Lese-Schritt, Baseline-Regelwerk `modul-06`). **Vierte Instanz, [slice-180](done/slice-180-closure-profil-spans.md) — und sie entstand beim KORRIGIEREN der dritten:** die Richtigstellung an slice-178 §1 ersetzte zwei **richtige** Zahlen (25/45 Backticks im DoD-Abschnitt) durch zwei falsche (21/4), weil der Zähl-Bereich `## 4.`…`## 5.` lief, während beide Slices ihre DoD als **§3** führen — gezählt war also §4 *Risiken*. Dazu ein zweiter Fehlschluss derselben Klasse: *„`spans` meldet nichts, also ist die Exposition null"* — `spans` schweigt bei einer **wohlgeformten** Spanne konstruktionsbedingt, sein Schweigen belegt keine Abwesenheit; repo-weit verschwinden **sechs** `- [ ]` durch wohlgeformte Spannen (alle bewusste Prosa). **Neu an dieser Instanz:** die Klasse trat im Akt ihrer eigenen Korrektur auf, und beide Male fand sie der zweite Leser. **Prozedur, ergänzt:** wer eine Zahl korrigiert, prüft zuerst den **Bereich**, aus dem die alte stammt — eine falsche Zahl und ein falscher Ausschnitt sehen im Ergebnis gleich aus; und ein **schweigender** Sensor ist nie ein Abwesenheits-Beleg, solange nicht gezeigt ist, dass er den Fall überhaupt melden **könnte** |
| BEO-019 | **Eine Probe auf einen Wettlauf, die zweimal grün läuft, hat nichts widerlegt — und liest sich wie ein Beweis.** Beim Untersuchen eines zeitabhängigen Tests wurde die vermutete Bedingung **erzwungen** statt abgewartet (gleiche Dateilänge **und** per `Chtimes` dieselbe Änderungszeit auf die Nanosekunde). Zwei Läufe grün ⇒ Schluss: *„die Deutung ist widerlegt, die Ursache ist unbekannt"*. Der **dritte** Lauf desselben unveränderten Codes meldete `cannot create empty commit: clean working tree` — die Deutung war richtig, und die Reparatur wäre um ein Haar als „auf Verdacht" unterblieben. **Die Klasse ist nicht „zu wenig Wiederholungen"**, sondern: *bei einem nichtdeterministischen Prüfling ist Grün kein Ergebnis*. Nur Rot trägt eine Aussage; Grün trägt sie erst, wenn der Prüfling deterministisch gemacht wurde | `*` | 1 | [slice-141](done/slice-141-fixture-racily-clean.md) | offen, Zähler **1**, unter der Schwelle — als **Sichtungs-Schritt** der Slice-Planung geführt. **Prozedur, sofort wirksam:** wer eine Rennbedingung untersucht, formuliert die Probe so, dass sie **deterministisch rot** wird, wenn die Bedingung besteht — oder verlagert die Prüfung dorthin, wo sie nicht mehr auf Zeit angewiesen ist. Umgesetzt: statt eines Tests, der auf die Kollision wartet, lehnt [`coretest.GitFixtureRewriteHazard`](../../../internal/hexagon/core/coretest/gitfixture.go) eine gleich lange Neuschreibung **beim Schreiben** ab, mit eigenem Negativ-Selbsttest. **Verwandt, aber nicht dasselbe:** `BEO-017` trifft ein Rot aus dem falschen Grund — hier trägt ein **Grün** gar keinen Grund |
| BEO-018 | **Eine Härtung am Rand eines Wächters kippt seine Fehlerpolitik, und die Proben sehen es nicht, weil sie einen anderen Kanal bespielen als der Ernstfall.** Die Härtung galt der Zusage *fail-closed*: eine zweite Host-Abhängigkeit fiel weg, damit ein fehlendes Werkzeug den Wächter nicht mehr ohne Urteil enden lässt. Der neue Leseweg funktionierte über eine Pipe — und nur dort; unter `set -e` beendete er den Wächter still, sobald die Eingabe anders ankam. Ergebnis: **jeder** Befehl lief durch, einen Arbeitstag lang, während an derselben Datei die Grenzen geschärft wurden. Kein Sensor sagte etwas, denn die Proben fuhren genau die eine Form, die noch ging; entdeckt wurde es per Zufall, an einem Befehl, der versehentlich einen blockierten Interpreter enthielt. **Die Klasse ist nicht „Regression"**, sondern: *ein Prüfling, den man am Rand anfasst, wird an einer Stelle geprüft, die der Rand nicht berührt* | `*` | 1 | [slice-156](done/slice-156-hook-antwortform.md) | **Zähler 1**, unter der Schwelle — als Lese-Schritt geführt, nicht als Regel. **Prozedur, sofort wirksam:** wer den Rand eines Wächters ändert (Eingabe, Ausgabe, Fehlerpolitik, Aufruf-Umgebung), fährt eine Probe **je Form**, die dieser Rand annehmen kann, und stellt die Regression in einer Kopie **wieder her**, um zu sehen, ob die Probe sie fängt. Umgesetzt: die Leseweg-Proben in [`guard-probe.sh`](../../../tools/harness/guard-probe.sh) fahren fünf stdin-Formen, und die Gegenprobe mit wiederhergestellter Regression meldet `crash`. **Verwandt, aber nicht dasselbe:** `BEO-007` trifft den verworfenen Exit *im Sensor*, `BEO-017` das Rot aus dem falschen Grund — hier schweigt der Sensor aus dem falschen Grund |
| BEO-017 | **Ein Gate wird rot, und der Grund ist ein anderer als der geprüfte.** Der Nachweis *„die Regel greift"* stützt sich auf den Exit, nicht auf die gelesene Ursache — und ein roter Exit ist erstaunlich leicht aus dem falschen Grund zu bekommen. Drei Gestalten, alle belegt: eine Probe nennt den **falschen Linter** und wird vom Nachbarn rot; eine Direktiv-Form gilt im Text als *wohlgeformt* und war **nie gefahren**; eine Probe wird ans Dateiende **angehängt** und landet im einzigen Abschnitt, den die Prüfung ausnimmt — der Wächter schweigt zu Recht und wird beinahe als „greift nicht" verbucht. Die Gegenrichtung gehört dazu: der **unveränderte Bestand**, auf dem der Sensor schweigen muss | `*` | 3 | [slice-132](done/slice-132-hard-rule-zensus.md), [slice-134](done/slice-134-nolintlint.md), [slice-138](done/slice-138-matrix-wellen-klasse.md) | **Schwelle erreicht**, Zähler **3** — verteilt auf **zwei** aufeinanderfolgende Arbeitstage (zweimal 2026-08-23, einmal 2026-08-25), in einem Repo, das diese Disziplin ernst nimmt. **Der erste Anlauf dieser Zeile schrieb „ein einziger Arbeitstag"** — dieselbe Dichte-Behauptung, die ein Review zwei Stunden zuvor an derselben Instanz-Menge widerlegt hatte; sie stammt aus demselben Quelltext und wurde beim Abschreiben nicht mitkorrigiert. **Der Kanon trägt die Regel seit `v5.12.0`** (`modul-13` §Fitness Function aus einem ADR-Satz, Schritt 6: *„das Rot muss von dieser Regel kommen … die Meldung nennt die gebrochene Regel und die Fundstelle, und beides gehört angesehen"*) — sie kam auf den Konsumenten-CR dieses Repos zustande. **Darum hier keine eigene Regelzeile:** sie zu duplizieren verstieße gegen [`AGENTS.md`](../../../AGENTS.md) §1. **Kein Gate fängt das und keines kann es** — ob ein roter Exit vom geprüften Grund kommt, ist ein Urteil über eine Meldung, kein `grep`. **Prozedur:** wer einen Wächter probt, liest die Meldung, nicht den Exit; wer eine Probe anhängt, prüft vorher, in welchem Abschnitt sie landet; und jede Probe hat eine Gegenprobe auf dem unveränderten Bestand |
| BEO-016 | **Prosa verschwindet in einer absatzweiten Inline-Code-Spanne — und kein Gate sieht es.** Die geteilte Lexik paart Backticks **je Absatz**, nicht je Zeile (`stripInlineCodeByLine`); zwei Backticks in verschiedenen Zeilen desselben Absatzes ziehen damit eine Spanne über alles dazwischen auf und leeren es positionserhaltend. Für **jedes** prosa-lesende Modul ist dieser Text danach nicht vorhanden. Das ist als Markdown-Lesart **korrekt** — der Schaden entsteht, wenn die Spanne unbeabsichtigt ist: das Dokument rendert falsch, und die Prüfung, die dort etwas finden sollte, schweigt. `spans` deckt es **nicht**: sein `span-unclosed` meldet **ungerade** Parität, und dieser Fall hat gerade | `*` | 2 | [slice-143](done/slice-143-structure-abschnitts-skopus.md), [slice-178](done/slice-178-offene-tasks-roh.md) | offen, Zähler **1**. **Gemessen an der Ablösung des Closure-Wächters:** dieselbe eingefügte Risikozeile ergibt beim abgelösten Skript (Backtick-Paarung je Zeile) Exit 1 und bei der Regel auf dem bereinigten Text Exit 0 — die Differenz ist in [ADR-0059](../adr/0059-closure-waechter-weicht-structure-regel.md) ausgewiesen. **Verwandt, aber nicht dieselbe Richtung:** in einem Schwester-Repo erzeugt **ungerade** Backtick-Parität im Absatz einen Fehl-Befund des Moduls `ids`; dort wird zu viel gesehen, hier zu wenig. **Mechanische Form, offen:** ein Sensor auf **mehrzeilige** Inline-Code-Spannen wäre baubar, hätte aber in einem Repo mit langen Prosa-Absätzen eine zu messende Falsch-Positiv-Last — die Entscheidung gehört gemessen, nicht aus dem Zähler abgeleitet. **Prozedur bis dahin:** wer einen Wächter auf bereinigten Text stellt, prüft eine Probe **innerhalb** einer mehrzeiligen Spanne mit. **Zweite Instanz, [slice-178](done/slice-178-offene-tasks-roh.md).** Eine Bedingung wurde ausdrücklich gebaut, um die absatzweise Paarung zu umgehen — und die Beobachtung, die genau diese Klasse führt, stand **nicht** in ihrer Sichtung. Sie hätte die Prozedur geliefert, die der Slice dann ad hoc erfand. **Neu daran:** der Defekt trat nicht in der gebauten Bedingung auf, sondern in ihrem **Beleg** — das Fixture des Regressions-Tests trug eine wohlgeformte Spanne statt einer unpaarigen. Wer die Klasse kennt, wählt das Fixture anders. **Und die Klasse ist breiter als gedacht:** derselbe stille Ausfall entsteht über einen vergessenen **Schluss-Fence**, nicht nur über einen Backtick; im Closure-Profil, das `spans` nicht führt, fängt ihn niemand |   <!-- d-check:cite .harness/baseline/v5.18.0/regelwerk/modul-05-planning-harness.md:142-142 -->
| BEO-015 | **Ein offener Punkt bekommt bei der Closure einen vierten Ausgang, den es nicht gibt.** Der Kanon nennt **drei** — *eingetreten* → Carveout oder **Folge-Slice mit ID** · *entfallen* → gestrichen mit Begründung · *weiter offen* → **ins Beobachtungs-Register** — und schließt mit dem Satz *„Ein Slice geht nicht nach `done/`, während ein Risiko ohne Ausgang dasteht"*. Der vierte, erfundene Ausgang ist ein Absatz in der Closure-Notiz („offen und benannt: X"). Er sieht aus wie Buchführung, ist aber Ablage: `done/` wird nach `modul-06` dem Implementer-Agenten **nicht geladen**, und `ls open/` meldet „nichts offen", während die Archiv-Dokumente etwas anderes sagen | `docs/plan/planning/` | 3 | [slice-136](done/slice-136-agents-34-klaerung.md), [slice-137](done/slice-137-toolchain-freshness.md), [slice-138](done/slice-138-matrix-wellen-klasse.md) | **Schwelle erreicht**, Zähler **3** — drei Closures in Folge. **Gemessen:** über alle 18 Ergebnisnotizen fehlt **keine** genannte Slice-Datei, aber **13** Punkte benennen Arbeit ohne Kennung; Häufung in welle-82/83/84. **Die mechanische Form muss nicht erfunden werden — der Kanon spezifiziert sie**: die maschinelle Hälfte der Register-Paarung, *„ob eine in `done/` zitierte `BEO-<NNN>` eine Registerzeile hat **und ob jede Registerzeile mindestens einen Beleg trägt**"*, dazu drei **urteilsfreie** Beleg-Prüfungen — **Form** (`slice-<NNN>`, kein Freitext) · **Anzahl** (so viele wie der Zähler) · **Lage** (in `done/`, geprüft **nach** dem `git mv`). Der Kanon überlässt ausdrücklich nur das Werkzeug dem Repo. **Wir haben davon nichts:** `planning` prüft Lifecycle-Invarianten, `ids` erzwingt Linkpflicht nur für `ADR-`/`MR-`/`DC-` (Nachrüsten von `slice-`/`BEO-` erzeugte **962** bzw. **295** Befunde), `structure` sieht nur Überschriften. **Benannte Grenze des Kanons:** die *Existenz* der Slice-Datei wird nicht verlangt — ein erfundenes `slice-999` bleibt unentdeckt. **Prozedur ab sofort:** jeder offene Punkt bekommt bei der Closure einen der **drei** Ausgänge; „in die Notiz schreiben" ist keiner |
| BEO-014 | **Ein Test-Fixture ändert Inhalt, ohne die Größe zu ändern — und faellt darum sporadisch aus.** Beobachtet an `TestRangeAndFileAt` (`internal/adapter/driven/git`): das Fixture schreibt dieselbe Datei von `"v1\n"` auf `"v2\n"` und committet sofort. Beide Fassungen sind **drei Byte** lang und entstehen in **derselben Sekunde**; eine stat-basierte Änderungserkennung darf die Datei dann als unverändert führen (*racily clean*). Der Lauf meldete `keep.md: M erwartet, got ok=false` — der unmittelbar folgende `make fullbuild` auf **demselben Arbeitsbaum** war grün. Die Klasse ist breiter als der eine Test: jedes git-Fixture, das Änderung über gleich lange Inhalte in einem Zug herstellt, traegt sie | `internal/adapter/driven/git` | 1 | [slice-138](done/slice-138-matrix-wellen-klasse.md) | **erledigt** durch [slice-141](done/slice-141-fixture-racily-clean.md), Zähler **1**. **Gemessener Bestand: zehn** Fundstellen in acht Testfunktionen und zwei Paketen — nicht die eine, mit der die Klasse auffiel; drei aufeinanderfolgende Zählungen waren zu kurz. Alle zehn tragen unterschiedlich lange Inhalte, und **beide** Fixture-Helfer rufen denselben Wächter (`coretest.GitFixtureRewriteHazard`), der eine gleich lange Neuschreibung **beim Schreiben** ablehnt. Ursache belegt über die gelesene Meldung `cannot create empty commit: clean working tree`, nicht über einen grünen Lauf. **Benannte Grenze:** gleiche Länge ist notwendig, nicht hinreichend — der Wächter ist konservativ; und er misst den Inhalt auf der Platte, während die Gefahr am Index-Eintrag hängt. **Warum es zaehlt:** der Test haengt in `make gates`. Ein Gate, das gelegentlich ohne Grund rot wird, erodiert die Zusage „grün heisst geprüft" schneller als eines, das gar nicht existiert |
| BEO-013 | **Ein Unterdrückungs-Marker, der nichts mehr unterdrückt, bleibt stehen — und deckt lautlos den nächsten Befund.** Ein Marker wird gegen **einen** konkreten Befund gesetzt. Verschwindet dieser (Text geändert, Regel geschärft, Ziel repariert), bleibt der Marker; von da an schweigt die Zeile über etwas, das niemand stummschalten wollte. Für Go ist die Frage seit `nolintlint` mit `allow-unused: false` **gemessen**; für den eigenen Marker gibt es kein Gegenstück. **Der Bestand ist inzwischen gemessen, und er ist ein anderer als beim Anlegen dieser Zeile geschätzt:** nicht zwölf Marker, sondern **233** Zeilen außerhalb von Fences, die die Zeichenkette tragen und damit unterdrücken — die Erkennung ist eine blanke Teilketten-Suche, also wirkt **jede Erwähnung** wie ein gesetzter Marker. Nur **87** stehen in der Kommentar-Form, **146** sind Prosa. Lenkt man die Konstante ins Leere, treten **58** Befunde auf **48** Zeilen hervor; die übrigen **185** Marker-Zeilen unterdrücken nichts | `*` | 1 | [slice-134](done/slice-134-nolintlint.md) | offen, Zähler **1**. **Die Vorfrage ist beantwortet** ([slice-146](done/slice-146-ignore-marker-wirkung.md)): das Produkt hält die Information **nicht** — in allen vier Konsumenten ist der Marker ein frühes `continue` **vor** der Prüfung. Erreichbar wäre sie durch Umstellung auf *prüfen, dann unterdrücken*. **Gebaut wird sie trotzdem nicht, und der Grund ist ein tieferliegender Defekt:** solange die Erkennung eine Teilketten-Suche ist, meldete die Regel 185 Zeilen, davon die meisten Doku-Prosa — richtig und nutzlos. Erst muss der Marker eine **Form** haben ([slice-159](done/slice-159-ignore-marker-syntax.md)); danach ist diese Zeile wieder aufzunehmen. **Mechanische Form** (unverändert gültig): ein Gegenstück zu `allow-unused: false` — melden, wenn an einer markierten Zeile **ohne** den Marker gar kein Befund entstünde. Die Klasse ist trotzdem die gleiche, die für Go gerade geschlossen wurde. **Prozedur bis dahin:** wer eine markierte Zeile anfasst, entfernt den Marker probeweise und misst, ob überhaupt noch etwas feuert |
| BEO-012 | **Eine Quelle wird über ihren Geltungsbereich hinaus zitiert.** Der Text der Quelle stimmt, die in Anspruch genommene **Reichweite** nicht: ein Satz, der im Kanon nur für einen benannten Fall gilt, wird als universale Regel geführt; ein `MR-`Eintrag wird nach seinem **Titel** zitiert statt nach seinem Feld `Geltungsbereich`; eine ADR-Entscheidung wird als stehendes Verbot gelesen, obwohl sie einen einmaligen Akt beschreibt. Der Unterschied zu `BEO-011` ist die Richtung: dort wird eine Regel aus dem Anlass **gebildet**, hier wird eine bestehende **gedehnt** — und weil ein Zitat wie ein Beleg aussieht, ist es schwerer zu bemerken als eine unbelegte Behauptung | `*`  | 12 | [slice-127](done/slice-127-claude-md-pointer.md), [slice-130](done/slice-130-lastenheft-historie-form.md), [slice-131](done/slice-131-reviewer-skill-waisen.md), [slice-140](done/slice-140-konsumenten-cr.md), [slice-147](done/slice-147-reviewer-anker-reichweite.md), [slice-171](done/slice-171-vorpruefungen-belegen.md), [slice-177](done/slice-177-structure-hint.md), [slice-181](done/slice-181-handbuch-link-in-cli.md), [slice-182](done/slice-182-erklaerte-leermenge.md), [slice-178](done/slice-178-offene-tasks-roh.md), [slice-185](done/slice-185-print-mk-doc-usage.md), [slice-174](done/slice-174-register-deckung.md) | **Schwelle überschritten.** Die schärfste Instanz: [slice-140](done/slice-140-konsumenten-cr.md) schrieb einen CR **über diese Klasse** und beging sie in dessen Punkt 4 — dazu zweimal im selben Dokument (ein Zitat, das zwei Sätze aus zwei Aufzählungspunkten in umgekehrter Folge zusammenstellte; eine Kanon-Zuschreibung in einem `MR-`Eintrag für einen Vorgang, den der vendorte Baum nicht kennt). **Gefunden hat es jedes Mal der zweite Leser, nie der Schreibende** — das ist die Feedforward-Lücke, nicht die Feedback-Lücke, und beide Hälften tragen seit [slice-147](done/slice-147-reviewer-anker-reichweite.md) einen **Ort in der verkörperten Form**: die **Feedforward**-Hälfte als Hard Rule in [`AGENTS.md`](../../../AGENTS.md) §5, die **Feedback**-Hälfte als MEDIUM-Anker im Reviewer-Skill, als Prüffrage an den Diff statt als Fallliste. **Der Kanon trägt die Regel längst** — `grundlagen-source-precedence.md` stellt die Reichweitenfrage an **jede** zitierte Aussage; die zwei Orte sind ihre Verkörperung, nicht ihre Erfindung. Erstmals gemessen: eine maschinelle Wortlaut-Probe **aller** Blockzitate eines Dokuments gegen die zitierte Quelle findet die Tilgung ohne Auslassungszeichen — die Reichweite selbst findet sie nicht. Ursprünglicher Stand: alle drei ersten Instanzen in welle-83. **Kein Gate fängt das**, und die Wortlaut-Probe des Moduls `citations` deckt es nicht: sie vergleicht Zeichen, nicht Anspruch. **Prozedur:** vor jedem Zitat einer Regel deren Geltungs-Feld lesen — bei einer `MR-<NNN>` das Feld `Geltungsbereich` **und** `Ersetzt-Baseline-Regel`, bei einer Kanon-Stelle den Absatz, in dem sie steht, bei einer ADR die Unterscheidung Akt/Verbot. Wer die Reichweite nicht nachgelesen hat, zitiert nicht, sondern erinnert sich. **Siebte Instanz, [slice-177](done/slice-177-structure-hint.md):** der Plan begründete den Verzicht auf ein zweites Feld mit [ADR-0069](../adr/0069-zellenlaenge-als-strukturbedingung.md) — die hat die Bedingung **eingeführt**; zurückgebaut hat die Redundanz [ADR-0070](../adr/0070-tabellen-klammer-und-spaltenliste.md), weshalb der Vorgänger im Index als teil-superseded geführt wird. Die begleitende ADR und der Feat-Commit nannten an derselben Stelle die richtige. **Sechste Instanz, [slice-171](done/slice-171-vorpruefungen-belegen.md):** der Slice, der die `d-check:cite`-Direktive als Vorprüfungs-Beleg einführt, ankerte eine seiner beiden eigenen Direktiven auf eine **Notier-Nebenregel** statt auf den vorschreibenden Satz — wortgleich, grün, und trotzdem nicht der Beleg, den die Anrede behauptete. **Neu an dieser Instanz:** die Dehnung steckte in einer maschinell geprüften Zeilenspanne, also genau dort, wo das Werkzeug Sicherheit suggeriert. Auch sie fand der zweite Leser. [`MR-054`](../../../harness/conventions.md#mr-054) verlangt seither ausdrücklich die vorschreibende Zeile  **Achte Instanz, [slice-181](done/slice-181-handbuch-link-in-cli.md) — und die breiteste bisher:** dieselbe überdehnte Aussage stand an **fünf** Stellen zugleich (Lastenheft-Akzeptanzkriterium, Lastenheft-Historie, Code-Kommentar, Test-Kommentar, Commit-Botschaft). Behauptet war, [`DC-FA-VER-001`](../../../spec/lastenheft.md#dc-fa-ver-001--versions-pin-konsistenz-modul-versions-opt-in) halte *„ausschließlich `ghcr`-präfixierte Pins"*; gemessen ist dessen Muster **frei konfigurierbar**, und dass dieses Repo es nur auf `ghcr` richtet, steht in seiner `.d-check.yml`. Die direkteste Quelle wäre `docs/user/releasing.md` gewesen, die korrekt vom **Gate** spricht. **Neu und lehrreich:** das Argument war nicht nur zu weit zitiert, sondern das **falsche** — der Fundort ist eine Go-Konstante, und d-check scannt nur Markdown; keine `versions`-Konfiguration könnte sie je erreichen. Der tragende Grund ist die **Scan-Menge**. **Prozedur, ergänzt:** eine Begründung, die aus dem Slice-Plan in Code, Test oder Spec **kopiert** wird, ist an jedem Zielort neu zu prüfen — sie war hier viermal dieselbe und viermal falsch platziert, und kein Gate sieht davon etwas. **Elfte Instanz, [slice-185](done/slice-185-print-mk-doc-usage.md) — und die Quelle widersprach sich selbst:** der Beanspruchungs-Commit führte [`MR-013`](../../../harness/conventions.md#mr-013) für den Übergang `open/` → `in-progress/`, den dessen Feld `Geltungsbereich` nicht nannte. **Neu an dieser Instanz:** die Dehnung stand nicht nur im Zitat — die Übersichtstabelle desselben Dokuments führte die breite Kurzform, das Feld die enge, und 71 von 83 Beanspruchungs-Commits folgten der Tabelle. Wer nur das Feld liest, hält die Praxis für falsch; wer nur die Tabelle liest, zitiert zu weit. **Gefunden hat es der Review** — und er beging die Klasse in derselben Zeile zuerst selbst, bevor er sie auf Rückfrage gegen den geprüften Commit wendete. Aufgelöst durch Erweiterung des Feldes auf beide Richtungen, gemessen statt analogisiert (`planning-drift`, Exit 2). **Ableiter, ergänzt:** widersprechen sich zwei Stellen derselben Quelle, ist die engere die Aussage und die weitere der Befund — nicht umgekehrt. **Zwölfte Instanz, [slice-174](done/slice-174-register-deckung.md) — zweimal im selben Slice, und beide Male war die gedehnte Quelle eine PRÄZEDENZ statt eines Textes:** (1) die Aktivierung wurde aufgeschoben unter Berufung auf `waves.mode: many`, dessen Rücksicht *„ein älterer Prüfer bricht am unbekannten Schlüssel"* bei uns **keinen Anlass** hat — nachgemessen fährt kein Target und kein Workflow ein released Image gegen die Konfiguration. (2) Das Handbuch-Beispiel übernahm die gepinnte Version aus den Nachbar-Aufgaben, obwohl die Fähigkeit in dieser Version **nicht existiert**; die Anleitung endete reproduzierbar mit `field observations not found` und Exit 2. **Neu an dieser Instanz:** die gedehnte Quelle ist kein Satz, sondern ein **Muster** — eine Nachbar-Entscheidung und eine Nachbar-Formulierung, beide übernommen, ohne zu prüfen, ob ihr Anlass am neuen Ort gilt. Gefunden hat beides die Verifikation, keines der Schreibende. **Ableiter, ergänzt:** eine übernommene Präzedenz trägt nur, wenn ihr **Anlass** am neuen Ort nachgewiesen ist — das Muster allein ist kein Beleg. |
| BEO-011 | **Eine Aussage oder Regel wird aus dem ANLASS gezogen statt aus dem BESTAND.** Wer eine Eigenschaft am gerade bearbeiteten Fall beobachtet, erklärt sie für exklusiv, allgemein oder abschließend — ohne die Nachbarn gezählt zu haben. Drei Ausprägungen: (a) eine **Exklusivitäts-Aussage** („das einzige Modul, das …", „als einzige Bedingung", „trägt gar kein Ventil"), (b) ein **Kriterium**, das eine bereits korrigierte Menge nachträglich begründet — die Aufzählung stimmt, die Herleitung trifft auch Nachbarn, die nicht dazugehören —, und (c) eine **Regel, die nur für den Ort geschrieben wird, an dem sie wehtat** statt für ihre Klasse. Kein Gate fängt eines davon: alle drei sind Prosa, und alle drei lesen sich belastbarer als die Messung, auf der sie stehen | `*` | 5 | [slice-123](done/slice-123-structure-heading-muster.md), [slice-124](done/slice-124-diagrams-ventile.md), [slice-125](done/slice-125-release-v0630.md), [slice-140](done/slice-140-konsumenten-cr.md), [slice-185](done/slice-185-print-mk-doc-usage.md) | **Schwelle überschritten**, Zähler **4**. Die vierte Instanz bringt Ausprägung (c) erstmals in die Belege — und aus einer neuen Ecke: [slice-140](done/slice-140-konsumenten-cr.md) schrieb sein §5-Risiko *„könnte sich als Lesefehler erweisen"* für **den einen Punkt**, an dem der Zweifel entstand, statt für seine Klasse (jeder Punkt, der eine Kanon-Lücke behauptet). Für den benannten Punkt lief die Baum-Suche und entkräftete das Risiko; für den ungenannten lief sie nie, und dort saß der Befund. **Ein Risiko-Abschnitt ist selbst eine Regel** — wer ihn nur für den Anlass schreibt, deckt genau die Nachbarn nicht ab, die niemand angesehen hat. Ursprünglicher Stand, Zähler **3** in einer einzigen Welle: zweimal Ausprägung (a) ([slice-123](done/slice-123-structure-heading-muster.md), [slice-124](done/slice-124-diagrams-ventile.md)), einmal (b) ([slice-125](done/slice-125-release-v0630.md)). **Der erste Zählstand dieses Eintrags war 5 und selbst aus dem Anlass gebildet** — ein unabhängiger Review hat ihn widerlegt: [slice-122](done/slice-122-versions-musterliste.md)s „zwei Paare, zwei Befunde" war eine von der geteilten Nachrunde widerlegte Zusage (BEO-004-nah, nicht diese Klasse), und die nach Dokument-Rolle statt nach Aussage gebildete Spiegel-Liste in [slice-125](done/slice-125-release-v0630.md) ist BEO-002. Beide sind herausgenommen; Ausprägung (c) war unter den fünf gar nicht vertreten. **Drei weitere Instanzen liegen in Arbeit** — je eine von (a), (b) und (c), alle im selben Commit entstanden, der diesen Eintrag anlegte, und alle vom Review gefunden; sie werden beim Closure ihres Slice gezählt und bringen (c) erstmals in die Belege. **Ableiter, drei Sätze, alle handlungsförmig:** wer „einzig/nur/alle/gar kein" schreibt, zählt vorher die Nachbarn (`grep` über die Geschwister-Module bzw. -Abschnitte, und beim eigenen Modul auch die **geerbte** Config-Maschinerie — `citations` hat sehr wohl einen `scope`). Wer eine falsche Exklusivität **repariert**, begründet die reparierte Menge **nicht**; sie ist eine *benannte Liste*, kein ableitbares Kriterium, und eine Begründung ist eine neue, ungezählte Behauptung. Wer eine **Regel** aus einem Vorfall schreibt, benennt zuerst die Klasse und erst dann den Ort. **Mechanische Form:** bewusst offen. Für (a) wäre ein Lint auf Exklusivitäts-Wörter denkbar, aber ein Heuristik-Wächter mit hoher Falsch-Positiv-Last in einem Repo, dessen Doku bewusst Grenzen benennt; (b) und (c) sind Urteile, kein `grep`. Verkörpert ist die Klasse zunächst im Reviewer-Skill und in dieser Zeile — die Entscheidung über einen Wortlisten-Lint ist eigens zu treffen, **nicht** aus dem Zähler abzuleiten (was genau der Fehler dieser Beobachtung wäre) **Fünfte Instanz, [slice-185](done/slice-185-print-mk-doc-usage.md), Ausprägung (a):** die Spezifikation sagte an zwei Stellen, `doc-usage` trage „als einziges der dreizehn" keine `--enable`/`--disable`-Wahl. Gezählt: sechs Targets tragen eine Fokus-Liste, sieben nicht — es ist eines von sieben. **Neu daran ist der Ort:** die Aussage stand im technisch verbindlichen Stratum, geschrieben im selben Zug, der die Aufzählung sanierte; wer eine Menge gerade nachzieht, hält sich für zuständig und schreibt eine Eigenschaft dazu, die er nicht gezählt hat. Gefunden vom unabhängigen Review, nicht vom Schreibenden. Der zweite Ableiter dieses Eintrags ist dabei ausdrücklich befolgt: die Reparatur nennt die sechs Nachbarn als **Liste** und leitet kein Kriterium ab, das eine neue ungezählte Behauptung wäre. |
| BEO-010 | **Die Modulliste des eigenen Prüf-Profils hat Spiegel außerhalb der Config — und keiner davon ist gate-gedeckt.** Wer ein Modul in `.d-check.yml` aufnimmt, ändert damit still drei weitere Stellen mit: `FOCUS_DISABLE` im `Makefile` (wählt für die fokussierten Gates alle Datei-Module ab; wächst es nicht mit, **über-feuert** das neue Modul im `pre-commit`-Hook auf ungestagtes WIP), die Netzlos-Modulliste im [`DC-QA-03`](../../../spec/lastenheft.md#dc-qa-03--seiteneffektfreiheit-und-netzwerk-sparsamkeit)-Test und die Prosa-Beschreibungen des Gates in drei Dateien. `gate-consistency` prüft Target-**Namen**, nicht Modul-**Mengen** | `.d-check.yml`, `Makefile`, Gate-Doku | 2 | [slice-115](done/slice-115-arc-vergabe-architektur.md), [slice-152](done/slice-152-citations-scharfschalten.md) | **Schwelle nicht erreicht**, Zähler **2**. Gate-blind, und die zweite Instanz zeigt die Bindung schwächer als notiert: der Netzlos-Guard im Go-Test prüft eine **Teilmenge** (`netlessDocModules() ⊆ modules`) — er fängt ein **entferntes** Modul, aber **kein hinzugefügtes ohne Spiegel**, und blieb deshalb grün, während `FOCUS_DISABLE` vier fokussierte Gates über-feuern ließ (fail-closed, über den ganzen Baum, `adr-check` im `pre-commit`-Hook). Gebunden ist also nur eine **Richtung** einer Hälfte. Mechanische Form bei 3×: Mengen-Abgleich `modules` gegen die `--disable`-Kette in `FOCUS_DISABLE`, **beide** Richtungen, ein Grund-Code — und dieselbe Beidseitigkeit für den Go-Guard|
| BEO-009 | **Eine Commit-Botschaft behauptet mehr, als die Arbeit trägt.** Zwei Richtungen: (a) sie behauptet eine Probe oder Änderung, die **nicht stattfand** — die Botschaft wird vor oder während der Arbeit formuliert und beschreibt den beabsichtigten Zustand; schlägt eine Probe fehl oder greift ein Edit still nicht, reist die Behauptung trotzdem mit. (b) Sie **verallgemeinert über die Messung hinaus** — die genannten Proben liefen und stimmen, aber der Schluss daraus gilt weiter als sie reichen | `*` | 9 | [slice-111](done/slice-111-wave-drift-zwei-haelften.md), [slice-123](done/slice-123-structure-heading-muster.md), [slice-149](done/slice-149-baseline-v5120-delta-audit.md), [slice-150](done/slice-150-pin-gebundene-zitate.md), [slice-144](done/slice-144-commit-hash-muster.md), [slice-177](done/slice-177-structure-hint.md), [slice-183](done/slice-183-baseline-v5150.md), [slice-189](done/slice-189-baseline-v5180.md), [slice-187](done/slice-187-handbuch-url-beide-formen.md) | **Zähler 6**, und beide neuen Instanzen sind Richtung **(a)** — die behauptete Prüfung, die nicht stattfand: [slice-149](done/slice-149-baseline-v5120-delta-audit.md) hakte einen unabhängigen Review ab, der nicht gelaufen war; [slice-150](done/slice-150-pin-gebundene-zitate.md) behauptete *„Nichts entfernt, nichts umgeschrieben"* bei einem Diff mit vier Löschungen. Die sechste ist Richtung **(b)**: ein Konfigurations-Kommentar setzte zwei Rohtext-Zahlen unter die Überschrift *„Gemessen mit dem Produkt, nicht per grep"* — richtig gemessen, falsch gerahmt. **Alle drei fanden sich erst beim Nachlesen bzw. im Review.** *Benannte Form-Lücke dieses Eintrags:* er trug schon bei Zähler 3 nur **zwei** Belege — die Beleg-Form (so viele Kennungen wie der Zähler) ist hier nie erfüllt gewesen; die dritte Instanz ist nicht rekonstruiert, und das steht hier, statt die Zahl stillschweigend passend zu machen. Ursprünglicher Stand, Zähler **3**. Die benannte mechanische Form deckt nur Richtung (a): ein Hook, der die in der Botschaft genannten Datei-Pfade gegen den Staged-Diff hält. Richtung (b) ist **nicht mechanisierbar** — ob ein Schluss über die gemessenen Fälle hinausreicht, ist ein Urteil, kein `grep`; sie steht seit dem Vollständigkeits-Zensus als Regel in [`AGENTS.md`](../../../AGENTS.md) §5, und der Reviewer-Skill trägt dazu den MEDIUM-Anker (*Botschaft verallgemeinert über die Messung hinaus*, Arbeitsanweisung: suche die N+1-te Form) — bis dahin war der Skill ihr einziger Ort und damit eine Waise. Prozedur für beide: Botschaften über Proben erst **nach** deren Exit formulieren, `git diff --stat` gegen jede Botschafts-Zeile halten, und jeden verallgemeinernden Satz auf die gemessene Menge zurückschneiden. **Sechste Instanz, [slice-177](done/slice-177-structure-hint.md):** beide Richtungen in einem Slice. (b) — die Botschaft nannte *„22 von 31 Regel-Dateien"*; die Zahl stimmt als Datei-Zählung eines Literals, misst aber die **falsche Menge**: acht der 31 sind geteilte Helfer ohne Regel, der reichweiten-relevante Schnitt lautet 21 von 23. (a) — sie sagte *„ein Befund bleibt EINE Zeile"* zu, während ein Tabulator im Hinweis fünf Felder erzeugte. **Neu daran:** nicht die Probe fehlte, sondern ihre **Menge** war falsch gewählt — eine Zählung, die läuft und stimmt, kann trotzdem über etwas anderes sprechen als die Behauptung, die sie stützen soll. **Siebte Instanz, [slice-183](done/slice-183-baseline-v5150.md) — und der Ort ist neu:** bisher waren alle Instanzen Commit-Botschaften; hier ist es der eigene Slice-Plan. §2 Schritt 3 hielt eine Vorab-Schätzung ("rund 33 echte Direktiven") unter dem Wort *„Gemessen"*, geschrieben, als der Plan noch auf `v5.14.0` zielte (§1). Bei der Ausführung gegen `v5.15.0` waren es tatsächlich 24 — die richtige Zahl steht seither im DoD (§4), die falsche blieb im Vorgehen-Abschnitt stehen und wurde nicht nachgezogen. **Neu daran:** Richtung (a) trifft normalerweise eine behauptete Probe, die nie lief; hier lief die Messung, nur an zwei Stellen derselben Datei mit unterschiedlichem Ergebnis, und niemand hat die ältere gegen die jüngere geprüft. Gefunden hat es der unabhängige Review, nicht der Schreibende. **Prozedur, ergänzt:** eine als „Gemessen" markierte Zahl im Vorgehen-Abschnitt ist bei Closure gegen die tatsächlich erreichte Zahl im DoD zu prüfen — nicht nur Commit-Botschaften gegen den Diff. **Achte Instanz, slice-189 — eine dritte Spielart von Richtung (a):** die Commit-Botschaft sagte im Präsens *„in der Closure-Notiz benannt statt verschwiegen"*, während §9 des Slice zu diesem Zeitpunkt planmäßig noch leer war — die Zwei-Commit-Konvention (AGENTS.md §3.3) sieht die Closure-Notiz erst im Commit **nach** dem `git mv` vor. **Neu daran:** anders als die ersten sieben Instanzen war die Arbeit nicht ausgeblieben, sondern nur noch nicht fällig — die Botschaft nahm die eigene spätere Erfüllung als bereits geschehen vorweg. Gefunden von der Verifikation, nicht vom Review. **Prozedur, ergänzt:** ein Verweis auf „die Closure-Notiz" in einer Nicht-Closure-Commit-Botschaft gehört in den Konjunktiv oder als Vorhaben markiert, nicht als abgeschlossene Tatsache. **Neunte Instanz, slice-187 — eine vierte Spielart, diesmal in der Commit-REIHENFOLGE statt im Tempus einer einzelnen Botschaft:** der Beanspruchungs-Commit (`e25f8b0`) hakte bereits DoD-Punkte ab und erhöhte das Beobachtungs-Register mit vollständiger Vergangenheitsform-Erzählung ("Umgesetzt: …", "geprüft und tatsächlich ergänzt …") — die Implementierung selbst folgte erst zwei Commits später (`1ae05ca`). Der Inhalt war am Ende korrekt (anders als die ersten sieben Instanzen), aber `git show e25f8b0:internal/adapter/driving/cli/cli.go` zeigt: die referenzierte Konstante gab es zu dem Zeitpunkt nicht. **Neu daran:** die Arbeitsreihenfolge dieser Sitzung lief geplant → implementiert → Beanspruchung nachgetragen, statt Beanspruchung → Implementierung; der Plan-Text wurde dabei aus dem Kopf des bereits fertigen Codes geschrieben und wirkt dadurch wie ein Ergebnis, obwohl die Commits die umgekehrte Reihenfolge zeigen. Gefunden vom Review, nicht vom Schreibenden. **Prozedur, ergänzt:** wer nach der Implementierung eine formale Beanspruchung nachträgt, prüft `git log` auf die tatsächliche Commit-Reihenfolge, nicht nur den Dateiinhalt — ein DoD-Haken und ein Registerbeleg sind Aussagen über einen Commit, nicht nur über einen Dateizustand. **Derselbe Fund, rekursiv, im selben Vorgang:** der Fix-Commit für die neunte Instanz (`96504a4`) begründete den Verzicht auf eine Commit-Umschreibung mit *„nichts gepusht"* — unbelegt, und falsch: `git reflog show origin/main` zeigt, dass `origin/main` bereits **vor** diesem Fix-Commit auf `1ae05ca` stand. Die Schlussfolgerung (keine Umschreibung) blieb richtig, mit der wahren Prämisse sogar zwingender — nur die Begründung war eine unbelegte Tatsachenbehauptung genau der Klasse, die der Commit gerade dokumentierte. Gefunden von der Verifikation, nicht vom Review, nicht vom Schreibenden — **drei** unabhängige Rollen haben in diesem einen Slice denselben Fehlertyp je einmal übersehen, bevor die letzte ihn fing. **Neu daran:** die Rekursion selbst — ein Beleg-Fehler *über* einen Beleg-Fehler, im selben Atemzug geschrieben. Kein separater Zähler-Schritt (derselbe Vorgang), aber eine eigene Zeile, weil die Lehre eine andere ist: **Prozedur, ergänzt:** eine Aussage über den Repo-Zustand („gepusht"/„nicht gepusht", „existiert"/„existiert nicht") ist so belegpflichtig wie jede andere Tatsachenbehauptung — auch und gerade in einem Commit, der einen Beleg-Fehler korrigiert |
| BEO-008 | **Die Spiegel einer Pin-Hebung sind drei Klassen, gehoben wird nur die grep-bare.** `baseline/<tag>`-Pfad-Verweise sind gate-gedeckt und werden beim Bump vollständig retargetet; Release-/Tree-**URLs** mit dem Tag (`releases/tag/`, `releases/download/`, `tree/`) und **Prosa-/Ellipsen-Pins** (`…/vX.Y.Z/…`, „Stand"-Angaben in MR-Körpern) deckt kein Gate und kein Pfad-grep — sie bleiben stehen, teils in sich widersprüchlich in derselben Zeile (Link-Ziel neu, Quell-URL alt) | `*` | 5 | [slice-106](done/slice-106-baseline-v560-vendoring.md), [slice-110](done/slice-110-baseline-v570-bump.md), [slice-117](done/slice-117-baseline-v590-bump.md), [slice-148](done/slice-148-baseline-v5120-vendoring.md), [slice-189](done/slice-189-baseline-v5180.md) | **Schwelle überschritten**, Zähler **4** — und die vierte Instanz zeigt, dass die Zahl im Titel dieses Eintrags zu klein war. **Es gibt eine VIERTE Klasse: der zitierende Verweis.** Ein Verweis zeigt nicht nur auf eine Datei, sondern zitiert ihren **Wortlaut**; beim Bump wird der Pfad mitgehoben, löst sauber auf, und das Zitat daneben existiert am neuen Ziel nicht mehr. Kein Gate sieht es, weil beide Hälften für sich stimmen. In [slice-148](done/slice-148-baseline-v5120-vendoring.md) gemessen: drei Dokumente, sechs Paare, davon **eines lebend** ([`MR-033`](../../../harness/conventions.md#mr-033), zwei Zitate). **Die tragfähige Messform ist eng und benannt:** Zitat gegen die Zeilen, die zwischen den **zwei gepinnten Bäumen** verschwunden sind — ein Korpus-Test („steht das Zitat irgendwo im eigenen Baum?") isoliert die Klasse NICHT, weil die eingefrorenen Dokumente den alten Wortlaut wörtlich führen und echte Brüche damit als Selbstzitat durchgehen. Ursprünglicher Stand, Zähler **3**. Gate-blind in **beide** Richtungen: die vergessene Hebung ebenso wie die Über-Hebung — ein Pfad-`grep` kennt keine Zeitform und hebt Vergangenheits-Aussagen mit. Gegenmittel: der Drei-Klassen-Zensus der Bump-Prozedur, ergänzt um die Gegenprobe, ob eine gehobene Stelle über die Gegenwart oder über die Vergangenheit spricht. **Die benannte mechanische Form ist seit [slice-122](done/slice-122-versions-musterliste.md) baubar:** `versions.patterns` trägt mehrere Muster-Quellen-Paare, ein zweiter Abgleich (Baseline-Tag in URLs und Prosa gegen den §Baseline-Pin) ist damit konfigurierbar. Das eigene Profil fährt ihn **nicht** — das ist ein eigener Entscheid mit eigener Messung, kein Rest dieses Slice. **Fünfte Instanz, slice-189 — beide benannten Klassen zugleich, in zwei Wellen gefunden:** fünf externe Release-/Tree-URLs blieben nach dem Bump auf `v5.15.0` stehen (`AGENTS.md`, `harness/conventions.md` ×2, `harness/README.md`, [`MR-021`](../../../harness/conventions.md#mr-021)) — derselbe Grep nach `baseline/v5\.15\.0` fängt sie nicht, weil sie kein `baseline/`-Pfadsegment tragen; gefunden vom unabhängigen Review. **Danach fand die Verifikation, unabhängig vom Review, zwei weitere Prosa-Pins** (`docs/plan/planning/in-progress/roadmap.md` §Offene Wellen, `.d-check.yml`s `planning`-Sektionskommentar) — beide nennen die Baseline-Version als Klammer-Zusatz in Prosa, ohne Pfad oder URL. **Neu daran:** derselbe Bump-Vorgang lieferte beide Spiegel-Klassen aus dem Eintrags-Titel nacheinander, nicht auf einen Schlag — ein einmaliger Zensus nach der jeweils zuletzt gefundenen Klasse hätte die andere übersehen. `versions.patterns` bliebe die mechanische Antwort für beide, weiterhin ungebaut |
| BEO-007 | **Ein Gate-Aufruf hinter einer Pipe meldet den Exit des letzten Pipe-Glieds.** `make gates 2>&1 \| tail` liefert den Exit von `tail` — die `&&`-Kette läuft weiter, ein roter Lauf wird als grün behandelt, und die Botschaft behauptet Gates, die nie bestanden wurden. Der Stop-Hook fängt nur das Sitzungs-Ende, nicht den Commit dazwischen | `*` | 3 | [slice-107](done/slice-107-baseline-v560-delta-audit.md), [slice-108](done/slice-108-roadmap-offene-wellen.md), [slice-111](done/slice-111-wave-drift-zwei-haelften.md) | **verkörpert in welle-79**, Zähler **3**. Die Form steht an der Stelle mit Biss: der `pre-commit`-Hook läuft zusätzlich den vollen `make doc-check` — ein roter Doku-Stand ist uncommitbar, unabhängig davon, wie die Shell-Kette gebaut ist. Die Verkörperung beendet das Zählen, nicht die Klasse: Gates jenseits von `doc-check` bleiben Kür der Arbeitsregel (Gate-Läufe in eine Datei umleiten, den Exit **bindend** verwenden) |
| BEO-006 | **Ein pfad-selektiver Commit nimmt still mit, was früher gestagt wurde.** `git rm`/`git mv` stagen sofort; ein späterer Commit, der nur weitere Pfade addet, trägt den gesamten Index — die Botschaft beschreibt einen Teil des Diffs, der Rest reist undeklariert mit, und der Zwischenstand kann gate-rot sein. Kein Gate sieht das: `commits` prüft die Message-**Form** (Kennung), keines die Deckung Botschaft ↔ Diff | `*` | 2 | [slice-106](done/slice-106-baseline-v560-vendoring.md), [slice-108](done/slice-108-roadmap-offene-wellen.md) | offen, Zähler **2**. Gegenmittel: vor pfad-selektiven Commits `git status` auf schon Gestagtes prüfen; Moves und Löschungen erst unmittelbar vor ihrem Commit stagen. Die Regel allein trägt nicht — sie ist zweimal trotz frischer Fassung verfehlt worden. Mechanische Form bei 3×: ein Hook, der Botschaft-Pfad-Nennungen gegen den Staged-Diff hält; er wäre ein Heuristik-Wächter, darum bei 3× zu entscheiden |
| BEO-004 | **Eine Modul-Grenze wird nur auf der Quell-Achse gedacht.** Ein Modul prüft, was es scannt — liest aber Dateien, die es nie scannt: Zieldateien außerhalb der Scan-Wurzeln (`matrix` den Status, `anchors`/`codepaths` die Slugs, `diagrams`/`versions` ihre Quellen, `pins` die gehashte Heading-Section), Post-Pässe ueber selbst benannte Verzeichnisse und **git-Revisionen** (`vcs` rechnet auf Blobs). In diesen Eingaben gilt keine Zusage, die das scannende Modul gibt — und bei `pins` ist die Folge **still** | `*` | 3 | [slice-101](done/slice-101-fence-unbalanciert.md) | offen, **verkörpert in welle-73**, Zähler **3**. Die Regel steht seit dem Vollständigkeits-Zensus in [`AGENTS.md`](../../../AGENTS.md) §3.8; der Reviewer-Skill trägt dazu den MEDIUM-Anker mit der Prüffrage an den Diff: *welche Eingaben liest dieses Modul, die es nicht scannt — und gilt dort dieselbe Zusage?* Bis dahin war der Skill ihr einziger Ort und damit eine Waise. Eine Liste der Achsen trägt nicht; sie war dreimal unvollständig. Offen bleibt die Klasse, sobald ein Modul eine neue Ziel-Achse öffnet Die Beleg-Form verlangt Slice-Kennungen — darum **eine** Kennung bei Zähler 3; die Klasse ist dichter als der Zähler. |
| BEO-003 | **Eine geteilte Lexik driftet an den Rändern, weil jeder Konsument sie selbst vorbereitet.** Nicht die Lexik selbst war das Problem, sondern dass fünf Stellen dieselbe Zeile verschieden trimmten (`TrimSpace` unicode-weit gegen `TrimLeft` auf Space/Tab) und zwei Automaten dieselbe Frage verschieden beantworteten. Jede Stelle für sich vertretbar — zusammen ein stiller Grün-Pfad, den kein Gate sieht, weil jedes Modul für sich konsistent ist | `*` | 4 | [slice-101](done/slice-101-fence-unbalanciert.md), [slice-099](done/slice-099-structure-modul.md), [slice-103](done/slice-103-geteilte-lexik-raender.md), [slice-178](done/slice-178-offene-tasks-roh.md) | **verkörpert in welle-74**, Zähler **3**. Die Form ist ein **Kopplungs-Test**: dieselbe Eingabe durch **alle** Konsumenten derselben Frage, Fehlschlag bei abweichender Antwort — eine Liste kann eine Stelle vergessen, eine Kopplung nicht. Ein geteiltes Prädikat allein genügt nicht; es braucht je Konsument eine Assertion. Offen bleibt die Klasse, wo noch kein Kopplungs-Test existiert (Absatz- und Überschriften-Achse) und **konfigurationsseitig** — ein Muster in der Prüf-Config kann dieselbe Lexik anders sprechen als das Modul, das es benutzt |
| BEO-002 | **Eine Semantik-Änderung wird nur im Dokumentkörper nachgezogen, ihre Ränder bleiben stehen** — Historie-Zeilen, Index-Einträge, Konsequenz-Abschnitte, Entscheidungs-Protokolle. Sie sind Prosa und darum gate-unsichtbar: `links`/`anchors`/`ids` prüfen die Auflösung, nicht die Aussage. Der Rand referiert danach eine Fassung, die es nicht mehr gibt, und wirkt dabei so belastbar wie der Körper | `*` | 8 | [slice-093](done/slice-093-closure-note-gate.md), [slice-096](done/slice-096-structure-modul-analyse.md), [slice-104](done/slice-104-floskel-wortgrenze.md), [slice-153](done/slice-153-lastenheft-token-ziel-klasse.md), [slice-172](done/slice-172-closure-uebergang-waechtern.md), [slice-185](done/slice-185-print-mk-doc-usage.md), [slice-186](done/slice-186-vcs-rename-im-range.md), [slice-187](done/slice-187-handbuch-url-beide-formen.md) | **verkörpert als** [`MR-025`](../../../harness/conventions.md#mr-025), Zähler **4** — und **bleibt offen**: eine Regel für Menschen kann weiter verfehlt werden. Die vierte Instanz zeigt eine **neue Ausprägung**: nicht vergessen zu zählen, sondern das **Falsche** gezählt — `grep` nach dem eben geschriebenen **neuen** Wortlaut statt nach dem alten, und damit vier Spiegel statt vierzehn. Der Eintrag nennt den richtigen Ableiter seit jeher; er wurde gelesen und in der Gegenrichtung angewandt. Gegenmittel: vor dem Ändern einer Semantik ihre Spiegel auflisten; der Ableiter für eine Wortlaut-Änderung ist das `grep` nach dem **alten** Wortlaut, und wo eine Aufzählung eine Menge spiegelt, gehört sie an ihre Quelle **gebunden** statt in die Liste geschrieben **Sechste Instanz, [slice-185](done/slice-185-print-mk-doc-usage.md) — ein Beinahe-Treffer, und die Methode war schuld:** der Plan setzte die Spiegel-Liste pflichtgemäß vor das erste Edit, stellte sie aber **handverlesen** zusammen (vier benannte Dateien) statt sie zu greppen. Der fünfte Spiegel — `docs/user/operations.md`, seit Langem bei drei von zwölf Targets stehengeblieben — fiel durch einen Seitenblick auf, nicht durch die Liste. Er ist jetzt kein Spiegel mehr, sondern ein Zeiger auf das erzeugte Fragment. **Ableiter, geschärft:** die Spiegel-Liste entsteht aus einem `grep` über den Baum nach dem **alten** Wortlaut, nicht aus dem Gedächtnis über die Orte — eine Datei, an die man nicht denkt, steht in keiner handverlesenen Liste. **Siebte Instanz, [slice-186](done/slice-186-vcs-rename-im-range.md) — der geschärfte Ableiter wurde EINEN Slice später nicht befolgt.** Vier Kommentare behaupteten dieselbe Eigenschaft (*„ein Rename erscheint als Delete + Add"*), die der Range-Pfad nicht hielt; die Reparatur fand **zwei** davon, weil die Liste wieder handverlesen war statt gegrept — und die Commit-Botschaft sagte *„die zwei falschen Zusagen sind weg"*, also Vollständigkeit über eine unvollständige Menge. Gefunden vom Review, mit dem Satz, der hier seit slice-185 steht: ein `grep` hätte alle vier geliefert. **Neu und tragfähiger als ein weiterer Ableiter:** die Reparatur ersetzt die zwei abgeleiteten Behauptungen durch **Rang-Zeiger** auf die Stelle, die die Eigenschaft hält. Damit gibt es einen Ort, an dem die Zusage kippen kann, statt vier, die still mitkippen — die Spiegel-Menge wird kleiner, statt besser gepflegt zu werden. Das ist die Form, die dieser Eintrag von Anfang an fordert (*an ihre Quelle gebunden statt in die Liste geschrieben*), erstmals an Code angewandt statt an Prosa. **Achte Instanz, slice-187 — eine ZWEITE Zeichenkette entsteht, und die Prüfung, ob sie mitspiegelt, verhindert eine unnötige Verdopplung.** Die Handbuch-URL bekommt eine rohe Zweitform (`raw.githubusercontent.com`, für Code-Agenten). Die naheliegende Annahme war, `packaging/dockerhub/overview.md` müsse jetzt **zwei** Zeichenketten führen statt einer — die Prüfung ergab: nein. Die Docker-Hub-Seite ist ein handkuratierter Link für einen Menschen, keine Illustration der `--help`/`--print-mk`-Ausgabe, und braucht die maschinenfreundliche Form nicht. **Neu daran:** nicht jeder Spiegel-Kandidat ist ein Spiegel — die Prüfung, ob eine Stelle den Bau tatsächlich **reproduziert** (Illustration) oder nur **verweist** (Dokumentation), entscheidet, ob eine neue Zeichenkette dort mitgeht. Geprüft und tatsächlich um die zweite Form ergänzt: die Test-Literale und die `--print-mk`-Illustration im Handbuch — beide reproduzieren den Bau wörtlich. |

## Gestrichene Einträge

Regeln dieser Sektion: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer eine Zeile still löscht, macht sie ununterscheidbar von
einer, die es nie gab; darum hierher verschieben (mit Begründung), nicht löschen.

| Kennung | Beobachtung | Gestrichen am | Warum sie nicht mehr auftreten kann |
|---|---|---|---|
| BEO-001 | Ein Datei-Register und seine Autoritäts-Tabelle driften **unbemerkt** — kein Gate deckte die Richtung „Artefakt ⇒ registriert“; zweimal eingetreten ([slice-087](done/slice-087-spec-historie-referenzrichtung.md), [slice-099](done/slice-099-structure-modul.md): drei Wellen-Closures ohne Register-Zeile) | 2026-08-16 | **Für die eingetretene Instanz mechanisiert:** die dritte `planning`-Fähigkeit ([slice-102](done/slice-102-wellen-lifecycle-invariante.md), `wave-unregistered`) prüft die Richtung „Ergebnisnotiz ⇒ Register-Zeile“ und läuft im eigenen Baum scharf in `make gates` — genau die Tabelle, an der die Beobachtung zweimal eingetreten ist, kann nicht mehr still unvollständig sein. **Rest bleibt benannt, nicht gezählt:** ADR-Index und Konventionsspeicher-Index tragen dieselbe Bauform und sind weiter ungeprüft; tritt die Klasse dort ein, ist der `registry`-Modul-Vorschlag aus dem ursprünglichen Eintrag die vorgezeichnete Form (opt-in, ein Verzeichnis-Muster je Register plus Autoritäts-Datei, **eine** Richtung, ein Grund-Code) |
| BEO-005 | Eine chronologische Tabelle kippt still ihre Richtung — aus „unten anhängen“ wird „oben einfügen“, danach führt dieselbe Tabelle zwei gegenläufige Blöcke; kein Gate las Reihenfolge. Am selben Tag an **drei** Tabellen eingetreten ([slice-099](done/slice-099-structure-modul.md)); die mechanische Form stand im Eintrag vorgezeichnet (typisierter Monotonie-Vergleich der Schlüsselspalte, drei Entscheidungen, die keine Details sind) | 2026-08-21 | **Für die eingetretene Instanz mechanisiert:** die siebte `structure`-Bedingung ([slice-105](done/slice-105-tabellen-monotonie.md), `table-order`/`table-column`, v0.61.0) prüft die sechs eigenen chronologischen Bestandstabellen scharf in `make gates` — genau die Tabellen, an denen die Beobachtung eingetreten ist, können nicht mehr still kippen. Der Retro-Beleg lief mit dem **Produkt** (27 Befunde am Stand vor der welle-73-Heilung, exakt die 14 · 6 · 7 der Skript-Messung; heutiger Bestand null), und die Release-Prep der Streich-Welle war der erste gefangene Fall (Handbuch-§11-Zeile im ersten Anlauf falsch herum). **Rest bleibt benannt, nicht gezählt:** chronologische Tabellen außerhalb der sechs aktivierten Regeln, in **zwei** Tabellen gesplittete Ordnungen und fremde Repos ohne Aktivierung deckt die Mechanisierung nicht — tritt die Klasse dort ein, ist die vorgezeichnete Form eine weitere `structure`-Regel bzw. der Grenz-Trigger aus [ADR-0057](../adr/0057-structure-tabellen-monotonie.md) |
