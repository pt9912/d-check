# Beobachtungs-Register

Regeln dieses Registers: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer schreibt (Slice-Closure), wer liest (Welle-Closure =
Lese-Schritt bei 3×; Slice-Planung = Sichtungs-Schritt darunter), wann gestrichen wird,
welche Form ein Beleg hat (Slice-Kennung, so viele wie der Zähler, in `done/`), und dass
eine leere Tabelle `— keine —` trägt statt zu verschwinden.

| Kennung | Beobachtung | Sub-Area | Zähler | Belege | Stand |
|---|---|---|---|---|---|
| BEO-020 | **Gemessen wird die eigene Menge, ausgesagt wird über die fremde.** Die Frage war, ob ein gepinnter Fremd-Bestand sich bewegt. Gezählt wurde, wie viele verschiedene Werte der Pin **in unserer Historie** trug — also wie oft **wir** gehoben haben. Das Ergebnis („Action-Pins: null Bewegungen", „`distroless`: nie bewegt") las sich wie eine Aussage über upstream und trug eine Entscheidung: eine Achse wurde damit abgelehnt. Die **direkte** Messung — upstream fragen — ergab bei sechs von zwölf Pins Rückstand, darunter beide Action-Pins. **Die Klasse ist nicht „zu wenig gezählt"**: die Zählung war vollständig und richtig, nur über die falsche Grundgesamtheit. Ein Bestand, den nur **wir** ändern, kann über einen Bestand, den nur **andere** ändern, nichts sagen. **Test vor jeder Messung:** wer ändert die Menge, die ich zähle — und wer ändert die, über die ich rede? Sind das zwei verschiedene, ist die Messung kein Beleg. Kein Gate fängt das; die Zahl ist echt, nur ihr Gegenstand ist ein anderer als der behauptete. **Verwandt, aber nicht dasselbe:** `BEO-011` trifft eine Aussage, die aus **zu wenig** Bestand gezogen ist — hier ist genug gezählt, aber das Falsche | `*` | 1 | [slice-142](done/slice-142-freshness-weitere-achsen.md) | offen, Zähler **1**, unter der Schwelle — als **Sichtungs-Schritt** der Slice-Planung geführt. **Prozedur, sofort wirksam:** wo eine Entscheidung an „bewegt sich das" hängt, wird der fremde Bestand **gefragt**, nicht die eigene Historie gelesen; wo das nicht geht, trägt die Entscheidung die Grenze offen. Umgesetzt: die zwölf Achsen des Nachtlaufs fragen upstream direkt, und der gemessene Rückstand ist als [slice-161](open/slice-161-sechs-pins-heben.md) geschnitten |
| BEO-019 | **Eine Probe auf einen Wettlauf, die zweimal grün läuft, hat nichts widerlegt — und liest sich wie ein Beweis.** Beim Untersuchen eines zeitabhängigen Tests wurde die vermutete Bedingung **erzwungen** statt abgewartet (gleiche Dateilänge **und** per `Chtimes` dieselbe Änderungszeit auf die Nanosekunde). Zwei Läufe grün ⇒ Schluss: *„die Deutung ist widerlegt, die Ursache ist unbekannt"*. Der **dritte** Lauf desselben unveränderten Codes meldete `cannot create empty commit: clean working tree` — die Deutung war richtig, und die Reparatur wäre um ein Haar als „auf Verdacht" unterblieben. **Die Klasse ist nicht „zu wenig Wiederholungen"**, sondern: *bei einem nichtdeterministischen Prüfling ist Grün kein Ergebnis*. Nur Rot trägt eine Aussage; Grün trägt sie erst, wenn der Prüfling deterministisch gemacht wurde | `*` | 1 | [slice-141](done/slice-141-fixture-racily-clean.md) | offen, Zähler **1**, unter der Schwelle — als **Sichtungs-Schritt** der Slice-Planung geführt. **Prozedur, sofort wirksam:** wer eine Rennbedingung untersucht, formuliert die Probe so, dass sie **deterministisch rot** wird, wenn die Bedingung besteht — oder verlagert die Prüfung dorthin, wo sie nicht mehr auf Zeit angewiesen ist. Umgesetzt: statt eines Tests, der auf die Kollision wartet, lehnt [`coretest.GitFixtureRewriteHazard`](../../../internal/hexagon/core/coretest/gitfixture.go) eine gleich lange Neuschreibung **beim Schreiben** ab, mit eigenem Negativ-Selbsttest. **Verwandt, aber nicht dasselbe:** `BEO-017` trifft ein Rot aus dem falschen Grund — hier trägt ein **Grün** gar keinen Grund |
| BEO-018 | **Eine Härtung am Rand eines Wächters kippt seine Fehlerpolitik, und die Proben sehen es nicht, weil sie einen anderen Kanal bespielen als der Ernstfall.** Die Härtung galt der Zusage *fail-closed*: eine zweite Host-Abhängigkeit fiel weg, damit ein fehlendes Werkzeug den Wächter nicht mehr ohne Urteil enden lässt. Der neue Leseweg funktionierte über eine Pipe — und nur dort; unter `set -e` beendete er den Wächter still, sobald die Eingabe anders ankam. Ergebnis: **jeder** Befehl lief durch, einen Arbeitstag lang, während an derselben Datei die Grenzen geschärft wurden. Kein Sensor sagte etwas, denn die Proben fuhren genau die eine Form, die noch ging; entdeckt wurde es per Zufall, an einem Befehl, der versehentlich einen blockierten Interpreter enthielt. **Die Klasse ist nicht „Regression"**, sondern: *ein Prüfling, den man am Rand anfasst, wird an einer Stelle geprüft, die der Rand nicht berührt* | `*` | 1 | [slice-156](done/slice-156-hook-antwortform.md) | **Zähler 1**, unter der Schwelle — als Lese-Schritt geführt, nicht als Regel. **Prozedur, sofort wirksam:** wer den Rand eines Wächters ändert (Eingabe, Ausgabe, Fehlerpolitik, Aufruf-Umgebung), fährt eine Probe **je Form**, die dieser Rand annehmen kann, und stellt die Regression in einer Kopie **wieder her**, um zu sehen, ob die Probe sie fängt. Umgesetzt: die Leseweg-Proben in [`guard-probe.sh`](../../../tools/harness/guard-probe.sh) fahren fünf stdin-Formen, und die Gegenprobe mit wiederhergestellter Regression meldet `crash`. **Verwandt, aber nicht dasselbe:** `BEO-007` trifft den verworfenen Exit *im Sensor*, `BEO-017` das Rot aus dem falschen Grund — hier schweigt der Sensor aus dem falschen Grund |
| BEO-017 | **Ein Gate wird rot, und der Grund ist ein anderer als der geprüfte.** Der Nachweis *„die Regel greift"* stützt sich auf den Exit, nicht auf die gelesene Ursache — und ein roter Exit ist erstaunlich leicht aus dem falschen Grund zu bekommen. Drei Gestalten, alle belegt: eine Probe nennt den **falschen Linter** und wird vom Nachbarn rot; eine Direktiv-Form gilt im Text als *wohlgeformt* und war **nie gefahren**; eine Probe wird ans Dateiende **angehängt** und landet im einzigen Abschnitt, den die Prüfung ausnimmt — der Wächter schweigt zu Recht und wird beinahe als „greift nicht" verbucht. Die Gegenrichtung gehört dazu: der **unveränderte Bestand**, auf dem der Sensor schweigen muss | `*` | 3 | [slice-132](done/slice-132-hard-rule-zensus.md), [slice-134](done/slice-134-nolintlint.md), [slice-138](done/slice-138-matrix-wellen-klasse.md) | **Schwelle erreicht**, Zähler **3** — verteilt auf **zwei** aufeinanderfolgende Arbeitstage (zweimal 2026-08-23, einmal 2026-08-25), in einem Repo, das diese Disziplin ernst nimmt. **Der erste Anlauf dieser Zeile schrieb „ein einziger Arbeitstag"** — dieselbe Dichte-Behauptung, die ein Review zwei Stunden zuvor an derselben Instanz-Menge widerlegt hatte; sie stammt aus demselben Quelltext und wurde beim Abschreiben nicht mitkorrigiert. **Der Kanon trägt die Regel seit `v5.12.0`** (`modul-13` §Fitness Function aus einem ADR-Satz, Schritt 6: *„das Rot muss von dieser Regel kommen … die Meldung nennt die gebrochene Regel und die Fundstelle, und beides gehört angesehen"*) — sie kam auf den Konsumenten-CR dieses Repos zustande. **Darum hier keine eigene Regelzeile:** sie zu duplizieren verstieße gegen [`AGENTS.md`](../../../AGENTS.md) §1. **Kein Gate fängt das und keines kann es** — ob ein roter Exit vom geprüften Grund kommt, ist ein Urteil über eine Meldung, kein `grep`. **Prozedur:** wer einen Wächter probt, liest die Meldung, nicht den Exit; wer eine Probe anhängt, prüft vorher, in welchem Abschnitt sie landet; und jede Probe hat eine Gegenprobe auf dem unveränderten Bestand |
| BEO-016 | **Prosa verschwindet in einer absatzweiten Inline-Code-Spanne — und kein Gate sieht es.** Die geteilte Lexik paart Backticks **je Absatz**, nicht je Zeile (`stripInlineCodeByLine`); zwei Backticks in verschiedenen Zeilen desselben Absatzes ziehen damit eine Spanne über alles dazwischen auf und leeren es positionserhaltend. Für **jedes** prosa-lesende Modul ist dieser Text danach nicht vorhanden. Das ist als Markdown-Lesart **korrekt** — der Schaden entsteht, wenn die Spanne unbeabsichtigt ist: das Dokument rendert falsch, und die Prüfung, die dort etwas finden sollte, schweigt. `spans` deckt es **nicht**: sein `span-unclosed` meldet **ungerade** Parität, und dieser Fall hat gerade | `*` | 1 | [slice-143](done/slice-143-structure-abschnitts-skopus.md) | offen, Zähler **1**. **Gemessen an der Ablösung des Closure-Wächters:** dieselbe eingefügte Risikozeile ergibt beim abgelösten Skript (Backtick-Paarung je Zeile) Exit 1 und bei der Regel auf dem bereinigten Text Exit 0 — die Differenz ist in [ADR-0059](../adr/0059-closure-waechter-weicht-structure-regel.md) ausgewiesen. **Verwandt, aber nicht dieselbe Richtung:** in einem Schwester-Repo erzeugt **ungerade** Backtick-Parität im Absatz einen Fehl-Befund des Moduls `ids`; dort wird zu viel gesehen, hier zu wenig. **Mechanische Form, offen:** ein Sensor auf **mehrzeilige** Inline-Code-Spannen wäre baubar, hätte aber in einem Repo mit langen Prosa-Absätzen eine zu messende Falsch-Positiv-Last — die Entscheidung gehört gemessen, nicht aus dem Zähler abgeleitet. **Prozedur bis dahin:** wer einen Wächter auf bereinigten Text stellt, prüft eine Probe **innerhalb** einer mehrzeiligen Spanne mit |
| BEO-015 | **Ein offener Punkt bekommt bei der Closure einen vierten Ausgang, den es nicht gibt.** Der Kanon nennt **drei** — *eingetreten* → Carveout oder **Folge-Slice mit ID** · *entfallen* → gestrichen mit Begründung · *weiter offen* → **ins Beobachtungs-Register** — und schließt mit dem Satz *„Ein Slice geht nicht nach `done/`, während ein Risiko ohne Ausgang dasteht"*. Der vierte, erfundene Ausgang ist ein Absatz in der Closure-Notiz („offen und benannt: X"). Er sieht aus wie Buchführung, ist aber Ablage: `done/` wird nach `modul-06` dem Implementer-Agenten **nicht geladen**, und `ls open/` meldet „nichts offen", während die Archiv-Dokumente etwas anderes sagen | `docs/plan/planning/` | 3 | [slice-136](done/slice-136-agents-34-klaerung.md), [slice-137](done/slice-137-toolchain-freshness.md), [slice-138](done/slice-138-matrix-wellen-klasse.md) | **Schwelle erreicht**, Zähler **3** — drei Closures in Folge. **Gemessen:** über alle 18 Ergebnisnotizen fehlt **keine** genannte Slice-Datei, aber **13** Punkte benennen Arbeit ohne Kennung; Häufung in welle-82/83/84. **Die mechanische Form muss nicht erfunden werden — der Kanon spezifiziert sie**: die maschinelle Hälfte der Register-Paarung, *„ob eine in `done/` zitierte `BEO-<NNN>` eine Registerzeile hat und ob jede Registerzeile mindestens einen Beleg trägt"*, dazu drei **urteilsfreie** Beleg-Prüfungen — **Form** (`slice-<NNN>`, kein Freitext) · **Anzahl** (so viele wie der Zähler) · **Lage** (in `done/`, geprüft **nach** dem `git mv`). Der Kanon überlässt ausdrücklich nur das Werkzeug dem Repo. **Wir haben davon nichts:** `planning` prüft Lifecycle-Invarianten, `ids` erzwingt Linkpflicht nur für `ADR-`/`MR-`/`DC-` (Nachrüsten von `slice-`/`BEO-` erzeugte **962** bzw. **295** Befunde), `structure` sieht nur Überschriften. **Benannte Grenze des Kanons:** die *Existenz* der Slice-Datei wird nicht verlangt — ein erfundenes `slice-999` bleibt unentdeckt. **Prozedur ab sofort:** jeder offene Punkt bekommt bei der Closure einen der **drei** Ausgänge; „in die Notiz schreiben" ist keiner |
| BEO-014 | **Ein Test-Fixture ändert Inhalt, ohne die Größe zu ändern — und faellt darum sporadisch aus.** Beobachtet an `TestRangeAndFileAt` (`internal/adapter/driven/git`): das Fixture schreibt dieselbe Datei von `"v1\n"` auf `"v2\n"` und committet sofort. Beide Fassungen sind **drei Byte** lang und entstehen in **derselben Sekunde**; eine stat-basierte Änderungserkennung darf die Datei dann als unverändert führen (*racily clean*). Der Lauf meldete `keep.md: M erwartet, got ok=false` — der unmittelbar folgende `make fullbuild` auf **demselben Arbeitsbaum** war grün. Die Klasse ist breiter als der eine Test: jedes git-Fixture, das Änderung über gleich lange Inhalte in einem Zug herstellt, traegt sie | `internal/adapter/driven/git` | 1 | [slice-138](done/slice-138-matrix-wellen-klasse.md) | **erledigt** durch [slice-141](done/slice-141-fixture-racily-clean.md), Zähler **1**. **Gemessener Bestand: zehn** Fundstellen in acht Testfunktionen und zwei Paketen — nicht die eine, mit der die Klasse auffiel; drei aufeinanderfolgende Zählungen waren zu kurz. Alle zehn tragen unterschiedlich lange Inhalte, und **beide** Fixture-Helfer rufen denselben Wächter (`coretest.GitFixtureRewriteHazard`), der eine gleich lange Neuschreibung **beim Schreiben** ablehnt. Ursache belegt über die gelesene Meldung `cannot create empty commit: clean working tree`, nicht über einen grünen Lauf. **Benannte Grenze:** gleiche Länge ist notwendig, nicht hinreichend — der Wächter ist konservativ; und er misst den Inhalt auf der Platte, während die Gefahr am Index-Eintrag hängt. **Warum es zaehlt:** der Test haengt in `make gates`. Ein Gate, das gelegentlich ohne Grund rot wird, erodiert die Zusage „grün heisst geprüft" schneller als eines, das gar nicht existiert |
| BEO-013 | **Ein Unterdrückungs-Marker, der nichts mehr unterdrückt, bleibt stehen — und deckt lautlos den nächsten Befund.** Ein Marker wird gegen **einen** konkreten Befund gesetzt. Verschwindet dieser (Text geändert, Regel geschärft, Ziel repariert), bleibt der Marker; von da an schweigt die Zeile über etwas, das niemand stummschalten wollte. Für Go ist die Frage seit `nolintlint` mit `allow-unused: false` **gemessen**; für den eigenen Marker gibt es kein Gegenstück. **Der Bestand ist inzwischen gemessen, und er ist ein anderer als beim Anlegen dieser Zeile geschätzt:** nicht zwölf Marker, sondern **233** Zeilen außerhalb von Fences, die die Zeichenkette tragen und damit unterdrücken — die Erkennung ist eine blanke Teilketten-Suche, also wirkt **jede Erwähnung** wie ein gesetzter Marker. Nur **87** stehen in der Kommentar-Form, **146** sind Prosa. Lenkt man die Konstante ins Leere, treten **58** Befunde auf **48** Zeilen hervor; die übrigen **185** Marker-Zeilen unterdrücken nichts | `*` | 1 | [slice-134](done/slice-134-nolintlint.md) | offen, Zähler **1**. **Die Vorfrage ist beantwortet** ([slice-146](done/slice-146-ignore-marker-wirkung.md)): das Produkt hält die Information **nicht** — in allen vier Konsumenten ist der Marker ein frühes `continue` **vor** der Prüfung. Erreichbar wäre sie durch Umstellung auf *prüfen, dann unterdrücken*. **Gebaut wird sie trotzdem nicht, und der Grund ist ein tieferliegender Defekt:** solange die Erkennung eine Teilketten-Suche ist, meldete die Regel 185 Zeilen, davon die meisten Doku-Prosa — richtig und nutzlos. Erst muss der Marker eine **Form** haben ([slice-159](open/slice-159-ignore-marker-syntax.md)); danach ist diese Zeile wieder aufzunehmen. **Mechanische Form** (unverändert gültig): ein Gegenstück zu `allow-unused: false` — melden, wenn an einer markierten Zeile **ohne** den Marker gar kein Befund entstünde. Die Klasse ist trotzdem die gleiche, die für Go gerade geschlossen wurde. **Prozedur bis dahin:** wer eine markierte Zeile anfasst, entfernt den Marker probeweise und misst, ob überhaupt noch etwas feuert |
| BEO-012 | **Eine Quelle wird über ihren Geltungsbereich hinaus zitiert.** Der Text der Quelle stimmt, die in Anspruch genommene **Reichweite** nicht: ein Satz, der im Kanon nur für einen benannten Fall gilt, wird als universale Regel geführt; ein `MR-`Eintrag wird nach seinem **Titel** zitiert statt nach seinem Feld `Geltungsbereich`; eine ADR-Entscheidung wird als stehendes Verbot gelesen, obwohl sie einen einmaligen Akt beschreibt. Der Unterschied zu `BEO-011` ist die Richtung: dort wird eine Regel aus dem Anlass **gebildet**, hier wird eine bestehende **gedehnt** — und weil ein Zitat wie ein Beleg aussieht, ist es schwerer zu bemerken als eine unbelegte Behauptung | `*`  | 5 | [slice-127](done/slice-127-claude-md-pointer.md), [slice-130](done/slice-130-lastenheft-historie-form.md), [slice-131](done/slice-131-reviewer-skill-waisen.md), [slice-140](done/slice-140-konsumenten-cr.md), [slice-147](done/slice-147-reviewer-anker-reichweite.md) | **Schwelle überschritten**, Zähler **4**. Die vierte Instanz ist die schärfste: [slice-140](done/slice-140-konsumenten-cr.md) schrieb einen CR **über diese Klasse** und beging sie in dessen Punkt 4 — dazu zweimal im selben Dokument (ein Zitat, das zwei Sätze aus zwei Aufzählungspunkten in umgekehrter Folge zusammenstellte; eine Kanon-Zuschreibung in einem `MR-`Eintrag für einen Vorgang, den der vendorte Baum nicht kennt). **Alle vier Male fand es der zweite Leser, nie der Schreibende** — das ist die Feedforward-Lücke, nicht die Feedback-Lücke, und beide Hälften tragen seit [slice-147](done/slice-147-reviewer-anker-reichweite.md) einen **Ort in der verkörperten Form**: die **Feedforward**-Hälfte als Hard Rule in [`AGENTS.md`](../../../AGENTS.md) §5, die **Feedback**-Hälfte als MEDIUM-Anker im Reviewer-Skill, als Prüffrage an den Diff statt als Fallliste. **Der Kanon trägt die Regel längst** — `grundlagen-source-precedence.md` stellt die Reichweitenfrage an **jede** zitierte Aussage; die zwei Orte sind ihre Verkörperung, nicht ihre Erfindung. Erstmals gemessen: eine maschinelle Wortlaut-Probe **aller** Blockzitate eines Dokuments gegen die zitierte Quelle findet die Tilgung ohne Auslassungszeichen — die Reichweite selbst findet sie nicht. Ursprünglicher Stand: alle drei ersten Instanzen in welle-83. **Kein Gate fängt das**, und die Wortlaut-Probe des Moduls `citations` deckt es nicht: sie vergleicht Zeichen, nicht Anspruch. **Prozedur:** vor jedem Zitat einer Regel deren Geltungs-Feld lesen — bei einer `MR-<NNN>` das Feld `Geltungsbereich` **und** `Ersetzt-Baseline-Regel`, bei einer Kanon-Stelle den Absatz, in dem sie steht, bei einer ADR die Unterscheidung Akt/Verbot. Wer die Reichweite nicht nachgelesen hat, zitiert nicht, sondern erinnert sich |
| BEO-011 | **Eine Aussage oder Regel wird aus dem ANLASS gezogen statt aus dem BESTAND.** Wer eine Eigenschaft am gerade bearbeiteten Fall beobachtet, erklärt sie für exklusiv, allgemein oder abschließend — ohne die Nachbarn gezählt zu haben. Drei Ausprägungen: (a) eine **Exklusivitäts-Aussage** („das einzige Modul, das …", „als einzige Bedingung", „trägt gar kein Ventil"), (b) ein **Kriterium**, das eine bereits korrigierte Menge nachträglich begründet — die Aufzählung stimmt, die Herleitung trifft auch Nachbarn, die nicht dazugehören —, und (c) eine **Regel, die nur für den Ort geschrieben wird, an dem sie wehtat** statt für ihre Klasse. Kein Gate fängt eines davon: alle drei sind Prosa, und alle drei lesen sich belastbarer als die Messung, auf der sie stehen | `*` | 4 | [slice-123](done/slice-123-structure-heading-muster.md), [slice-124](done/slice-124-diagrams-ventile.md), [slice-125](done/slice-125-release-v0630.md), [slice-140](done/slice-140-konsumenten-cr.md) | **Schwelle überschritten**, Zähler **4**. Die vierte Instanz bringt Ausprägung (c) erstmals in die Belege — und aus einer neuen Ecke: [slice-140](done/slice-140-konsumenten-cr.md) schrieb sein §5-Risiko *„könnte sich als Lesefehler erweisen"* für **den einen Punkt**, an dem der Zweifel entstand, statt für seine Klasse (jeder Punkt, der eine Kanon-Lücke behauptet). Für den benannten Punkt lief die Baum-Suche und entkräftete das Risiko; für den ungenannten lief sie nie, und dort saß der Befund. **Ein Risiko-Abschnitt ist selbst eine Regel** — wer ihn nur für den Anlass schreibt, deckt genau die Nachbarn nicht ab, die niemand angesehen hat. Ursprünglicher Stand, Zähler **3** in einer einzigen Welle: zweimal Ausprägung (a) ([slice-123](done/slice-123-structure-heading-muster.md), [slice-124](done/slice-124-diagrams-ventile.md)), einmal (b) ([slice-125](done/slice-125-release-v0630.md)). **Der erste Zählstand dieses Eintrags war 5 und selbst aus dem Anlass gebildet** — ein unabhängiger Review hat ihn widerlegt: [slice-122](done/slice-122-versions-musterliste.md)s „zwei Paare, zwei Befunde" war eine von der geteilten Nachrunde widerlegte Zusage (BEO-004-nah, nicht diese Klasse), und die nach Dokument-Rolle statt nach Aussage gebildete Spiegel-Liste in [slice-125](done/slice-125-release-v0630.md) ist BEO-002. Beide sind herausgenommen; Ausprägung (c) war unter den fünf gar nicht vertreten. **Drei weitere Instanzen liegen in Arbeit** — je eine von (a), (b) und (c), alle im selben Commit entstanden, der diesen Eintrag anlegte, und alle vom Review gefunden; sie werden beim Closure ihres Slice gezählt und bringen (c) erstmals in die Belege. **Ableiter, drei Sätze, alle handlungsförmig:** wer „einzig/nur/alle/gar kein" schreibt, zählt vorher die Nachbarn (`grep` über die Geschwister-Module bzw. -Abschnitte, und beim eigenen Modul auch die **geerbte** Config-Maschinerie — `citations` hat sehr wohl einen `scope`). Wer eine falsche Exklusivität **repariert**, begründet die reparierte Menge **nicht**; sie ist eine *benannte Liste*, kein ableitbares Kriterium, und eine Begründung ist eine neue, ungezählte Behauptung. Wer eine **Regel** aus einem Vorfall schreibt, benennt zuerst die Klasse und erst dann den Ort. **Mechanische Form:** bewusst offen. Für (a) wäre ein Lint auf Exklusivitäts-Wörter denkbar, aber ein Heuristik-Wächter mit hoher Falsch-Positiv-Last in einem Repo, dessen Doku bewusst Grenzen benennt; (b) und (c) sind Urteile, kein `grep`. Verkörpert ist die Klasse zunächst im Reviewer-Skill und in dieser Zeile — die Entscheidung über einen Wortlisten-Lint ist eigens zu treffen, **nicht** aus dem Zähler abzuleiten (was genau der Fehler dieser Beobachtung wäre) |
| BEO-010 | **Die Modulliste des eigenen Prüf-Profils hat Spiegel außerhalb der Config — und keiner davon ist gate-gedeckt.** Wer ein Modul in `.d-check.yml` aufnimmt, ändert damit still drei weitere Stellen mit: `FOCUS_DISABLE` im `Makefile` (wählt für die fokussierten Gates alle Datei-Module ab; wächst es nicht mit, **über-feuert** das neue Modul im `pre-commit`-Hook auf ungestagtes WIP), die Netzlos-Modulliste im [`DC-QA-03`](../../../spec/lastenheft.md#dc-qa-03--seiteneffektfreiheit-und-netzwerk-sparsamkeit)-Test und die Prosa-Beschreibungen des Gates in drei Dateien. `gate-consistency` prüft Target-**Namen**, nicht Modul-**Mengen** | `.d-check.yml`, `Makefile`, Gate-Doku | 2 | [slice-115](done/slice-115-arc-vergabe-architektur.md), [slice-152](done/slice-152-citations-scharfschalten.md) | **Schwelle nicht erreicht**, Zähler **2**. Gate-blind, und die zweite Instanz zeigt die Bindung schwächer als notiert: der Netzlos-Guard im Go-Test prüft eine **Teilmenge** (`netlessDocModules() ⊆ modules`) — er fängt ein **entferntes** Modul, aber **kein hinzugefügtes ohne Spiegel**, und blieb deshalb grün, während `FOCUS_DISABLE` vier fokussierte Gates über-feuern ließ (fail-closed, über den ganzen Baum, `adr-check` im `pre-commit`-Hook). Gebunden ist also nur eine **Richtung** einer Hälfte. Mechanische Form bei 3×: Mengen-Abgleich `modules` gegen die `--disable`-Kette in `FOCUS_DISABLE`, **beide** Richtungen, ein Grund-Code — und dieselbe Beidseitigkeit für den Go-Guard|
| BEO-009 | **Eine Commit-Botschaft behauptet mehr, als die Arbeit trägt.** Zwei Richtungen: (a) sie behauptet eine Probe oder Änderung, die **nicht stattfand** — die Botschaft wird vor oder während der Arbeit formuliert und beschreibt den beabsichtigten Zustand; schlägt eine Probe fehl oder greift ein Edit still nicht, reist die Behauptung trotzdem mit. (b) Sie **verallgemeinert über die Messung hinaus** — die genannten Proben liefen und stimmen, aber der Schluss daraus gilt weiter als sie reichen | `*` | 5 | [slice-111](done/slice-111-wave-drift-zwei-haelften.md), [slice-123](done/slice-123-structure-heading-muster.md), [slice-149](done/slice-149-baseline-v5120-delta-audit.md), [slice-150](done/slice-150-pin-gebundene-zitate.md), [slice-144](done/slice-144-commit-hash-muster.md) | **Zähler 6**, und beide neuen Instanzen sind Richtung **(a)** — die behauptete Prüfung, die nicht stattfand: [slice-149](done/slice-149-baseline-v5120-delta-audit.md) hakte einen unabhängigen Review ab, der nicht gelaufen war; [slice-150](done/slice-150-pin-gebundene-zitate.md) behauptete *„Nichts entfernt, nichts umgeschrieben"* bei einem Diff mit vier Löschungen. Die sechste ist Richtung **(b)**: ein Konfigurations-Kommentar setzte zwei Rohtext-Zahlen unter die Überschrift *„Gemessen mit dem Produkt, nicht per grep"* — richtig gemessen, falsch gerahmt. **Alle drei fanden sich erst beim Nachlesen bzw. im Review.** *Benannte Form-Lücke dieses Eintrags:* er trug schon bei Zähler 3 nur **zwei** Belege — die Beleg-Form (so viele Kennungen wie der Zähler) ist hier nie erfüllt gewesen; die dritte Instanz ist nicht rekonstruiert, und das steht hier, statt die Zahl stillschweigend passend zu machen. Ursprünglicher Stand, Zähler **3**. Die benannte mechanische Form deckt nur Richtung (a): ein Hook, der die in der Botschaft genannten Datei-Pfade gegen den Staged-Diff hält. Richtung (b) ist **nicht mechanisierbar** — ob ein Schluss über die gemessenen Fälle hinausreicht, ist ein Urteil, kein `grep`; sie steht seit dem Vollständigkeits-Zensus als Regel in [`AGENTS.md`](../../../AGENTS.md) §5, und der Reviewer-Skill trägt dazu den MEDIUM-Anker (*Botschaft verallgemeinert über die Messung hinaus*, Arbeitsanweisung: suche die N+1-te Form) — bis dahin war der Skill ihr einziger Ort und damit eine Waise. Prozedur für beide: Botschaften über Proben erst **nach** deren Exit formulieren, `git diff --stat` gegen jede Botschafts-Zeile halten, und jeden verallgemeinernden Satz auf die gemessene Menge zurückschneiden |
| BEO-008 | **Die Spiegel einer Pin-Hebung sind drei Klassen, gehoben wird nur die grep-bare.** `baseline/<tag>`-Pfad-Verweise sind gate-gedeckt und werden beim Bump vollständig retargetet; Release-/Tree-**URLs** mit dem Tag (`releases/tag/`, `releases/download/`, `tree/`) und **Prosa-/Ellipsen-Pins** (`…/vX.Y.Z/…`, „Stand"-Angaben in MR-Körpern) deckt kein Gate und kein Pfad-grep — sie bleiben stehen, teils in sich widersprüchlich in derselben Zeile (Link-Ziel neu, Quell-URL alt) | `*` | 4 | [slice-106](done/slice-106-baseline-v560-vendoring.md), [slice-110](done/slice-110-baseline-v570-bump.md), [slice-117](done/slice-117-baseline-v590-bump.md), [slice-148](done/slice-148-baseline-v5120-vendoring.md) | **Schwelle überschritten**, Zähler **4** — und die vierte Instanz zeigt, dass die Zahl im Titel dieses Eintrags zu klein war. **Es gibt eine VIERTE Klasse: der zitierende Verweis.** Ein Verweis zeigt nicht nur auf eine Datei, sondern zitiert ihren **Wortlaut**; beim Bump wird der Pfad mitgehoben, löst sauber auf, und das Zitat daneben existiert am neuen Ziel nicht mehr. Kein Gate sieht es, weil beide Hälften für sich stimmen. In [slice-148](done/slice-148-baseline-v5120-vendoring.md) gemessen: drei Dokumente, sechs Paare, davon **eines lebend** ([`MR-033`](../../../harness/conventions.md#mr-033), zwei Zitate). **Die tragfähige Messform ist eng und benannt:** Zitat gegen die Zeilen, die zwischen den **zwei gepinnten Bäumen** verschwunden sind — ein Korpus-Test („steht das Zitat irgendwo im eigenen Baum?") isoliert die Klasse NICHT, weil die eingefrorenen Dokumente den alten Wortlaut wörtlich führen und echte Brüche damit als Selbstzitat durchgehen. Ursprünglicher Stand, Zähler **3**. Gate-blind in **beide** Richtungen: die vergessene Hebung ebenso wie die Über-Hebung — ein Pfad-`grep` kennt keine Zeitform und hebt Vergangenheits-Aussagen mit. Gegenmittel: der Drei-Klassen-Zensus der Bump-Prozedur, ergänzt um die Gegenprobe, ob eine gehobene Stelle über die Gegenwart oder über die Vergangenheit spricht. **Die benannte mechanische Form ist seit [slice-122](done/slice-122-versions-musterliste.md) baubar:** `versions.patterns` trägt mehrere Muster-Quellen-Paare, ein zweiter Abgleich (Baseline-Tag in URLs und Prosa gegen den §Baseline-Pin) ist damit konfigurierbar. Das eigene Profil fährt ihn **nicht** — das ist ein eigener Entscheid mit eigener Messung, kein Rest dieses Slice |
| BEO-007 | **Ein Gate-Aufruf hinter einer Pipe meldet den Exit des letzten Pipe-Glieds.** `make gates 2>&1 \| tail` liefert den Exit von `tail` — die `&&`-Kette läuft weiter, ein roter Lauf wird als grün behandelt, und die Botschaft behauptet Gates, die nie bestanden wurden. Der Stop-Hook fängt nur das Sitzungs-Ende, nicht den Commit dazwischen | `*` | 3 | [slice-107](done/slice-107-baseline-v560-delta-audit.md), [slice-108](done/slice-108-roadmap-offene-wellen.md), [slice-111](done/slice-111-wave-drift-zwei-haelften.md) | **verkörpert in welle-79**, Zähler **3**. Die Form steht an der Stelle mit Biss: der `pre-commit`-Hook läuft zusätzlich den vollen `make doc-check` — ein roter Doku-Stand ist uncommitbar, unabhängig davon, wie die Shell-Kette gebaut ist. Die Verkörperung beendet das Zählen, nicht die Klasse: Gates jenseits von `doc-check` bleiben Kür der Arbeitsregel (Gate-Läufe in eine Datei umleiten, den Exit **bindend** verwenden) |
| BEO-006 | **Ein pfad-selektiver Commit nimmt still mit, was früher gestagt wurde.** `git rm`/`git mv` stagen sofort; ein späterer Commit, der nur weitere Pfade addet, trägt den gesamten Index — die Botschaft beschreibt einen Teil des Diffs, der Rest reist undeklariert mit, und der Zwischenstand kann gate-rot sein. Kein Gate sieht das: `commits` prüft die Message-**Form** (Kennung), keines die Deckung Botschaft ↔ Diff | `*` | 2 | [slice-106](done/slice-106-baseline-v560-vendoring.md), [slice-108](done/slice-108-roadmap-offene-wellen.md) | offen, Zähler **2**. Gegenmittel: vor pfad-selektiven Commits `git status` auf schon Gestagtes prüfen; Moves und Löschungen erst unmittelbar vor ihrem Commit stagen. Die Regel allein trägt nicht — sie ist zweimal trotz frischer Fassung verfehlt worden. Mechanische Form bei 3×: ein Hook, der Botschaft-Pfad-Nennungen gegen den Staged-Diff hält; er wäre ein Heuristik-Wächter, darum bei 3× zu entscheiden |
| BEO-004 | **Eine Modul-Grenze wird nur auf der Quell-Achse gedacht.** Ein Modul prüft, was es scannt — liest aber Dateien, die es nie scannt: Zieldateien außerhalb der Scan-Wurzeln (`matrix` den Status, `anchors`/`codepaths` die Slugs, `diagrams`/`versions` ihre Quellen, `pins` die gehashte Heading-Section), Post-Pässe ueber selbst benannte Verzeichnisse und **git-Revisionen** (`vcs` rechnet auf Blobs). In diesen Eingaben gilt keine Zusage, die das scannende Modul gibt — und bei `pins` ist die Folge **still** | `*` | 3 | [slice-101](done/slice-101-fence-unbalanciert.md) | offen, **verkörpert in welle-73**, Zähler **3**. Die Regel steht seit dem Vollständigkeits-Zensus in [`AGENTS.md`](../../../AGENTS.md) §3.8; der Reviewer-Skill trägt dazu den MEDIUM-Anker mit der Prüffrage an den Diff: *welche Eingaben liest dieses Modul, die es nicht scannt — und gilt dort dieselbe Zusage?* Bis dahin war der Skill ihr einziger Ort und damit eine Waise. Eine Liste der Achsen trägt nicht; sie war dreimal unvollständig. Offen bleibt die Klasse, sobald ein Modul eine neue Ziel-Achse öffnet Die Beleg-Form verlangt Slice-Kennungen — darum **eine** Kennung bei Zähler 3; die Klasse ist dichter als der Zähler. |
| BEO-003 | **Eine geteilte Lexik driftet an den Rändern, weil jeder Konsument sie selbst vorbereitet.** Nicht die Lexik selbst war das Problem, sondern dass fünf Stellen dieselbe Zeile verschieden trimmten (`TrimSpace` unicode-weit gegen `TrimLeft` auf Space/Tab) und zwei Automaten dieselbe Frage verschieden beantworteten. Jede Stelle für sich vertretbar — zusammen ein stiller Grün-Pfad, den kein Gate sieht, weil jedes Modul für sich konsistent ist | `*` | 3 | [slice-101](done/slice-101-fence-unbalanciert.md), [slice-099](done/slice-099-structure-modul.md), [slice-103](done/slice-103-geteilte-lexik-raender.md) | **verkörpert in welle-74**, Zähler **3**. Die Form ist ein **Kopplungs-Test**: dieselbe Eingabe durch **alle** Konsumenten derselben Frage, Fehlschlag bei abweichender Antwort — eine Liste kann eine Stelle vergessen, eine Kopplung nicht. Ein geteiltes Prädikat allein genügt nicht; es braucht je Konsument eine Assertion. Offen bleibt die Klasse, wo noch kein Kopplungs-Test existiert (Absatz- und Überschriften-Achse) und **konfigurationsseitig** — ein Muster in der Prüf-Config kann dieselbe Lexik anders sprechen als das Modul, das es benutzt |
| BEO-002 | **Eine Semantik-Änderung wird nur im Dokumentkörper nachgezogen, ihre Ränder bleiben stehen** — Historie-Zeilen, Index-Einträge, Konsequenz-Abschnitte, Entscheidungs-Protokolle. Sie sind Prosa und darum gate-unsichtbar: `links`/`anchors`/`ids` prüfen die Auflösung, nicht die Aussage. Der Rand referiert danach eine Fassung, die es nicht mehr gibt, und wirkt dabei so belastbar wie der Körper | `*` | 4 | [slice-093](done/slice-093-closure-note-gate.md), [slice-096](done/slice-096-structure-modul-analyse.md), [slice-104](done/slice-104-floskel-wortgrenze.md), [slice-153](done/slice-153-lastenheft-token-ziel-klasse.md) | **verkörpert als** [`MR-025`](../../../harness/conventions.md#mr-025), Zähler **4** — und **bleibt offen**: eine Regel für Menschen kann weiter verfehlt werden. Die vierte Instanz zeigt eine **neue Ausprägung**: nicht vergessen zu zählen, sondern das **Falsche** gezählt — `grep` nach dem eben geschriebenen **neuen** Wortlaut statt nach dem alten, und damit vier Spiegel statt vierzehn. Der Eintrag nennt den richtigen Ableiter seit jeher; er wurde gelesen und in der Gegenrichtung angewandt. Gegenmittel: vor dem Ändern einer Semantik ihre Spiegel auflisten; der Ableiter für eine Wortlaut-Änderung ist das `grep` nach dem **alten** Wortlaut, und wo eine Aufzählung eine Menge spiegelt, gehört sie an ihre Quelle **gebunden** statt in die Liste geschrieben |

## Gestrichene Einträge

Regeln dieser Sektion: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer eine Zeile still löscht, macht sie ununterscheidbar von
einer, die es nie gab; darum hierher verschieben (mit Begründung), nicht löschen.

| Kennung | Beobachtung | Gestrichen am | Warum sie nicht mehr auftreten kann |
|---|---|---|---|
| BEO-001 | Ein Datei-Register und seine Autoritäts-Tabelle driften **unbemerkt** — kein Gate deckte die Richtung „Artefakt ⇒ registriert“; zweimal eingetreten ([slice-087](done/slice-087-spec-historie-referenzrichtung.md), [slice-099](done/slice-099-structure-modul.md): drei Wellen-Closures ohne Register-Zeile) | 2026-08-16 | **Für die eingetretene Instanz mechanisiert:** die dritte `planning`-Fähigkeit ([slice-102](done/slice-102-wellen-lifecycle-invariante.md), `wave-unregistered`) prüft die Richtung „Ergebnisnotiz ⇒ Register-Zeile“ und läuft im eigenen Baum scharf in `make gates` — genau die Tabelle, an der die Beobachtung zweimal eingetreten ist, kann nicht mehr still unvollständig sein. **Rest bleibt benannt, nicht gezählt:** ADR-Index und Konventionsspeicher-Index tragen dieselbe Bauform und sind weiter ungeprüft; tritt die Klasse dort ein, ist der `registry`-Modul-Vorschlag aus dem ursprünglichen Eintrag die vorgezeichnete Form (opt-in, ein Verzeichnis-Muster je Register plus Autoritäts-Datei, **eine** Richtung, ein Grund-Code) |
| BEO-005 | Eine chronologische Tabelle kippt still ihre Richtung — aus „unten anhängen“ wird „oben einfügen“, danach führt dieselbe Tabelle zwei gegenläufige Blöcke; kein Gate las Reihenfolge. Am selben Tag an **drei** Tabellen eingetreten ([slice-099](done/slice-099-structure-modul.md)); die mechanische Form stand im Eintrag vorgezeichnet (typisierter Monotonie-Vergleich der Schlüsselspalte, drei Entscheidungen, die keine Details sind) | 2026-08-21 | **Für die eingetretene Instanz mechanisiert:** die siebte `structure`-Bedingung ([slice-105](done/slice-105-tabellen-monotonie.md), `table-order`/`table-column`, v0.61.0) prüft die sechs eigenen chronologischen Bestandstabellen scharf in `make gates` — genau die Tabellen, an denen die Beobachtung eingetreten ist, können nicht mehr still kippen. Der Retro-Beleg lief mit dem **Produkt** (27 Befunde am Stand vor der welle-73-Heilung, exakt die 14 · 6 · 7 der Skript-Messung; heutiger Bestand null), und die Release-Prep der Streich-Welle war der erste gefangene Fall (Handbuch-§11-Zeile im ersten Anlauf falsch herum). **Rest bleibt benannt, nicht gezählt:** chronologische Tabellen außerhalb der sechs aktivierten Regeln, in **zwei** Tabellen gesplittete Ordnungen und fremde Repos ohne Aktivierung deckt die Mechanisierung nicht — tritt die Klasse dort ein, ist die vorgezeichnete Form eine weitere `structure`-Regel bzw. der Grenz-Trigger aus [ADR-0057](../adr/0057-structure-tabellen-monotonie.md) |
