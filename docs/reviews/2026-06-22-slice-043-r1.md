# Review-Report: slice-043 — `codepaths` `exempt-paths`-Ventil — 2026-06-22 (R1)

**Review-Art:** Unabhängiges Code-Review der Implementierung über den
Commit-Diff. Eigenes Urteil; keine Zusammenfassung übernommen. Gegenstand ist
die Umsetzung von slice-043, nicht erneut der Vertrag.

**Gegenstand:** Implementierungs-Commit `d4ebedc`
(`feat(codepaths): exempt-paths Datei-Ventil wie ids (slice-043, DC-FA-CODE-001)`).
Betroffen: `internal/hexagon/core/model/config.go`,
`internal/adapter/driven/configyaml/configyaml.go`,
`internal/hexagon/core/rules/codepaths.go`,
`internal/adapter/driving/cli/config_template.go`,
`internal/hexagon/core/app/suggest.go`, `*_test.go` (codepaths + configyaml),
`spec/lastenheft.md`, `spec/spezifikation.md`, `.d-check.yml`, `CHANGELOG.md`,
Slice-Plan.

**Skill:** `.harness/skills/reviewer.md` · **Datum:** 2026-06-22

**Eingangs-Kontext:** `DC-FA-CODE-001` (CR 0.23.0) + `DC-FA-ID-001` (ids-Vorbild)
in `spec/lastenheft.md`/`spec/spezifikation.md`; `DC-QA-02`/`DC-QA-03`;
`internal/hexagon/core/rules/paths.go` (`ignored`/`matchGlob`/`matchSegs`);
`internal/hexagon/core/rules/ids.go` (Anwendung des ids-Ventils);
`AGENTS.md` §3 (insb. §3.1 make/Docker-only, §3.2 Suppression-Verbot); slice-043.

**Adversarial belegt (alles über `make`/Image — kein Host-go):**

- `make test` grün; `internal/hexagon/core/rules` und
  `internal/adapter/driven/configyaml` beide `ok` (enthält
  `TestCodepathsExemptPaths` und den erweiterten `configyaml`-Decode-Test).
- Dogfooding-Wirkung am gebauten Image (`make doc-check`, `--network none`,
  `:ro`): Baseline 108 Dateien / 0 Befunde. Wegwerf-Report
  `docs/reviews/_probe_throwaway.md` mit Inline-Code `` `../fehlt-definitiv.md` ``
  und `` `docs/gibt-es-nicht-xyz.md` `` → 109 Dateien / **0 Befunde** (Ventil
  greift). Identischer Pfad in `docs/_probe_outside.md` (außerhalb des
  Ventils) → **1 Befund** `codepath-missing` — der Pfad klassifiziert also als
  Codepath und feuert; nur das `docs/reviews/**`-Glob unterdrückt ihn. Beide
  Probe-Dateien entfernt, Working-Tree wieder clean.
- Determinismus (DC-QA-02): `ignored(file, nil)`/`ignored(file, [])` iteriert
  über die leere Liste → `false` (`internal/hexagon/core/rules/paths.go:123`);
  ohne gesetztes `exempt-paths` läuft `CheckCodepaths` byte-identisch weiter.
- Glob-Über-Breite: derselbe `matchGlob`-Helfer ist in
  `internal/hexagon/core/rules/paths_test.go:45` (`TestMatchGlob`) bereits beide
  Richtungen getestet — `docs/archive/** → docs/archive/tief/x.md` true
  (verschachtelt), `docs/archive/** → docs/aktuell.md` false (Geschwister nicht
  getroffen). `codepaths` nutzt exakt diesen Helfer.

---

## Findings

| # | Kategorie | Quelle | Pfad | Befund | Verifizierbar |
|---|---|---|---|---|---|
| 1 | 🔵 INFO | Maintainability | `internal/adapter/driven/configyaml/configyaml.go:153` | `applyCodepaths` validiert `exempt-paths` nicht (kein Leer-/Format-Check), während `roots` Leer-Präfix und `..`/absolut ablehnt. Ein leerer oder die Repo-Wurzel verlassender Glob (z. B. `""` oder `../x`) wird stillschweigend übernommen; Wirkung ist höchstens ein nie matchendes bzw. zu breites Muster, kein falscher Befund/Exit. Parität: das ids-Ventil validiert `exempt-paths` ebenfalls nicht (`applyIDs`, ebd. Zeile 285 ff.) — bewusste, dokumentierenswerte Annahme „Glob-Liste ist frei". | nein — Code-Lesung; keine Gate-Auswirkung |
| 2 | 🔵 INFO | `DC-QA-02` / Coverage | `internal/hexagon/core/rules/codepaths_test.go:70` | `TestCodepathsExemptPaths` belegt die DC-QA-02-Richtung indirekt über zwei separate `Run`-Aufrufe (ohne Ventil 2 Befunde, mit Ventil 0), nicht über einen Byte-Vergleich desselben Befundsatzes mit/ohne leerem `exempt-paths`. Die Aussage „ohne gesetztes `exempt-paths` byte-identisch" hält am Code (`ignored(file,nil)=false`), ist aber im Test nicht als Gleichheits-Assertion fixiert. Die file-skip-Zeile selbst (`return nil`) ist von beiden Test-Richtungen abgedeckt. | ja — `make test`/`make coverage-gate` |

## Negativbefunde (geprüft, ohne Befund)

- **Korrektheit / Silent-Green:** Das Ventil greift ausschließlich datei-weit
  über `ignored(file, cfg.ExemptPaths)` als frühes `return nil` in
  `internal/hexagon/core/rules/codepaths.go:30` — keine Wirkung auf andere
  Module (eigener Aufruf in `run.go:106`), keine zu breite Glob-Wirkung über
  den bereits geprüften `matchGlob` hinaus (Geschwister-Pfade matchen nicht,
  `paths_test.go:45`). Versehentliche Unterdrückung echter Befunde nur, wenn der
  Operator ein Glob explizit setzt — wie spezifiziert.
- **Determinismus (DC-QA-02):** Ohne `exempt-paths` byte-identisch (Beleg oben).
- **Spec-Treue:** Lastenheft-CR (`spec/lastenheft.md`, Akzeptanzkriterium
  „Ventil (exempt-paths)", Out-of-Scope „Zeilen-Granularität"),
  §`DC-FA-CODE-001.a` Datei-Ventil-Absatz und §2-Schema-Zeile
  (`codepaths.exempt-paths`) decken sich mit dem Code (datei-weit, Glob wie
  `scan.ignore`, unabhängig von `roots`). Version 0.23.0 + Historie-Zeile
  vorhanden.
- **Konsistenz / ids-Parität:** Gleicher `ignored`/`matchGlob`-Helfer, gleiche
  Glob-Syntax, gleiche (fehlende) Glob-Validierung wie das ids-Ventil — saubere
  Parität; keine Erkennungs-Differenz zur Eingabe-Klasse „Datei-Ventil".
- **Refactor-Risiko (rawCodepaths):** Der anonyme Struct ist zu `rawCodepaths`
  benannt; `raw.Codepaths` und `scopeOfCodepaths(v *rawCodepaths)` sind typgleich
  (`configyaml.go:107,260`), der `Scope`-Schlüssel bleibt erhalten → DC-FA-CONF-002
  modul-lokaler Scope für `codepaths` decodet weiter (kein verwaister Verweis auf
  den alten anonymen Typ; `make test` grün).
- **suggest/print-config:** Der neue Block
  `codepaths:\n  exempt-paths: [CHANGELOG.md, "docs/reviews/**"]`
  (`suggest.go:384`) wird in `TestCLI006_AiHarness_Happy/_Boundary/_KeinQuellenfehler`
  über `configyaml.Decode` round-trip-validiert; `--print-config` ebenso
  (`cli_acceptance_test.go:811`). Kein bestehender suggest/cli-Test bricht.
- **Hard Rules (AGENTS.md §3):** Keine Inline-Suppression, keine neue
  Import-Kante (Kern→Kern via vorhandenem `ignored`), kein Netzzugriff;
  Verifikation ausschließlich make/Docker (Host-go vom Sandbox blockiert).

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| 🔴 HIGH | 0 |
| 🟡 MEDIUM | 0 |
| 🟢 LOW | 0 |
| 🔵 INFO | 2 |

## Verdikt

**ACCEPT (kein Blocker).** Keine HIGH/MEDIUM-Findings. Das Ventil ist
korrekt datei-weit, abwärtskompatibel (byte-identisch ohne Konfiguration),
glob-sauber (geteilter, beidseitig getesteter Helfer) und spec-treu; die
ids-Parität ist konsistent durchgezogen und das Dogfooding wirkt nachweislich
am gebauten Image. Die zwei INFO-Punkte (keine Glob-Validierung — paritätisch
zu ids; DC-QA-02 als Zwei-Lauf- statt Byte-Assertion) sind dokumentierenswerte
Annahmen ohne Failure-Szenario, kein Merge-Hindernis.
