# Review R1 — slice-040 „planning-consistency"-Meta-Gate

**Datum:** 2026-06-21 · **Slice:** slice-040 · **Reviewer:** unabhängiger
Subagent (adversarial, read-only) · **Commit zum Zeitpunkt:** `e4c606b`.

**Geprüft:** `tools/planning-consistency.sh`, der Slice-Plan, `Makefile`,
`AGENTS.md` §4, `harness/README.md` §Sensors, gegen das etablierte Muster
`tools/gate-consistency.sh`. Verifiziert mit funktions-isolierten Fixtures
und vier Code-Mutationen.

## Wahrheitstabelle (verifiziert)

| in-progress slice | Roadmap §Aktuelle Welle | Gate | beobachtet |
|---|---|---|---|
| vorhanden | aktiv | grün | ✓ (echte Lage) |
| vorhanden | idle (Marker) | **ROT** | ✓ — fängt genau die Audit-Drift |
| fehlt | aktiv | **ROT** | ✓ — fängt stale aktive-Welle-Behauptung |
| fehlt | idle (Marker) | grün | ✓ |

Kernregel `hasActive == hasSlices` in beiden Richtungen korrekt.

## Befunde

- **MEDIUM — Heading-Tippfehler/Umbenennung = silent-green bei vorhandenem
  Slice.** Wird `## Aktuelle Welle` umbenannt, entfernt oder um Zusatztext
  ergänzt (`## Aktuelle Wellen`, `## Aktuelle Welle (Stand 2026)`), liefert
  die awk-Extraktion einen **leeren** Block; `grep -F` findet den Marker nie
  und `has_active` meldet immer „aktiv". Bei vorhandenem Slice bleibt das
  Gate dann **grün**, obwohl die Roadmap optisch „Keine aktive Welle" sagen
  kann — die eigentliche Daseinsberechtigung des Gates. Fix-Empfehlung:
  fehlende kanonische Überschrift als harten FAIL behandeln (fail-closed).
- **LOW — Selbsttest deckt den Heading-Pfad nicht ab.** Beide Fixtures
  nutzen die exakt korrekte Überschrift; der Selbsttest kann das MEDIUM
  daher nicht regress-sichern. Empfehlung: Negativ-Fixture mit kaputter
  Überschrift.
- **NIT — `mktemp -d` ohne `trap`-Cleanup.** Leak nur auf einem unter
  `errexit` praktisch unerreichbaren Abbruch-Pfad; informativ.

## Korrekt bestätigt (kein Handlungsbedarf)

Glob-No-Match (`slice-*.md` + `[ -e ]`), Section-Scoping (Marker in anderer
H2 leakt nicht), `set -euo pipefail`-Interaktion in `if`-Bedingungen,
Exit-Code-Vertrag (Selbsttest-Bruch → 2, Drift → 1, fehlende Roadmap → 1),
`Makefile` (`.PHONY`, in `gates` vor `record-gates`, Hilfetext), Doku-Sync in
beiden Dateien, alle DoD-Punkte aus §3.

## Verdikt

**NACHBESSERN** — MEDIUM (Heading fehlt → fail-closed) + LOW (Selbsttest-
Fixture) beheben; NIT optional.

## Disposition (durch den Umsetzer)

- **MEDIUM behoben** (`2d7b136`): `heading_present()` erzwingt die kanonische
  H2 (eine `HEADING_RE`-Wahrheit für grep-Guard + awk-Extraktion); fehlt sie,
  schlägt `check_pair` fehl. Adversarial belegt: kaputte Überschrift +
  Slice → Exit 1.
- **LOW behoben** (`2d7b136`): Selbsttest Richtung C (kaputte Überschrift +
  idle-Marker + Slice muss feuern).
- **NIT won't-fix:** wie von R1 als optional eingestuft; jeder Pfad räumt
  `tmp` bereits explizit ab, ein `trap EXIT` auf eine funktions-lokale
  Variable wäre scoping-fragil.
- **Kein R2** (Nutzer-Entscheid): Fix klein, selbsttest-gesichert,
  adversarial belegt → direkt Closure.
