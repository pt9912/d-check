# Review-Report: slice-119 — Kopf-Zustandsfelder entfernt, die Sicht behält ihre begründet

**Datum:** 2026-08-22 · **Review-Art:** Doku-/Konventions-Review (geprüft gegen Slice-Plan slice-119 §2/§3/§5, Wellendokument welle-81 §1/§3/§6, Hard Rules AGENTS §1/§3.3/§3.4/§3.7, `MR-000` (Baseline-Aussage), `MR-013` (Lifecycle-Move-Bündelung), `MR-029` (v5.9.0-Pin), Kanon `.harness/baseline/v5.9.0/regelwerk/grundlagen-harness-dateien.md` §Was ein Kommentar trägt (*Dieselbe Regel für Zustandsfelder*, *Die Kopfzeile eines lebenden Registers ist derselbe Fall*) und `.harness/baseline/v5.9.0/regelwerk/modul-03-spec.md` §Ziel-Form: Spezifikation / §Ziel-Form: Architektur-Sicht, Ziel-Formen `.harness/baseline/v5.9.0/templates/spec/spezifikation.template.md`, `.harness/baseline/v5.9.0/templates/spec/architecture.template.md`, `.harness/baseline/v5.9.0/templates/spec/lastenheft.template.md`, `.harness/baseline/v5.9.0/templates/docs/plan/planning/roadmap.template.md`, `.harness/baseline/v5.9.0/templates/docs/plan/planning/observations.template.md`) mit eigenen Gegenproben am gebauten Image
**Gegenstand:** Commit `d414382` (Range `00c8fff..d414382`) — `222600c` (reiner Lifecycle-Move slice-119 `open/` → `in-progress/` samt Ruhe-Marker-Flip und zwei Pfad-Verweisen, `MR-013`) und `d414382` (Kopf-Zustandszeile entfernt in `docs/plan/planning/in-progress/roadmap.md`, `docs/plan/planning/observations.md`, `spec/spezifikation.md`; Begründungs-Absatz ergänzt in `spec/architecture.md`); **vor** der Closure, kein Release, kein Produkt-Code
**Skill:** `.harness/skills/reviewer.md` @ 1.7.0 · **Modell-ID:** `claude-opus-5[1m]`
**Eingangs-Kontext:** Slice-Plan `docs/plan/planning/in-progress/slice-119-kopf-zustandsfelder.md` (§1 Ziel, §2 Vorgehen, §3 NICHT-Liste, §4 DoD, §5 Risiken, §7 Vorgelagert); Wellendokument `docs/plan/planning/welle-81-zustandsfelder.md` (Treffer-Tabelle, §3 Closure-Trigger, §6 Out-of-Scope); geschlossene Vorgänger `docs/plan/planning/done/slice-117-baseline-v590-bump.md` und `docs/plan/planning/done/slice-118-zustandsfeld-regel.md` samt deren Reports `docs/reviews/2026-08-22-slice-117-baseline-v590-review.md` und `docs/reviews/2026-08-22-slice-118-zustandsfeld-regel-review.md`; Folge-Slice `docs/plan/planning/open/slice-120-register-und-drift-log.md`; `harness/conventions.md` §Baseline, `harness/conventions/MR-029-baseline-v590.md`; `.d-check.yml`, `.d-check.closure.yml`, `Makefile`, `.githooks/`. Nicht erhalten: die DoD-Abhakung (Verifikations-Rolle). Kein `make`-Target im echten Repo — alle Proben liefen als Image-Lauf (`d-check:latest`, netzlos, read-only) gegen `.git`-freie Baum-Kopien außerhalb des Repos; Exit je Lauf explizit gelesen (Arbeitsregel BEO-007).

## Findings

### F-1 · MEDIUM

- **kategorie:** MEDIUM
- **quelle:** Kanon `.harness/baseline/v5.9.0/regelwerk/modul-03-spec.md` §Ziel-Form: Architektur-Sicht / `.harness/baseline/v5.9.0/regelwerk/grundlagen-harness-dateien.md` §Was ein Kommentar trägt (*Die Kopfzeile eines lebenden Registers ist derselbe Fall*) / AGENTS §3.7
- **pfad:** `spec/architecture.md:8-9`
- **befund:** Der neue Absatz begründet die behaltene Zeile mit „Ein benannter Trigger pflegt sie: jede Änderung darüber (Vertrag oder Technik) zieht sie nach." Der Kanon sagt an dieser Stelle die **Gegenrichtung** — „die Sicht hat keinen [benennbaren Urheber], jede ihrer Änderungen folgt aus einer Änderung darüber" —, und er definiert den Marker als „die Aussage, wann die Sicht zuletzt **gegen den Code** gehalten wurde"; eine Änderung im Vertrag oder in der Technik ist nicht dieses Ereignis. Der genannte Trigger ist zudem nirgends im Harness verankert (`grep` nach `Frische` über `AGENTS.md`, `harness/`, `docs/user/`, `Makefile`, `.githooks/`: die einzigen lebenden Nennungen sind AGENTS §3.7, der Reviewer-Skill, der Slice-Plan und diese Datei selbst) und ist am eigenen Bestand messbar nicht gefahren: zwischen den beiden Marker-Hebungen 2026-06-29 (`e850b84`) und 2026-08-22 (`6181757`) fassten **94 Commits** `spec/lastenheft.md` oder `spec/spezifikation.md` an, davon 46 mit Lastenheft-Versions-Bump — der Marker stand still; umgekehrt änderten `3fdbcd4` (2026-06-20) und `dff44f8` (2026-07-01) den Rumpf der Sicht, ohne die Zeile zu bewegen. Beide Lesarten des Satzes tragen ein Versagen: als Beschreibung ist er von der Historie der eigenen Datei widerlegt, als Vorschrift stempelt sein Vollzug ein Datum, das eine Code-Gegenprüfung behauptet, die bei einer Vertrags-Änderung nicht stattfand. Damit steht genau der Zustand, dessen Beseitigung derselbe Commit für drei andere Dateien begründet („ein Datum, das niemand pflegt, behauptet einen [Zustand]") — nur mit einer Begründung davor. Nicht HIGH, weil der Kanon die Zeile der Sicht namentlich ausnimmt: gemeldet ist die Trigger-Aussage, nicht das Behalten der Zeile.
- **verifizierbar:** nein — kein Gate liest Zustandsfeld-Semantik; Gegenprobe 4 (dieselbe Zeile zusätzlich entfernt) bleibt bei Exit 0 / 0 Befunden. Belegt per Textvergleich gegen `.harness/baseline/v5.9.0/regelwerk/modul-03-spec.md` §Ziel-Form: Architektur-Sicht und per `git log -L3,3:spec/architecture.md` gegen `git log e850b84..6181757 -- spec/lastenheft.md spec/spezifikation.md`.
- **klasse:** zustandsfeld-nennt-einen-trigger-den-niemand-faehrt

### F-2 · LOW

- **kategorie:** LOW
- **quelle:** Ziel-Form `.harness/baseline/v5.9.0/templates/spec/architecture.template.md` (Hard-Rule-Block) / AGENTS §1 (Pointer statt Duplikat)
- **pfad:** `spec/architecture.md:5-9` gegenüber `spec/architecture.md:11-14`
- **befund:** Die Ziel-Form trägt genau diese Aussage **im Hard-Rule-Block** („**keine Historie**: `Letzte Änderung` oben ist ein Frische-Marker, kein Protokoll"), gebündelt mit den übrigen Verboten. Der Commit legt sie stattdessen als eigenen Absatz **zwischen** Statuszeile und Hard Rule; der Hard-Rule-Block dieser Datei bleibt ohne die Frische-Marker-Hälfte und führt weiter nur die Meilenstein-Freiheit. Damit hat der Kopf zwei normative Blöcke zu einem Gegenstand, und ein späterer Ziel-Form-Abgleich derselben Bauart wie dieser Slice (§2 Schritt 1: „je Datei die Ziel-Form der vendorten Vorlage danebenlegen") trägt die fehlende Hälfte in die Hard Rule nach — dann steht die Aussage zweimal in einem Kopf von 25 Zeilen. Der neue Absatz ist außerdem der einzige des Kopfes ohne Rang-Zeiger: die Hard-Rule-Zeile nennt `AGENTS.md` §3.4, der Bezug-Absatz nennt beide oberen Straten, der neue Absatz nennt seine Quelle nicht — obwohl er dieselbe Regel referiert, die AGENTS §3.7 und der Reviewer-Skill bereits tragen.
- **verifizierbar:** nein — `structure` führt für `spec/architecture.md` keine Abschnitts-Invariante (nur `table-order`-Regeln auf `spec/lastenheft.md`, `spec/spezifikation.md`, Roadmap, `version.md`, Handbuch); Gegenprobe 1 bleibt bei 0 Befunden. Belegt per Zeilenvergleich Datei ↔ Vorlage.
- **klasse:** aussage-neben-statt-in-dem-block-den-die-ziel-form-dafuer-fuehrt

### F-3 · LOW

- **kategorie:** LOW
- **quelle:** Wellendokument welle-81 §1 (Treffer-Tabelle, Ist-Zustand-Spalte) / Slice-Plan slice-119 §1
- **pfad:** `docs/plan/planning/welle-81-zustandsfelder.md:39`
- **befund:** Die Zeile *Kopfzeilen lebender Register* sagt „**drei** Dateien tragen `**Status:** Aktiv. **Letzte Änderung:** …`", während die Spezifikation und die Sicht je eine eigene Zeile derselben Tabelle haben. Gemessen trugen die Zeile vier Dateien — zwei lebende Register (`docs/plan/planning/in-progress/roadmap.md`, `docs/plan/planning/observations.md`), die Spezifikation und die Sicht —, und zwar unverändert seit der Wellen-Eröffnung (`git grep` am Eröffnungs-Commit `d07bf2e` und am Range-Vorgänger `00c8fff`: dieselben vier Pfade). Die Tabellen-Zahl und die Gesamtzahl des Slice („vier Dateien") können nicht beide stimmen: bei drei Registern wären es fünf. Die Zeile ist der Ist-Zustand, gegen den §3 die Wellen-Closure lesen lässt („Kein lebendes Register und kein Technik-Stratum trägt eine Kopf-Zustandszeile") — sie überzählt den größten Posten der Welle um eins.
- **verifizierbar:** nein — kein Gate zählt Treffer in Planungs-Tabellen. Belegt per `git grep -l "Status:\*\* Aktiv"` an `d07bf2e`, `00c8fff` und `d414382` außerhalb des vendorten Baums und der Review-Reports.
- **klasse:** ist-zustand-zahl-im-wellendokument-nicht-nachgemessen

### F-4 · LOW

- **kategorie:** LOW
- **quelle:** Ziel-Formen `.harness/baseline/v5.9.0/templates/spec/spezifikation.template.md` und `.harness/baseline/v5.9.0/templates/spec/architecture.template.md` (Rollen-Zeile) / Slice-Plan slice-119 §2 Schritt 1
- **pfad:** `spec/spezifikation.md:3` und `spec/architecture.md:3-25`
- **befund:** Beide Vorlagen führen im Kopf neben dem Bezug eine **Rollen-Zeile** — „Technik-Stratum — fortschreibbar ohne Change Request; eine ADR darf sie schärfen, das Lastenheft nicht" bzw. „Sicht-Stratum — *keine* eigenen Anforderungen, derivativ", je mit Regel-Zeiger auf `modul-03-spec.md`. Keine der beiden Dateien trägt sie; `harness/conventions.md` deklariert dazu keine Adaption (`grep` nach `Technik-Stratum`/`Sicht-Stratum` findet außerhalb des vendorten Baums nur die aufgelöste `MR-027` und den neuen Absatz der Sicht). Nach dem Entfernen besteht der Kopf der Spezifikation aus einer einzigen Zeile, und die Frage, die ihn vom Vertrag trennt — braucht diese Änderung einen Change Request? —, beantwortet die Datei selbst nicht mehr; sie stand auch vorher nicht drin, aber der Slice hat den Kopf gegen genau diese Vorlage gehalten (§2 Schritt 1). Bestandslage, nicht durch den Commit erzeugt.
- **verifizierbar:** nein — keine Abschnitts- oder Kopf-Regel der Prüf-Profile deckt Kopf-Felder; Gegenprobe 1 bleibt bei 0 Befunden. Belegt per Zeilenvergleich Datei ↔ Vorlage.
- **klasse:** ziel-form-abgleich-nur-auf-dem-entfernten-feld-nicht-auf-dem-kopf

## Negativbefunde (geprüft, ohne Befund)

1. **Auswahl vollständig — der Zensus ist selbst gemessen, nicht aus dem Plan übernommen.** `git grep` am Range-Vorgänger `00c8fff` nach `Status:** Aktiv` liefert außerhalb des vendorten Baums und der Review-Reports genau vier lebende Fundstellen: `docs/plan/planning/in-progress/roadmap.md:3`, `docs/plan/planning/observations.md:3`, `spec/spezifikation.md:3`, `spec/architecture.md:3`. Der Suchraum war breiter als der Wortlaut: `^\*\*Status:\*\*`, `^\*\*Stand`, `Letzte Änderung` und ein Kopf-Sweep (erste acht Zeilen) über **alle** getrackten `*.md` außerhalb `.harness/baseline/**`, `done/` und `docs/reviews/**`. Die übrigen Kopf-Felder sind je begründet: ADR-Köpfe (`**Status:** Accepted` / `**Datum:**`) und der Lastenheft-Kopf folgen ihren Ziel-Formen, `docs/user/benutzerhandbuch.md:3-4` trägt Handbuch-/Software-Version plus `Stand` nach eigenem Standard §9 („Ohne Versionsbezug ist ein Handbuch bei produktiver Software schnell wertlos"), die beiden Skill-Dateien tragen `Version · Datum` mit dem Versions-Bump als Trigger. Ein Reconciliation-Register führt d-check nicht (reiner GF-Betrieb). Ohne Befund.
2. **Richtigkeit der drei Entfernungen.** Roadmap und Beobachtungs-Register sind die im Kanon namentlich genannten lebenden Register; die vendorten Ziel-Formen `roadmap.template.md` und `observations.template.md` führen nach dem Titel **keine** Kopf-Zeile, sondern direkt Format-Regel bzw. Regel-Absatz — genau die Form, die beide Dateien jetzt haben. Für die Spezifikation sagt `modul-03-spec.md` §Ziel-Form: Spezifikation „Kein Kopf-Datum, kein Kopf-Status" mit derselben Begründung, die die Commit-Botschaft trägt (zwei Felder für eines driften); die Vorlage trägt entsprechend nur Bezug und Rolle. Ohne Befund.
3. **Richtigkeit des Behaltens.** Die Sicht führt nachweislich keine Historie (`spec/architecture.md` hat fünf `##`-Abschnitte, keiner davon eine Historie), und der Kanon nimmt ihren Marker zweimal ausdrücklich aus. Das Behalten ist damit korrekt; die Kritik an der *Begründung* steht als F-1. Ohne Befund.
4. **Der Vertrag ist unberührt (Slice §3, Welle §6).** `spec/lastenheft.md:3-7` trägt weiter `**Version:** 0.62.1`, `**Status:** Draft`, `**Autor:**/**Datum:**` — die Ziel-Form `lastenheft.template.md` führt Version und Status unverändert, weil der Version-Bump der Fußabdruck des Change Requests ist. Die Datei ist in der ganzen Range nicht angefasst. Ohne Befund.
5. **Der fünfte Treffer ist unangetastet und richtig adressiert.** `harness/conventions.md:27` (`**Stand:** v5.9.0` mit der angehängten Kette aller bisherigen Pin-Hebungen) steht byte-gleich; die Datei ist in der Range nicht berührt. `docs/plan/planning/open/slice-120-register-und-drift-log.md` §2 Schritt 4a beansprucht sie ausdrücklich als nachgemessenen fünften Treffer. Der Übergang ist damit belegt, nicht bloß behauptet. Ohne Befund.
6. **Kein Rest in Prüf-Regeln, Tests oder Werkzeugen.** `grep` über `.d-check.yml`, `.d-check.closure.yml`, `Makefile`, `.githooks/`, `tools/`, `internal/`, `cmd/`: keine Regel liest die entfernte Zeile. Der naheliegende Verwechslungskandidat ist geprüft und trifft nicht — der „Aktiv-Status" des Moduls `planning` (`internal/hexagon/core/rules/planning.go:51`, `planning_waves.go`) ist der Block unter der konfigurierten Überschrift `## Offene Wellen` (`.d-check.yml:349`, `.d-check.closure.yml:23`), nicht die Kopfzeile; die `structure`-Regeln greifen an `## 7. Historie`, `## Historische Trigger-Verschiebungen`, `## Abgeschlossene Wellen`, `## Verlauf` und `## 11. Änderungshistorie` (`.d-check.yml:259-278`). Gegenprobe 2 bestätigt es am Produkt: `planning` läuft am Commit-Stand auf Exit 0. Ohne Befund.
7. **Kein Anker- und kein Prosa-Rest.** Die entfernten Zeilen waren keine Überschriften; es gibt keinen Anker, der auf sie zeigen könnte, und Gegenprobe 1 (mit `links`, `anchors`) ist grün. `grep` nach `Kopfzeile`, `Kopf-Datum`, `Kopf-Status`, `siehe Kopf`, `im Kopf`, `Letzte Änderung`, `Status: Aktiv` über den lebenden Bestand ergibt als Nenn-Stellen: AGENTS §3.7 und der Reviewer-Skill (die Regel selbst — beide sprechen im Verbot, nicht im Bestand), der Slice-Plan und das Wellendokument (Vorhaben), `harness/conventions/MR-029-baseline-v590.md:33-36` (beschreibt das Baseline-Delta, keine Aussage über den eigenen Bestand) und `docs/reviews/2026-08-22-slice-117-baseline-v590-review.md:78` (Vergangenheitsaussage über den damaligen Stand, Lauf-Beleg). Keine Stelle steht nach dem Commit falsch da. Ohne Befund.
8. **Kein Informations-Verlust an der Register-Kopfzeile.** Die entfernte Zeile in `docs/plan/planning/observations.md` war eine vierstufige Chronik („… Davor … Davor … Davor …"). Jede darin genannte Tatsache steht in der Tabelle selbst: BEO-010 Zähler 1 mit Beleg slice-115, BEO-009 Zähler 2 mit Beleg slice-111, BEO-008 Zähler 3 mit drei Belegen (slice-106/110/117), BEO-007 als verkörpert, BEO-002 Zähler 3. Zählerstände, Belege und Stand-Zellen sind im Commit unverändert (`2 --`, nur die Kopfzeile plus Leerzeile). Ohne Befund.
9. **Die §7-Historie der Spezifikation trägt die Ziel-Form.** Kopf `| Datum | Änderung |` — **keine** Versions-Spalte, wie `spezifikation.template.md` §7 und `modul-03-spec.md` („Ihre Historie führt Datum und Änderung — keine Version") es verlangen; oberste Datenzeile 2026-08-22, absteigend, die `structure`-Regel `table-order: desc` läuft grün (Gegenprobe 1). Der Abschnitt ist im Commit nicht angefasst, die Botschaft behauptet das auch nicht anders. Zum restlichen Kopf siehe F-4. Ohne Befund.
10. **Scope-Treue gegen Slice §3 und Welle §6.** Der Inhalts-Commit fasst genau vier Dateien an (+6/−6). Keine `Stand`-Zelle, keine Drift-Log-Zeile, keine Meilenstein-Tabelle: der Roadmap-Diff besteht aus den zwei Kopfzeilen, der Register-Diff ebenso, `## Historische Trigger-Verschiebungen` und `## Abgeschlossene Wellen` sind unberührt. Kein Produkt-Code, keine Config, kein Gate, keine Version, kein Release, kein Sensor. Ohne Befund.
11. **Der Lifecycle-Move im Range-Vorlauf ist sauber (AGENTS §3.3, `MR-013`).** `222600c` ist ein reiner `git mv` (Slice-Body 0 geänderte Zeilen) gebündelt mit dem Ruhe-Marker-Flip in der Roadmap (Marker raus, weil `in-progress/` nicht mehr leer ist — die Marker-Hälfte der Offene-Wellen-Form, kein Eingriff in ein Zustandsfeld), dem Zeiger `open/` → `in-progress/` in welle-81 §4 und dem Folge-Slice-Zeiger in der slice-118-Closure-Notiz. Inhalt und Move liegen in getrennten Commits. Ohne Befund.
12. **Der HIGH-Anker *Zustandsfeld trägt Chronik*, auf den Bestand nach diesem Commit angewandt.** Kein verbliebenes **Kopf**-Zustandsfeld in slice-119s Zuständigkeit erzählt eine Chronik: die drei Chronik-tragenden Flächen sind die `Stand`-Zellen des Beobachtungs-Registers, die Schließungs-Zeilen des Drift-Logs und die Pin-Ketten-Klammer in `harness/conventions.md:27` — alle drei sind slice-120 zugeschrieben (dessen §1 und §2 Schritt 3/4a) und in diesem Commit unangetastet. Die Kopfzeile der Sicht ist ein Datum ohne Chronik; ihr Problem ist der Trigger (F-1), nicht die Erzählung. Grenzfälle gegengelesen und außerhalb: RTM-`Status`-Spalten, ADR-Index-`Status`, `**Ausgang:**`-Felder der Slice-Pläne, die `Status`-Spalte der Meilenstein-Tabelle (leer). Ohne Befund.
13. **Risiken §5 des Plans sind adressiert.** Risiko 1 („eine Abschnitts-Regel könnte die Zeile erwarten") ist gemessen widerlegt — Negativbefund 6 plus Gegenproben 1–4, die zeigen, dass die Zeile in **beiden** Richtungen gate-neutral ist. Risiko 2 („die Sicht-Ausnahme sieht wie eine Inkonsistenz aus") ist durch den Absatz in der Datei adressiert, also am verlangten Ort; die Qualität seiner Aussage steht als F-1. Die `**Ausgang:**`-Felder sind erwartungsgemäß noch offen (Closure-Schritt). Ohne Befund.
14. **Die Commit-Botschaft behauptet keine Probe, die nicht stattfand (BEO-009).** „Probe: make doc-check grün (433 Dateien / 0 Befunde)" ist unabhängig reproduziert — Gegenprobe 1 liefert wörtlich `433 Datei(en) geprüft, 0 Befund(e)` bei Exit 0. Auch die Rand-Behauptung („keine Prüf-Regel, kein Test und keine Autoritäts-Doku hängt an den entfernten Zeilen — Suche über Prüf-Profile, `internal/`, `tools/` und `Makefile`") ist unabhängig nachgesucht und hält (Negativbefund 6). Die Zahl „vier Dateien" der Botschaft ist korrekt; die abweichende Zahl steht im Wellendokument (F-3). Ohne Befund.
15. **Die Statuszeile der Sicht ist unverändert übernommen.** `spec/architecture.md:3` steht byte-gleich zum Vorzustand (`**Status:** Aktiv. **Letzte Änderung:** 2026-08-22.`); der Commit fügt ausschließlich sechs Zeilen ein, ohne Datum oder Status zu bewegen. Die Datei bleibt sprach- und meilensteinfrei (AGENTS §3.4): der neue Absatz nennt keine Welle, keinen Slice, keinen Commit-Hash, kein Closure-Datum und keine Technologie. Ohne Befund.

## Gegenproben (Exit explizit)

Alle Läufe netzlos und read-only gegen `.git`-freie Baum-Kopien außerhalb des Repos, Ausgabe je Lauf in eine Datei umgeleitet und der Exit separat gelesen (BEO-007).

| # | Baum | Lauf | Exit | Ausgabe |
|---|---|---|---|---|
| 1 | `d414382` | Sollform, volles Profil (`doc-check`-Äquivalent) | 0 | 433 Datei(en), 0 Befund(e) |
| 2 | `d414382` | `--enable planning` mit abgewählten Datei-Modulen (`planning-check`-Äquivalent) | 0 | 433 Datei(en), 0 Befund(e) |
| 3 | `00c8fff` (alle vier Kopfzeilen noch da) | volles Profil | 0 | 433 Datei(en), 0 Befund(e) |
| 4 | `d414382` **plus** entfernte Kopfzeile der Sicht | volles Profil | 0 | 433 Datei(en), 0 Befund(e) |
| 5 | Arbeitsstand inklusive dieses Reports | volles Profil | 0 | 434 Datei(en), 0 Befund(e) |

Lesart: Proben 3 und 4 belegen, dass **kein** Gate die Kopf-Zustandszeile in irgendeiner Richtung hält — weder ihr Vorhandensein noch ihr Fehlen. Das bestätigt die Kanon-Aussage „Kein Gate fängt das" am eigenen Bestand und ist zugleich der Grund, warum F-1 nur per Urteil und nicht per Gate-Lauf verifizierbar ist.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 1 |
| LOW | 3 |
| INFO | 0 |

**Finding-Klassen dieses Laufs:** zustandsfeld-nennt-einen-trigger-den-niemand-faehrt · aussage-neben-statt-in-dem-block-den-die-ziel-form-dafuer-fuehrt · ist-zustand-zahl-im-wellendokument-nicht-nachgemessen · ziel-form-abgleich-nur-auf-dem-entfernten-feld-nicht-auf-dem-kopf

## Verdikt

**Merge-blockierend:** ja — F-1 (MEDIUM) blockiert nach der Regel des Skills. Die Begründung ist inhaltlich tragend und nicht formal: sie ist der einzige Grund, aus dem eine der vier Zeilen stehen bleibt, sie ist die Aussage, gegen die der nächste Durchgang liest, und sie ist am eigenen Bestand widerlegt (94 Änderungen „darüber" ohne Nachzug). Die drei LOW blockieren nicht; F-3 betrifft ein Planungs-Artefakt, das die Wellen-Closure noch liest, F-4 ist Bestandslage.

**Übergabe:** Findings gehen an den Implementer; die Finding-Klassen gehen zusätzlich in die Slice-Closure §7 und von dort in den Zähler. Dieser Report ist ein **Lauf-Beleg** (dieser Diff, dieser Skill, dieses Modell, dieses Verdikt) und ersetzt keine Verifikation — DoD- und Spec-Konformität prüft der Verifier separat.
