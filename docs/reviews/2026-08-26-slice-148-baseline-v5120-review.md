# Review-Report: slice-148 — Etappe A, Bundle `v5.12.0` vendored und Pin gehoben

**Datum:** 2026-08-26 · **Review-Art:** Doku-/Konventions-Review (geprüft gegen Slice-Plan slice-148 §1/§2/§3/§4/§5/§7, Wellendokument welle-85 §1/§3/§6, `MR-011`/`MR-013`/`MR-021`/`MR-023`/`MR-030`/`MR-037`, vendorte Vorlage `MR-NNN-titel.template.md`, Hard Rules `AGENTS.md` §3/§5, Beobachtungs-Register `BEO-008`/`BEO-011`/`BEO-012`), unabhängiger Reviewer ohne Anteil an der Arbeit
**Gegenstand:** Commit `9ee805b` (`HEAD~1..HEAD`) — *„feat(harness): Baseline-Pin auf v5.12.0 — und eine vierte Spiegel-Klasse, die BEO-008 nicht führt"*; **79 Dateien, 216 Einfügungen / 122 Löschungen** (`git show 9ee805b --stat`)
**Skill:** `.harness/skills/reviewer.md` @ 1.10.0 (`9ee805b`) · **Modell-ID:** `claude-opus-5[1m]`

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde):

- Slice-Plan `docs/plan/planning/done/slice-148-baseline-v5120-vendoring.md`
- Wellendokument `docs/plan/planning/welle-85-baseline-v5120-migration.md`
- `harness/conventions.md` §Baseline, §Adoptierte Konventions-Quellen, §Adaptions-Block (beide Index-Tabellen)
- `harness/conventions/MR-037-baseline-v5120.md` (neu), `harness/conventions/done/MR-030-baseline-v5110.md` (verschoben), `MR-011`, `MR-021`, `MR-023`, `MR-033`
- vendorte Vorlage `.harness/baseline/v5.12.0/templates/harness/conventions/MR-NNN-titel.template.md`
- `AGENTS.md` §3 (Hard Rules) und §5
- `docs/plan/planning/observations.md` — `BEO-008` (drei Spiegel-Klassen), `BEO-011`, `BEO-012`
- **Vorherige Findings am gleichen Vorgang:** `docs/reviews/2026-08-23-slice-128-baseline-v5110-review.md` (Vorgänger-Hebung, vier MEDIUM / drei LOW / ein INFO) und `docs/reviews/2026-08-26-slice-143-structure-abschnitts-skopus-review.md`
- `docs/plan/cr/2026-08-26-antwort-regelwerk-v5110.md` (die Lieferzusage, gegen die das Delta gehalten wurde)
- `.d-check.yml` (`scan.ignore`, `ignore-refs`, `codepaths`, `versions`, `structure`, `planning`), `tools/harness/fetch-baseline-cache.sh`, `internal/hexagon/core/rules/paths.go` (`matchGlob`/`refIgnored`)
- **Nicht erhalten:** die DoD-Abhakung (Verifikations-Rolle, getrennter Kontext, anderes Prüf-Artefakt)

**Vom Reviewer selbst gefahren** (Exit je Lauf in eine Datei umgeleitet und direkt gelesen, `BEO-007`):

- `make gates` Exit **0** — zehn Glieder (`baseline-verify + workflow-pins + doc-check + lint + test + arch-check + coverage-gate + semgrep + gate-consistency + planning-check`), `doc-check` 506 Dateien / 0 Befunde.
- `make fullbuild` Exit **0** — Abschluss `image-hash sha256:f41f99ad9a1e6d256423de4cf3aec77586e16fcedc3aac7d2d1cee408dfafc72`, Closure-Profil 464 Dateien / 0 Befunde, 48 Anforderungen / 0 Waisen.
- `bash tools/harness/fetch-baseline-cache.sh --verify` Exit **0** — „verify ok (51 Dateien, vollständig)".
- **Fünf eigene Gate-Gegenproben mit temporären Sonden** (jede unmittelbar nach dem Lauf mit `rm` entfernt; `git status --short` danach leer, kein `git checkout --` nötig, da nur neue Dateien angelegt wurden): `docs/reviews/zz-probe-review.md` mit `v5.11.0`-Link (**P1**, Exit 0), dieselbe Datei mit fehlendem `v5.12.0`-Ziel (**P2**, Exit 2 `target-missing`), `docs/plan/zz-probe.md` mit `v5.11.0`-Link (**P3**, Exit 2 `target-missing`), `docs/plan/planning/done/zz-probe.md` mit `v5.11.0`-Link (**P4**, Exit 0), `harness/conventions/done/zz-probe.md` mit der Zeigerform aus `MR-037` (**P5**, Exit 2 `target-missing`).
- **Beide gepinnten Bäume rekonstruiert und Datei für Datei verglichen** (`git archive HEAD~1 .harness/baseline/v5.11.0` gegen `git archive HEAD .harness/baseline/v5.12.0`), inklusive maschineller Prüfung aller Zwei-Zeilen-Deltas.
- **Zwei eigene Zensus-Skripte** unter dem Scratchpad: eine Klassifikation aller `v5.11.0`/`v5.12.0`-Vorkommen nach Pfad/URL/nackt über `git ls-tree` beider Revisionen, und eine Zitat-Drift-Probe (alle `„…"`-Spannen ≥15 Zeichen über **alle** getrackten `.md`/`.yml`/`.sh`/`.go`/`.json`-Dateien, Whitespace- und Auszeichnungs-normalisiert, gegen den alten **und** den neuen Baum gehalten).
- Keine Repo-Datei wurde verändert; die einzige geschriebene Datei ist dieser Report.

**Verdikt: blockierend** — kein HIGH, zwei MEDIUM, fünf LOW.

---

## Findings

### F-1

- **kategorie:** MEDIUM
- **quelle:** `BEO-011` (Aussage aus dem Anlass statt aus dem Bestand) · Reviewer-Skill §MEDIUM *„Botschaft verallgemeinert über die Messung hinaus — suche die N+1-te Form"* · Slice-Plan §2 Schritt 3 / §4 DoD („Die Zitat-Klasse ist **gemessen** und beantwortet")
- **pfad:** Commit-Botschaft `9ee805b`, Absatz „DIE VIERTE KLASSE IST REAL UND GEMESSEN" (gespiegelt in `docs/plan/planning/welle-85-baseline-v5120-migration.md` als Folge-Frage)
- **befund:** Die Botschaft beziffert die neue Klasse mit „vier Dokument/Quell-Paare, ZWEI Dokumente" und benennt sie als den CR und `MR-033`. Eine Gegenmessung mit **demselben** Raster (Zitat gegen die aus den fünf inhaltlich geänderten Dateien verschwundenen Zeilen, ≥50 Zeichen) findet ein **drittes** Dokument mit zwei weiteren Paaren: `docs/reviews/2026-08-23-slice-136-agents-34-klaerung-review.md:129-133` zitiert beide getilgten Sätze wörtlich (86 bzw. 105 Zeichen) und nennt als Fundstelle sogar `.harness/baseline/v5.11.0/templates/AGENTS.template.md:128–138`. Auch die Mengen-Angabe „die sieben Zeilen, die aus den fünf geänderten Dateien verschwanden" ist gegen den Diff nicht reproduzierbar: entfernt wurden **neun** Zeilen, davon fünf Versions-Stempel, also **vier** mit Inhalt. Die *Schlussfolgerung* („genau eine lebende Instanz") ist davon unberührt — sie ist unabhängig bestätigt (Negativbefund 7) —, der Zensus, der sie trägt, ist es nicht.
- **verifizierbar:** ja — Zitat-Drift-Probe über alle getrackten `.md`/`.yml`/`.sh`/`.go`/`.json`-Dateien gegen beide rekonstruierten Bäume; `sed -n '126,136p' docs/reviews/2026-08-23-slice-136-agents-34-klaerung-review.md` zeigt beide Zitate im Volltext. Kein Gate deckt die Klasse (das ist ihre Definition).
- **klasse:** `zensus-unvollstaendig-trotz-eigenem-raster`

### F-2

- **kategorie:** MEDIUM
- **quelle:** vendorte Vorlage `.harness/baseline/v5.12.0/templates/harness/conventions/MR-NNN-titel.template.md` §*Regeln dieser Datei* („`Löst auf` und `Ausgelöst durch Baseline-Stand` **nur, wenn** dieser Eintrag einen früheren ablöst") und Feldzeile *(„Pflicht zusammen mit „Löst auf"")* · **vorheriges Finding am gleichen Vorgang:** `docs/reviews/2026-08-23-slice-128-baseline-v5110-review.md` Negativbefund 13 („`Löst auf` und `Ausgelöst durch Baseline-Stand` als Paar gesetzt — **die Auflage, an der der Vorgänger-Review anschlug**")
- **pfad:** `harness/conventions/MR-037-baseline-v5120.md:50`
- **befund:** `MR-037` setzt `Ausgelöst durch Baseline-Stand: v5.12.0` und löst ausweislich seines eigenen Rumpfs und der Index-Tabelle (`conventions.md:148`, „MR-030 … aufgelöst durch MR-037") einen früheren Eintrag ab — das Feld `Löst auf:` fehlt jedoch ganz. Die Vorlage führt beide Felder ausdrücklich als Paar, und die zwei unmittelbaren Vorgänger der Pin-Serie (`done/MR-029-baseline-v590.md:67`, `done/MR-030-baseline-v5110.md:74`) tragen es. Damit ist der Ablöse-Zeiger, den die Vorlage in der bruchsicheren Index-Anker-Form vorsieht, im Eintrag nicht vorhanden; auffindbar bleibt die Kette nur über `conventions.md`.
- **verifizierbar:** nein — kein Gate prüft den Feldsatz einer `MR-`Datei (`structure` erfasst nur Überschriften, `planning` nur Slice-/Wellen-Lifecycle). Belegt durch Feld-Inventar (`grep -n '^- \*\*' `) gegen die vendorte Vorlage und gegen `MR-023`/`MR-026`/`MR-028`/`MR-029`/`MR-030`.
- **klasse:** `mr-feldpaar-halbiert`

### F-3

- **kategorie:** LOW
- **quelle:** vendorte Vorlage `MR-NNN-titel.template.md`, Feld `Löst auf` *(„der Verweis geht auf die Index-Zeile, nicht auf die Eintrags-Datei: die wandert bei Auflösung nach `done/` und ein Pfad-Link bricht genau dann")* · `MR-013` (Lifecycle-Move bündelt gekoppelte Verweise)
- **pfad:** `harness/conventions/MR-037-baseline-v5120.md:19`
- **befund:** Der Ablöse-Zeiger steht als Pfad-Link `[`MR-030`](done/MR-030-baseline-v5110.md)` im Rumpf — genau die Form, vor der die Vorlage an dieser Stelle warnt. Heute löst er auf; sobald `MR-037` bei der nächsten Pin-Hebung selbst nach `conventions/done/` wandert, zeigt er auf `harness/conventions/done/done/MR-030-baseline-v5110.md`. Es ist dieselbe Reparatur, die **dieser** Commit an `MR-030` dreimal vornehmen musste (`done/MR-029-…` → `MR-029-…`), und `MR-030` trägt die Begründung dafür im eigenen `Löst auf`-Feld mit.
- **verifizierbar:** ja — Sonde P5: eine Datei in `harness/conventions/done/` mit demselben Ziel liefert `make doc-check` Exit **2**, `done/MR-030-baseline-v5110.md target-missing`.
- **klasse:** `ablöse-zeiger-als-pfad-link`

### F-4

- **kategorie:** LOW
- **quelle:** vendorte Vorlage `MR-NNN-titel.template.md` §*Regeln dieser Datei* („Pflichtfelder sind Datum, Geltungsbereich, **Ersetzt-Baseline-Regel**, Adaption, **Begründung** und Auflösungs-Trigger")
- **pfad:** `harness/conventions/MR-037-baseline-v5120.md` (Feldsatz, Z. 3–51)
- **befund:** Das Pflichtfeld `Begründung:` fehlt. Alle fünf Vorgänger der Pin-Serie tragen es (`MR-023`, `done/MR-026`, `done/MR-028`, `done/MR-029`, `done/MR-030` — dort mit dem Auslöser, der Asset-Wahl und der Currency-Gegenprobe). Ohne das Feld fehlt dem Freshness-Audit der Etappe B (welle-85 §3 Trigger 2: *„Regelt die neue Fassung das, wofür diese Adaption angelegt wurde?"*) genau die Angabe, gegen die er fragt; die drei letzten aktiven Einträge davor (`MR-032`–`MR-036`) haben dieselbe Lücke, `MR-037` ist der erste der Pin-Serie mit ihr.
- **verifizierbar:** nein — kein Gate prüft den Feldsatz. Belegt durch `grep -c '^- \*\*Begründung:'` über alle aktiven und aufgelösten `MR-`Dateien.
- **klasse:** `mr-pflichtfeld-fehlt`

### F-5

- **kategorie:** LOW
- **quelle:** `MR-030` §Adaption (Präzedenz: „von 52 Bundle-Dateien unterscheiden sich **30**; 23 davon ändern ausschließlich den Versions-Stempel …, **eine ist das Manifest**") · Vorgänger-Review F-5 (`delta-kategorie-ueberzaehlt`)
- **pfad:** `harness/conventions/MR-037-baseline-v5120.md:25` · gleichlautend Commit-Botschaft `9ee805b`, Absatz „DAS DELTA IST GEMESSEN" und `docs/plan/planning/welle-85-baseline-v5120-migration.md:26`
- **befund:** „von **52** Bundle-Dateien unterscheiden sich **28**". Gemessen unterscheiden sich **29** von 52: die 22 Stempel-Dateien, `regelwerk/README.md`, die fünf Inhalts-Dateien **und `SHA256SUMS`** (+28/−28 Zeilen, im `--stat` als `56 +++---` ausgewiesen). Der Nenner 52 enthält das Manifest, der Zähler 28 nicht; die Subtraktion ist nirgends genannt. Der unmittelbare Vorgänger hat genau diese Ausweisung vorgenommen, und der Vorgänger-Review hat die analoge Kategorie-Verschiebung als F-5 beanstandet.
- **verifizierbar:** nein — Prosa-Zahl. Belegt durch `diff -rq` beider aus `HEAD~1`/`HEAD` rekonstruierten Bäume (29 abweichende Dateien, identische Dateiliste, je 52 Dateien).
- **klasse:** `delta-nenner-und-zaehler-verschieden-abgegrenzt`

### F-6

- **kategorie:** LOW
- **quelle:** `DC-FA-REF-001.a` (geteiltes Referenz-Ventil, Quell-Skopus `in`) · `MR-021` §Adaption („Wird (2) vergessen, schlägt nach (1) `make doc-check` mit `target-missing` an — die Pin-Kopplung ist damit **gate-erzwungen, kein stiller Drift**") · Präzedenz der drei älteren Tombstones in derselben Datei (datei-genau)
- **pfad:** `.d-check.yml:109-110`
- **befund:** Der Quell-Skopus `in: docs/reviews/**` nimmt nicht nur die **vierzehn** heute betroffenen eingefrorenen Dateien aus, sondern jede künftige Datei desselben Verzeichnisses. Anders als bei `docs/plan/planning/done/**` — wo ein Slice seine scharfe Phase in `open/`/`in-progress/` durchläuft und ein falscher Verweis dort noch rot wird — entstehen Review-Reports unmittelbar unter `docs/reviews/`; ein neu geschriebener Report, der `.harness/baseline/v5.11.0/…` verlinkt (der naheliegende Copy-Paste beim Report zu slice-149), trägt einen toten Link, und `make doc-check` bleibt grün. Die drei älteren Tombstones derselben Datei (`v1.4.0`, `v5.0.0`, `v5.6.0`) sind datei-genau und haben diese Eigenschaft nicht; der Kommentar begründet den Glob mit dem Wachstum der Menge, benennt die Ausweitung auf künftige Dateien aber nicht.
- **verifizierbar:** ja — Sonden P1/P3/P4: dieselbe Referenz liefert aus `docs/plan/` Exit **2** (`target-missing`), aus `docs/reviews/` und `docs/plan/planning/done/` Exit **0**. Sonde P2 belegt zugleich, dass die `refs`-Seite eng bleibt.
- **klasse:** `ventil-quell-skopus-deckt-kuenftige-dateien`

### F-7

- **kategorie:** LOW
- **quelle:** `BEO-011` · `MR-030` §Adaption (der Drei-Klassen-Zensus ausdrücklich „fortgeschrieben für den Nachfolger") · Slice-Plan §2 Schritt 2 („Die **Zahl** kommt in die Closure-Notiz")
- **pfad:** `harness/conventions/MR-037-baseline-v5120.md:42` · gleichlautend Commit-Botschaft `9ee805b`, Klasse 3
- **befund:** „nackte Nennungen **24**, davon **4 gehoben** und 20 … stehengelassen". Weder `MR-037` noch die Botschaft nennt die Messvorschrift, und keine der naheliegenden ergibt 24: eine Zählung aller `v5.11.0`-Vorkommen außerhalb von Pfad- und URL-Form liefert repo-weit **60** an `HEAD~1`; dieselbe Zählung ohne die eingefrorenen Verzeichnisse (`docs/reviews/**`, `docs/plan/planning/done/**`, `harness/conventions/done/**`, `docs/plan/cr/**`, `CHANGELOG.md`) liefert **22**, und an `HEAD` stehen in lebenden Dateien **14** solcher Nennungen. Die beiden anderen Klassenzahlen sind demgegenüber exakt reproduzierbar (Negativbefunde 1 und 3). Ein Zensus, der als Checkliste für die nächste Hebung dient, aber nicht nachgemessen werden kann, ist bei der nächsten Hebung kein Vergleichspunkt.
- **verifizierbar:** nein — Prosa-Zahl ohne Vorschrift; kein Gate liest nackte Versions-Nennungen (`versions.pin-pattern` matcht nur `ghcr.io`-Pins, `.d-check.yml:287`). Belegt durch die eigene Klassifikation über beide Revisionen.
- **klasse:** `zahl-ohne-messvorschrift`

---

## Negativbefunde

1. **Klasse 1 (Pfad-Verweise), eigene Zählung mit eigenem Muster — die Zahlen stimmen exakt.** Nicht `.harness/baseline/…`, sondern `baseline/v5\.[0-9]+\.[0-9]+` über **alle** getrackten Dateien, unabhängig von Endung und relativer Tiefe. Ergebnis: **42** lebende Vorkommen auf `v5.12.0` (24 Dateien) und **76** eingefrorene auf `v5.11.0`, zusammen **118** — genau die drei Zahlen aus `MR-037:39-41`. Die relative Schreibweise (`../baseline/…`, `../../.harness/…`, `../../../../.harness/…`) ist mitgezählt; die fünf Vorkommen in `.harness/skills/reviewer.md`, die die Botschaft als Beinahe-Fehler ausweist, sind vorhanden und gehoben.
2. **Nichts Lebendes ist übersehen — auch nicht außerhalb der Markdown-Welt.** Gesucht wurde zusätzlich nach `<tag>`-Platzhaltern, nach `ai-harness-course`-URLs jeder Form, und in `.github/workflows/**`, `Dockerfile`, `Makefile`, `*.mk`, `.golangci.yml`, `.semgrepignore`, `.claude/**` (inkl. `commands/`, `hooks/`), `tools/harness/*.sh`, `internal/**/*.go`, `README.md`, `docs/user/**`, `spec/**`. Treffer außerhalb des gehobenen Bestands: **keine**. `tools/harness/fetch-baseline-cache.sh` verdrahtet keinen Tag, sondern liest ihn aus `**Stand:**` in `harness/conventions.md` (Z. 54-58) — der Bump wirkt dort automatisch; `.github/workflows/upstream-drift.yml` nennt nur Target-Namen. Die `<tag>`-Stellen in `AGENTS.md:29/30/53/54`, `MR-021:6/23`, `MR-023:31` und dem Skript sind versionsagnostisch und zu Recht unberührt.
3. **Klasse 2 (Release-/Tree-URLs): fünf lebende, alle gehoben, Text und Ziel decken sich.** `AGENTS.md:36`, `harness/README.md:60`, `harness/conventions.md:27/41/46`. Bei den beiden `conventions.md`-Stellen mit Versions-Linktext stimmen Text und Ziel überein (`v5.12.0` ↔ `releases/tag/v5.12.0`, `ai-harness-course@v5.12.0` ↔ `tree/v5.12.0`). Die Zeile `harness/README.md:60`, an der der Vorgänger-Review sein F-1 hatte (Link-Ziel neu, Quell-URL alt), trägt diesmal **beides** auf `v5.12.0` — die dreifach eingetretene Klasse ist hier nicht wieder eingetreten.
4. **Keine Über-Hebung: jede der 48 gehobenen Stellen spricht über die Gegenwart.** Einzeln geprüft: die neun `Ersetzt-Baseline-Regel`-/`Ergänzt-Baseline-Regel`-Links der aktiven `MR-`Dateien, die zwei zusätzlichen Rumpf-Zeiger in `MR-031`/`MR-033`/`MR-035`, die fünf Kanon-Zeiger in `AGENTS.md`, die vier in `harness/README.md`, die vier in `harness/conventions.md`, die Ziel-Form-Zeiger in `spec/architecture.md:7` und `spec/spezifikation.md:9`, der Steering-Loop-Zeiger `docs/plan/planning/README.md:17`, die Bezug-Felder der zwei lebenden `open/`-Slices, der Format-Zeiger `roadmap.md:4` und die fünf relativen Zeiger im Reviewer-Skill. **Zu Recht stehengelassen:** die sieben `**Ausgelöst durch Baseline-Stand:** v5.11.0`-Felder der aktiven `MR-031`–`MR-036`, `MR-031:38` („Kanon **seit** `v5.11.0`"), `AGENTS.md:226/260-262/372` („seit dem v5.9.0-Bump", „v5.6.0-Bump", „Baseline v5.5.0"), `roadmap.md:88` (Graphknoten der geschlossenen welle-83), `roadmap.md:141` (Drift-Log-Zeile der welle-84-Eröffnung), `.d-check.yml:244` (Historie der Lastenheft-Verweis-Spalte), `slice-148:24/47` (der Plan beschreibt seinen eigenen Vorher-Zustand) und `welle-85:18` („Der Baseline-Pin steht auf `v5.11.0` (Kurs-Welle 94)") — letzteres präzedenzkonform: `done/welle-83-…:16-21` trägt dieselbe Form auf `v5.9.0` und wurde von der damaligen Hebung ebenfalls nicht angefasst.
5. **Delta selbst nachgemessen — 52 Dateien, identische Dateiliste, alle Umfangs-Angaben exakt.** Die 22 Zwei-Zeilen-Deltas sind **mechanisch** geprüft: jedes ist eine einzelne `<!-- Quelle: … blob/v5.11.0/… -->`-Stempelzeile bzw. (bei `templates/harness/conventions.template.md`) eine `releases/download/`-URL; kein einziges trägt etwas anderes — die Behauptung hält wörtlich. `regelwerk/README.md` ist +3/−3 und besteht aus zwei `blob/`-Stempeln plus der `**Stand:**`-Zeile (`Kurs-Welle 94 · 2026-08-23` → `Kurs-Welle 98 · 2026-08-26`); die Ausweisung „trägt zusätzlich nur seine `**Stand:**`-Zeile" ist damit richtig, und der Vorgänger-Fehler (README als Regel-Inhalt gezählt) ist nicht wiederholt. Die Umfangs-Tabelle stimmt Zeile für Zeile: `modul-05` +11, `modul-13` +8, `grundlagen-source-precedence` +6, `modul-03-spec` 5 für 2, `templates/AGENTS.template.md` 3 für 2 — jeweils nach Abzug der Stempelzeile. Ohne Befund außer der Nenner-Frage in F-5.
6. **Die fünf Inhalts-Dateien decken sich exakt mit der Lieferzusage.** `docs/plan/cr/2026-08-26-antwort-regelwerk-v5110.md:121-125` sagt: „Im gelieferten Umfang ändern sich **vier Regelwerk-Dateien** und bei Punkt 2 zusätzlich `AGENTS.template.md`. Kein Gate, kein Werkzeug, keine Vorlagen-Änderung." Gemessen: vier Regelwerk-Dateien (`modul-05`, `modul-13`, `modul-03`, `grundlagen-source-precedence`) plus `templates/AGENTS.template.md`; die einzige weitere Template-Änderung ist ein Versions-Stempel. Auch die Zuordnung zu den CR-Punkten hält am vendorten Text: `modul-05` trägt die urteilsfreie Hälfte samt *„Welches Werkzeug … ist Repo-Entscheidung"*, `modul-13` die Positivkontrolle samt Verweis auf `modul-11 §Fitness Function ohne Standard-Tool`, `grundlagen-source-precedence` die Reichweitenfrage als Frage, `modul-03`/`AGENTS.template` das klärende Wort **Code**-Modul-Pfad samt *„Die Erlaubnis ist keine Pflicht"*.
7. **Die vierte Klasse: „genau eine lebende Instanz" ist unabhängig bestätigt.** Eigene Probe ohne den 50-Zeichen-Schwellwert und ohne Beschränkung auf die fünf geänderten Dateien: alle `„…"`-Spannen ≥15 Zeichen über **alle** getrackten `.md`/`.yml`/`.sh`/`.go`/`.json`-Dateien, Whitespace- und Auszeichnungs-normalisiert, gegen den alten und den neuen Baum. Ergebnis: **neun** Zitate stehen im alten Baum und nicht mehr im neuen; **zwei davon in einer lebenden Datei**, beide in `harness/conventions/MR-033-sicht-ohne-modul-pfade.md:5-6` und `:8-9`, deren Ziel-Links im selben Commit auf `v5.12.0` gehoben wurden. Die übrigen sieben liegen in `docs/plan/cr/**`, `docs/plan/planning/done/**` und `docs/reviews/**`. `MR-033` ist damit betroffen, und es ist die einzige lebende Instanz — die Zählung der Dokumente insgesamt steht in F-1.
8. **Die `refs`-Seite des neuen Ventils ist tatsächlich eng.** Sonde P2: ein Link aus `docs/reviews/` auf eine nicht existierende Datei **unter dem gepinnten** Baum (`v5.12.0/regelwerk/gibt-es-nicht.md`) liefert Exit **2**, `target-missing`. Sonde P3: dieselbe `v5.11.0`-Referenz außerhalb der beiden Globs liefert Exit **2**. Der Kommentar-Satz „ausgenommen ist ausschliesslich der ENTFERNTE Baum, nicht der gepinnte" hält; die Beanstandung in F-6 betrifft ausschließlich die Quell-Achse.
9. **„ZWEI Eintraege statt vierzehn" ist gemessen richtig.** Genau **14** Dateien tragen einen Markdown-**Link** ins entfernte `v5.11.0`-Verzeichnis (zwölf unter `docs/plan/planning/done/`, zwei unter `docs/reviews/`); eine datei-genaue Liste hätte diese vierzehn Einträge gebraucht. Die übrigen Nennungen des entfernten Pfads stehen in Inline-Code, und `.harness/…` ohne führendes `./`/`../` wird von `classifyCodepath` gar nicht als Pfad geführt — die relativen Formen (`../../.harness/…`) hingegen schon und sind vom selben Ventil gedeckt.
10. **Alt-Baum entfernt, kein hängender Verweis.** `.harness/baseline/` enthält ausschließlich `v5.12.0/`; `--verify` bestätigt 51 Dateien plus Manifest. Kein lebendes Artefakt verweist mehr auf `v5.11.0`-Pfade (einzige Ausnahme: die beiden Ventil-Einträge selbst).
11. **`MR-013`-Link-Tiefen der bewegten Datei sind vollständig.** Alle zehn relativen Ziele in `harness/conventions/done/MR-030-baseline-v5110.md` lösen vom neuen Ort auf: `../../conventions.md` (sechsmal, mit Ankern), `../../../AGENTS.md`, `../../README.md`, `../../../.harness/skills/reviewer.md`, `MR-029-baseline-v590.md` (dreimal, `done/`-Präfix korrekt entfernt). Einzeln per Existenzprobe aus dem Zielverzeichnis geprüft, kein Rest auf altem Tiefenstand.
12. **Beide Index-Tabellen sind sachlich nachgezogen — und die Zeilenposition stimmt diesmal.** Die aktive Zeile ist ersetzt (`MR-030` raus, `MR-037` rein, Geltungsbereich auf `.harness/baseline/v5.12.0/` gehoben); die aufgelöste Zeile für `MR-030` steht **am Ende** der Tabelle und hält die aufsteigende Ordnung (`… 027 028 029 030`) — das F-6 des Vorgänger-Reviews (`register-tabelle-ausser-reihe`) ist nicht wiederholt. Beide `<a id>`-Anker je Zeile (Voll-Slug und Kurzform) sind gesetzt, `#mr-030` und `#mr-037` lösen auf, und `conventions.md:29` zeigt mit dem Voll-Slug auf die neue Zeile. `§Baseline` nennt `v5.12.0` als Stand und `MR-037` als geltende Pin-Adaption, `§Adoptierte Konventions-Quellen` beide Kurs-URLs und den vendorten Pfad — konsistent.
13. **Der Auflösungs-Trigger von `MR-030` ist wirklich eingetreten.** Er lautet: „die **nächste Pin-Hebung** ersetzt diesen Eintrag durch ihren Nachfolger — wie `MR-029` durch diesen Eintrag ersetzt wurde", und daneben: „`MR-023` bleibt **aktiv** stehen: es trägt das Bundle-Layout, nicht den Pin-Wert." Beides ist eingehalten: `MR-037` ist die nächste Pin-Hebung, `MR-023` steht weiter in §Aktive Adaptionen. Die Titelzeile „siebter Nachtrag zu MR-011" ist gegen die Serie (`MR-012`, `MR-016`, `MR-026`, `MR-028`, `MR-029`, `MR-030`) korrekt.
14. **Kanon-Zuschreibungen gegen den vendorten Baum (BEO-012-Klasse) — alle bestätigt.** „Kurs-Tag vom 2026-08-26" ↔ `regelwerk/README.md:3` *„Kurs-Welle 98 · 2026-08-26"*. „Vier Kurs-Wellen" ↔ 94 → 98 im **alten** und **neuen** Manifest-Baum, also aus den Bäumen selbst ableitbar und nicht nur aus der Kurs-Antwort. „die von `MR-011` vorgesehene Fortschreibung" ↔ `done/MR-011:…` §Auflösungs-Trigger *„der konkrete Tag wird bei Adoption einer neueren Baseline-Version per Nachtrags-MR hochgezogen"*. „innerhalb des von `MR-023` festgelegten self-contained Bundle-Layouts" ↔ `MR-023:29-31` (beide Bäume aus `lab-regelwerk.zip`, Pfadschema `.harness/baseline/<tag>/{regelwerk,templates}/`). Der Config-Kommentar-Satz „was dieses Repo an Listen beanstandet" ↔ `MR-021:6-11` („Eine Aufzählung einzelner Dateien stünde hier falsch: sie wächst mit jedem Eintrag"). Die Botschaft „BEO-008 nennt genau diese Falle: ein Pfad-`grep` kennt keine Zeitform" ↔ `observations.md:19` wörtlich. Auch die zwei gehobenen **nackten** Fassungs-Angaben halten am neuen Text: `roadmap.md:13` und `.d-check.yml:377` behaupten für `v5.12.0` die Zwei-Aussagen-Form von *§Roadmap-Struktur*, und `.harness/baseline/v5.12.0/regelwerk/modul-06-roadmap.md:60` trägt sie wörtlich („Der Abschnitt trägt **zwei unabhängige Aussagen**"). Keine überdehnte Zuschreibung gefunden.
15. **Scope-Treue gegen §3 des Slice-Plans.** Außerhalb des vendorten Baums enthält der Diff keine Zeile, die eine **Regel** des neuen Stands anwendet: jede geänderte Zeile ist entweder eine `v5.11.0`→`v5.12.0`-Substitution, Lifecycle-Buchführung (die zwei Index-Zeilen, die `MR-030`-Tiefen), der neue Eintrag `MR-037` oder das Ventil. Insbesondere ist die Punkt-2-Klärung (*„Die Erlaubnis ist keine Pflicht"*), die `MR-033` unmittelbar berührt, **nicht** angewandt — der Slice sagt das ausdrücklich („dieser Slice misst und benennt"), und die welle-85 §3-Trigger 2/3 tragen die Folgearbeit.
16. **Die von `MR-011` mitverlangte Re-Evaluierung ist deklariert verschoben, nicht vergessen.** `MR-011` §Auflösungs-Trigger nennt neben der Tag-Hebung „dann auch **Re-Evaluierung** der an die Kurs-Quelle gekoppelten Trigger von `MR-006` und `MR-008`". Slice-Plan §3 („Kein Freshness-Audit der Adaptionen. Ebenfalls Etappe B.") und welle-85 §3 Trigger 2 („Jede aktive Adaption hat eine Antwort auf die Freshness-Frage … je Eintrag ein Ergebnis, auch ‚unberührt', und keiner ungefragt") fangen sie am Wellen-Rand auf. Bewusste, benannte Verschiebung — ohne Befund.
17. **Der neue Config-Kommentar bleibt in den fünf zulässigen Klassen.** Herkunft steht als **ein** auflösbares Feld (`MR-037`), der Rest ist Abgrenzung und Grenze (was ausgenommen ist, was nicht). Die Form entspricht den drei älteren Tombstone-Kommentaren derselben Datei, die mehrere Reviews passiert haben; keine Review-Historie, kein Deliberations-Protokoll über Verworfenes im engeren Sinn. Ohne Befund.
18. **`Ersetzt-Baseline-Regel: — (keine)` ist keine neue Abweichung.** Die Vorlage nennt einen Eintrag ohne benannte Baseline-Regel einen „Fork"; `MR-037` setzt hier dasselbe Muster wie `MR-023`, `MR-026`, `MR-028`, `MR-029` und `MR-030` (Pin-Fortschreibung im Layout des Vorgängers). Bestandsform der Serie, vom Vorgänger-Review nicht beanstandet — hier nur benannt, nicht gemeldet.
19. **Go-Testfixtures sind keine Verweise.** `internal/hexagon/core/rules/versions_test.go` und `internal/adapter/driving/cli/cli_versions_patterns_test.go` enthalten `baseline/v5.6.0`/`v5.7.0`/`v5.9.0` als synthetische Fixture-Strings in temporären Verzeichnissen; sie zeigen auf nichts im Repo und sind zu Recht unberührt. Ebenso `.claude/settings.local.json` (historische Permission-Einträge mit alten Kurs-URLs).
20. **Nicht-Träger korrekt ausgelassen.** `.harness/skills/closure-note-reviewer.md:4` nennt `modul-11-verification.md` als **Klartext** ohne Link und ist daher nicht pin-gebunden; `harness/conventions/MR-034-matrix-scope-welle.md:4` trägt „Ersetzt-Baseline-Regel: keine" und verweist nur repo-intern. Beide Dateien sind zu Recht nicht im Diff.
21. **Gate-Belege der Botschaft exakt reproduziert.** „`make gates` Exit 0 (zehn Glieder)" ✓ (Abschlusszeile nennt genau zehn), „`make fullbuild` Exit 0" ✓, „`fetch-baseline-cache --verify` ok (51 Dateien, vollständig)" ✓ wortgleich. `make fullbuild` schließt mit Image-Hash und 48 Anforderungen / 0 Waisen.

---

## Kategorie-Summary

| Kategorie | Anzahl | Findings |
| --- | --- | --- |
| HIGH | 0 | — |
| MEDIUM | 2 | F-1, F-2 |
| LOW | 5 | F-3, F-4, F-5, F-6, F-7 |
| INFO | 0 | — |

## Verdikt

**Blockierend.** Kein HIGH; zwei MEDIUM.

Die **Sacharbeit** dieser Etappe ist die sauberste der Serie. Zum ersten Mal sind alle drei Spiegel-Klassen aus `BEO-008` vollständig gehoben und in **beide** Zeitform-Richtungen korrekt: 42 lebende Pfad-Verweise über vier verschiedene relative Tiefen, fünf Release-/Tree-URLs mit deckungsgleichem Text und Ziel, und keine einzige mitgehobene Vergangenheits-Aussage. Die zwei Wiederholungsfälle des Vorgänger-Reviews (`harness/README.md:60`, die Zeilenposition in §Aufgelöste Adaptionen) sind nicht wieder eingetreten. Die Delta-Messung ist bis auf einen Abgrenzungsfehler exakt reproduzierbar, das Ventil ist auf der `refs`-Seite belegbar eng, und die Entdeckung der vierten Spiegel-Klasse ist ein echter Zugewinn — die eine lebende Instanz (`MR-033`) ist unabhängig bestätigt.

Blockierend sind zwei Befunde, die beide dieselbe Stelle betreffen wie beim letzten Mal: den **Beleg** und die **Form des neuen Eintrags**. F-1 ist die Klasse, für die dieser Slice den Namen erfunden hat, angewandt auf ihn selbst — der Zensus der vierten Klasse nennt zwei Dokumente, ein drittes trifft dasselbe Raster mit demselben Schwellwert, und die Zeilen-Zahl daneben lässt sich am Diff nicht nachrechnen. F-2 ist ein Rückfall hinter einen ausdrücklich zertifizierten Stand: `Löst auf` und `Ausgelöst durch Baseline-Stand` waren die Auflage aus dem `MR-029`-Review, sie standen in `MR-029` und `MR-030` als Paar, und der Vorgänger-Review hat genau das als Negativbefund 13 festgehalten. `MR-037` trägt nur noch die Hälfte des Paars — zusammen mit F-3 und F-4 ist der neue Eintrag der formal schwächste der Pin-Serie, und er ist derjenige, den ab jetzt jeder Agentenlauf liest.

Die fünf LOW reisen in dauerhaften Aufzeichnungen mit: zwei Zahlen, die die nächste Hebung als Vergleichspunkt übernehmen würde (F-5, F-7), eine Ventil-Ausweitung, die genau die Dokumentklasse blind stellt, in der dieser Report entsteht (F-6), und ein Zeiger, der beim nächsten Lifecycle-Move bricht (F-3).
