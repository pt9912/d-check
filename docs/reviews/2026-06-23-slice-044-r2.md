# Review R2 — slice-044 (Slice-Doc gegen Implementierung) — Doku-Drift-Befund

- **Datum:** 2026-06-23
- **Rolle:** unabhängig, adversarial. Kein DoD-Abhaken, keine Gate-Bestätigung (getrennter Kontext).
- **Gegenstand:** das Slice-Planungsdokument `docs/plan/planning/in-progress/slice-044-print-mk-trace-targets.md`, geprüft gegen die Implementierung, die es beschreibt (Working Tree gegen `bf9411a`).
- **Bezug:** Folgelauf zu R1 (`docs/reviews/2026-06-23-slice-044-r1.md`, ACCEPT). R1 wird nicht überschrieben (Reviewer-Skill: ein Report pro Lauf).

## Verdikt

**ACCEPT** (Slice-Doc ist sauber) — mit 1 echten Doku-Drift-Finding (F-A) und einer Beobachtung zur R1-Aktualität.

Das Slice-Dokument ist akkurat und MR-014-konform (§1 Ziel · §2 Entscheidungen · §3 DoD · §4 Risiken · §5 Trigger · §6 Sub-Area · §7 Closure-Platzhalter). Jede Aussage in §2 deckt sich mit dem Diff (comboError-Regel; runTrace-Strict-Exit mit Zähl-Zeile auf stderr / RTM rein auf stdout; mkTemplate-Erweiterung; Lastenheft 0.24.0; trace.go unverändert — `matrix.Orphans` existierte bereits, trace.go:75). Die DoD-Testliste mappt 1:1 auf die fünf neuen TestCLI044_*. Referenz-Anker (inkl. des neuen `dc-fa-cli-011--...`) lösen auf. MR-006 gewahrt (neue Spec-Abschnitte verlinken nur same-stratum).

## Beobachtung — R1 auf Platte ist gegenüber dem Working Tree veraltet

Beide LOW-Findings in R1 sind durch den aktuellen Working Tree widerlegt (R1 gegen einen früheren Stand geschrieben, danach nachgebessert):

- R1 F-1 (§4.12 erwähnt `--require-complete` nicht) ist erledigt: `benutzerhandbuch.md:545-552` trägt den Hinweis jetzt.
- R1 F-2 (§4.12-Beispiel bleibt auf v0.22.0) war bei R2-Lauf faktisch falsch: `benutzerhandbuch.md:527` stand zum R2-Zeitpunkt auf v0.24.0.

R1s ACCEPT bleibt gültig; aber die R1-F-2-„Behebung" (Pin-Bump auf v0.24.0) erzeugte F-A.

## Findings

### F-A (LOW→MEDIUM) — Handbuch-Header-Versionsdrift: 1.5/v0.23.0 vs. neu gestempeltes 1.6/v0.24.0

- **Quelle:** Maintainability / Doku-Konsistenz.
- **Pfad:** `docs/user/benutzerhandbuch.md:3` (Header) vs. neue 1.6-Historie-Zeile + die v0.24.0-Pins in §4.12/§4.13.
- **Befund:** Der Slice stempelte v0.24.0 nur halb durchs Handbuch. Es koexistierten drei Versionsstände: Header `1.5 · v0.23.0`; neu editierte §4.12/§4.13 + 1.6-Historie `v0.24.0`; ~20 übrige Beispiele §2–§4.11 `v0.22.0` (pre-existing). Die Historie behauptet 1.6/v0.24.0, während der Header 1.5/v0.23.0 nennt — widersprüchliche Selbstaussage.
- **Verifizierbar:** ja — Sichtprüfung des Headers gegen die Historie-Zeile.

## Negativbefunde (geprüft, ohne Befund)

- **§2 Entscheidungen ↔ Diff:** geprüft, deckungsgleich (comboError, runTrace, mkTemplate, trace.go unberührt).
- **§3 DoD ↔ Tests:** geprüft, 1:1-Mapping auf TestCLI044_*.
- **Referenz-Anker / MR-006:** geprüft, lösen auf; keine Abwärts-Links in den Spec-Straten.
- **MR-014-Struktur:** geprüft, Haus-Stil eingehalten.

## Kategorie-Summary

HIGH 0 · MEDIUM 0 · LOW 1 (F-A, Grenzfall LOW→MEDIUM) · INFO 0.

## Auflösung (nach R2)

F-A behoben — **Release-Prep-gebunden** (Auftraggeber-Entscheid 2026-06-23, Präzedenz `7716be3` „CHANGELOG-Stempel + Handbuch 1.5" als eigener Release-Prep-Commit):

- 1.6-Historie-Zeile zurückgenommen; Handbuch-Header bleibt `1.5 · v0.23.0`.
- §4.13-Fragment-Beispiel auf den Platzhalter `v<version>` (Lastenheft-/Spezifikations-Konvention) — versionsneutral, kein v0.24.0-Forward-Claim; die neuen Inhalte `doc-trace`/`doc-complete`/`TRACE_FLAGS` bleiben.
- §4.12-`docker run`-Pin zurück auf `v0.22.0` (Stand der Geschwister-Beispiele); der `--require-complete`-Hinweis (Inhalt) bleibt.
- `CHANGELOG.md`-Überschrift `## [0.24.0] — 2026-06-23` → `## [Unreleased]`; der v0.24.0-Stempel (Header + Historie + Pins + CHANGELOG-Datum) erfolgt im separaten Release-Prep-Commit.
- Lastenheft-Version `0.24.0` bleibt (Spec-Vertragsversion, entkoppelt vom Software-Release).
