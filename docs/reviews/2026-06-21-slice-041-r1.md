# Review R1 — slice-041 ADR-Immutable-Gate

**Datum:** 2026-06-21 · **Slice:** slice-041 · **Reviewer:** unabhängiger
Subagent (adversarial, read-only) · **Commit zum Zeitpunkt:** `1592fb3`.

**Geprüft:** `tools/adr-immutable-check.sh` (alle Modi + beide Quadranten),
[ADR-0016](../plan/adr/0016-adr-immutable-gate.md), der Slice-Plan, `Makefile`,
`.githooks/pre-commit`, `.github/workflows/ci.yml`, `AGENTS.md` §4,
`harness/README.md` §Sensors/§Traceability — gegen `tools/trace-check.sh`
(Muster) und `tools/planning-consistency.sh` (Schwester-Gate, Heading-Guard-Lehre).
Reale + gebaute git-Fixtures; Repo unverändert hinterlassen.

## Korrekt bestätigt (verifiziert)

- Selbsttest grün; `make gate-consistency`/`make planning-check`/`make
  adr-check` grün.
- **Gefangene False-Negative-Proben:** Körper in Geschichte verschoben,
  no-Geschichte-Körper-Edit, Tippfehler-Geschichte-Überschrift,
  Status-Smuggling, Edit im Abschnitt **nach** Geschichte, Doppel-`##
  Geschichte`, Typechange (T)→symlink, Löschen/Umbenennen einer Accepted-ADR.
  Die Geschichte-Grenze ist **fail-closed** (nicht-kanonische Überschrift →
  legitimer Anhang feuert, nie silent-green — wie slice-040).
- **Korrekt grün gebliebene False-Positive-Proben:** Geschichte-Anhang,
  Accepted→Superseded, Teil-Supersede-Annotation, Proposed-BASE-Edit, neue
  Accepted-ADR (A), Proposed→Accepted-Reifung.
- Modi/Wiring: fail-closed bei Zero-SHA/Müll/leerer Range (Exit 2); `fail`
  überlebt die Prozess-Substitution; `R100\talt\tneu`-Parsing korrekt;
  `adr-check`+`hooks` in `.PHONY`; `adr-check` **nicht** in `gates`/`ci`;
  CI teilt die identische RANGE-Variable mit `trace-check`. DoD-Akzeptanz an
  echter Accepted-ADR (ADR-0013) im Wegwerf-Worktree bewiesen (Hook blockte;
  `--range` fing es per `--no-verify`). Doku-Sync konsistent; „Schärft: keine
  Spec-Stelle" wie ADR-0013.

## Befunde

- **MEDIUM — `core()` strippte jede `^**Status:**`-Zeile dateiweit** (latenter
  False-Negative). Eine Körper-Zeile, die mit `**Status:**` beginnt, fiel aus
  dem core → Edit daran rutschte grün durch. Im heutigen Bestand nicht aktiv
  (jede ADR hat genau eine Status-Zeile, Zeile 3), aber latent.
- **LOW — Selbsttest deckte den core-resume-Pfad (`## ` nach Geschichte) und
  die D/R-Branches nicht ab.**
- **LOW — `--range` ohne `..`** prüft einen leeren Diff (still grün); nur über
  Fehlaufruf erreichbar (CI liefert stets `BASE..HEAD`).
- **NIT — Supersede-Stelligkeit:** Gate verlangt `ADR-[0-9]{4}`, Index/AGENTS-
  Prosa schrieb `ADR-NN` (2-stellig illustrativ).
- **NIT — Erfolgsmeldung** zählt Diff-Einträge inkl. grandfathered A/D.

## Verdikt

**MERGE-FÄHIG** — keine *aktive* False-Negative/Positive im heutigen
ADR-Bestand; Wiring, fail-closed-Verhalten und DoD-Akzeptanz erfüllt. MEDIUM +
LOWs als Härtung vor Langzeit-Verlass empfohlen.

## Disposition (durch den Umsetzer, `4ce7b70`)

- **MEDIUM behoben:** `core()` strafft die `**Status:**`-Zeile nur im
  Kopf-Block (vor der ersten `## `-H2); `classify` nutzt die **erste**
  Status-Zeile für BASE-Accepted- und HEAD-OK-Prüfung. Adversarial bestätigt:
  Körper-`**Status:**`-Edit wird jetzt gefangen.
- **LOW behoben:** Selbsttest-Fälle 6 (Edit nach `## Geschichte` →
  core-resume) + 7 (Körper-`**Status:**` → MEDIUM-Regress); `--range` ohne
  `..` → Exit 2.
- **NIT behoben:** ADR-Index + `AGENTS.md` §3.5 auf vierstellig `ADR-NNNN`
  angeglichen.
- **NIT (Meldung):** belassen (kosmetisch).
- **Kein R2** (Nutzer-Entscheid): Fix klein, selbsttest-gesichert,
  adversarial belegt → direkt Closure.
