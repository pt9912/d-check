# Review R2 — slice-045 (`diagrams` — Nachbesserungs-Verifikation der R1-Findings, doc-first)

## Kopf-Metadaten

- **Gegenstand:** uncommittete Working-Tree-Änderungen slice-045
  (welle-34-diagram-ids) **nach** R1-Nachbesserung — doc-first-Fundament des
  geplanten Moduls `diagrams`. **Kein Code** — beabsichtigt (Greenfield „Doc
  führt, Code folgt"); „kein Code/keine Tests" ist hier kein Mangel.
- **Reviewer:** unabhängig (nicht Autor), adversarial, **R2** (Re-Review).
- **Datum:** 2026-06-23.
- **Verifikations-Auftrag:** sind die fünf R1-Findings
  ([`2026-06-23-slice-045-r1.md`](2026-06-23-slice-045-r1.md): F-1 MEDIUM,
  F-2/F-3 LOW, F-4/F-5 INFO) **real aufgelöst** und hat die Nachbesserung
  **nichts Neues gebrochen**?
- **Diff-Range:** Working Tree gegen `HEAD` (`bf9411a`); 2 geänderte
  (`spec/lastenheft.md`, `docs/plan/planning/in-progress/roadmap.md`) +
  1 neue Datei (`docs/plan/planning/in-progress/slice-045-diagram-ids.md`).
- **Quergeprüft (real gelesen):** Trigger `/Development/KI/belief-agent/spec/architecture.md`
  (R1 nannte den Pfad `belief-agent/...`; reale Lage `KI/belief-agent/...` — der
  *Inhalt* deckt sich), bestehende `ids`-Definitionsauflösung
  `spec/spezifikation.md:460-475` (`DC-FA-ID-001.a`), DIAG-Anker-Ziele,
  Roadmap-Invariante.
- **Nicht meine Rolle:** DoD-Abhaken, Gate-Lauf-Bestätigung (Verifikation,
  getrennter Kontext).

## Findings

### F-1 (R1 MEDIUM) — Definition von „definiert" — **AUFGELÖST**

- **kategorie:** — (verifiziert geschlossen)
- **quelle:** `DC-FA-DIAG-001` (Anforderungs-Anlege-Prozess: prüfbare AKs)
- **pfad:** `spec/lastenheft.md:792-796`
- **befund:** Die Lücke ist **real geschlossen**. Der Vertrag definiert
  „definiert" jetzt operational: „die Kennung kommt in der `defined-in`-Datei
  als eigenständiges Kennungs-Token **außerhalb von Fences** vor — als Heading
  **oder** in Fließtext/Tabelle; bewusst **nicht** heading-zentriert (anders als
  das Modul `ids`), damit auch **tabellen-definierte** Komponenten-Kennungen
  (z. B. Architektur-IDs) erfasst werden". Gegen den genannten Anlass geprüft:
  die `ARC-01..ARC-09` der Trigger-Datei stehen ausschließlich in einer
  **Markdown-Tabelle** (`/Development/KI/belief-agent/spec/architecture.md:65-73`),
  ohne `### ARC-0x`-Heading und außerhalb jedes Fence — sie fallen unter die neue
  Klausel „in Fließtext/Tabelle". Der Happy-Path-AK (`:815`, „alle Kennungen
  kommen in ihrer `defined-in`-Quelle **vor**") ist damit **konsistent** zur
  Beschreibung (beide sprechen von Vorkommen, nicht von Heading-Match) und gegen
  die reale Trigger-Quelle **erfüllbar** — die in R1 diagnostizierte
  Interpretierbarkeit (heading-zentriert ⇒ Happy-Path scheitert am Anlass) ist
  beseitigt.
- **verifizierbar:** ja — ein Fixture aus der ARC-Tabelle + Mermaid-Flowchart
  liefert unter der neuen Definition deterministisch „0 Befunde" (Exit 0); die
  vormalige Mehrdeutigkeit (Heading vs. Tabelle) hat keine zwei zulässigen
  Lesarten mehr.

### F-2 (R1 LOW) — Glossar „Regelmodul" — **AUFGELÖST**

- **kategorie:** — (verifiziert geschlossen)
- **quelle:** Maintainability (Doku-Drift innerhalb des CR-Artefakts)
- **pfad:** `spec/lastenheft.md:931`
- **befund:** Der Glossar-Eintrag „Regelmodul" trägt jetzt `diagrams` als
  achtes/neuntes Modul und ist deckungsgleich zur ergänzten `DC-FA-CLI-002`
  (`:76`) und zur Schema-Konvention (`:46`, `DIAG`). Die in R1 monierte Drift
  (Glossar bei 8er-Liste, CR ergänzt nur CLI-002) ist geschlossen.
- **verifizierbar:** ja — Lese-Vergleich der Modul-Liste §6 ↔ `DC-FA-CLI-002`
  ↔ Schema-Konvention: alle drei führen `…, hostpaths, diagrams`.

### F-3 (R1 LOW) — Allquantor „einzige Ausnahme" — **AUFGELÖST**

- **kategorie:** — (verifiziert geschlossen)
- **quelle:** `DC-FA-DIAG-001` (Behauptungs-Präzision)
- **pfad:** `spec/lastenheft.md:802-804`
- **befund:** Die R1-Fassung trug die selbsttragende, drift-anfällige
  Allquantor-Behauptung „das Modul ist die **einzige Ausnahme** von der
  Fence-Opazität". Sie ist umformuliert zu „Das Modul **öffnet als bislang
  einziges** gezielt benannte Diagramm-Fences; alle übrigen Module behandeln
  Fence-Inhalt als opak (vgl. die Vorverarbeitung des Moduls `links`)". Das
  „bislang" entschärft den Allquantor (Zustandsaussage statt zeitloses
  Invariant), und der explizite Verweis auf die `links`-Vorverarbeitung bindet
  die Aussage an ihre Quelle, ohne neue Unschärfe einzuführen.
- **verifizierbar:** ja — Lese-Abgleich gegen `spec/spezifikation.md`-
  Vorverarbeitung; die abgeschwächte Formulierung wird nicht still falsch, wenn
  künftig ein weiteres Modul Fences öffnet.

### F-4 (R1 INFO) — `spans`-Interaktion — **AUFGELÖST (in Slice §4 benannt)**

- **kategorie:** — (verifiziert geschlossen)
- **quelle:** `DC-FA-SPAN-001` / `DC-FA-DIAG-001` (Modul-Interaktion)
- **pfad:** `docs/plan/planning/in-progress/slice-045-diagram-ids.md:92-95`
- **befund:** Die Interaktion ist jetzt explizit benannt: Risiko-Punkt
  „**`spans`-Interaktion (R1-F-4)**. Das aktive Modul `spans` meldet
  ungeschlossene Code-Spans. Geöffnete Diagramm-Fences dürfen dort keine
  False-Positives erzeugen — die ADR/`.a` legt fest, dass `diagrams` den
  Fence-**Rohtext** liest, ohne den Fence-Zustand für andere Module zu ändern."
  Die Isolations-Zusicherung ist damit als offene ADR-/`.a`-Auflage verortet,
  wie es R1 verlangte (INFO, nicht-blockierend).
- **verifizierbar:** ja — ein Fixture mit aktivem `spans`+`diagrams` und einem
  Backtick-haltigen Mermaid-Label zeigt im Code, ob `spans` byte-identisch
  bleibt; heute (kein Code) ist die Verortung im Plan die maximale Auflösung.

### F-5 (R1 INFO) — Trigger-Faktenlage — **AUFGELÖST (Trigger-Fakt korrekt)**

- **kategorie:** — (verifiziert geschlossen)
- **quelle:** Maintainability (Trigger-Genauigkeit, MR-014 §5)
- **pfad:** `docs/plan/planning/in-progress/slice-045-diagram-ids.md:110-112`
- **befund:** Die in R1 monierte Sammel-Formulierung („`ARC-01…ARC-09` +
  `LH-FA-POL-004` im Fence", die nahelegte, beide Familien lägen in **einem**
  Block) ist präzisiert: „zwei nicht-triviale `mermaid`-Diagramme — `ARC-01…ARC-09`
  im **Flowchart**, `LH-FA-POL-004` im **Sequenzdiagramm**". Quergeprüft gegen
  die reale Trigger-Datei: `ARC-01..ARC-09` in der ersten (Flowchart-)Fence
  (`…/architecture.md:20-53`), `LH-FA-POL-004` in der zweiten (Sequenz-)Fence
  (`:115`). Die Zuordnung stimmt jetzt blockgenau.
- **verifizierbar:** ja — `grep` über die Trigger-Datei (oben durchgeführt:
  Flowchart-Fence trägt nur `ARC-NN`; `LH-FA-POL-004` erscheint im
  `opt`-Zweig der `sequenceDiagram`-Fence).

## Negativbefunde (Regressions-Check — geprüft, ohne Befund)

- **Restmehrdeutigkeit F-1 (scharf nachgeprüft):** „eigenständiges
  Kennungs-Token" grenzt gegen Substring-Treffer ab (z. B. matcht ein Muster
  für `ARC-01` nicht versehentlich ein hypothetisches `ARC-011`); „außerhalb
  von Fences in der `defined-in`-Datei" ist die schon gelebte Opazitäts-
  Invariante (`DC-FA-LINK-001.a`, `spec/spezifikation.md:369`) — keine neue
  Mehrdeutigkeit, sondern Re-Use der bestehenden Vorverarbeitungs-Semantik. Die
  konkrete Token-Grenz-Mechanik (Wortgrenze/Regex-Anker) ist berechtigt auf die
  noch offene `.a`-Sektion deferiert (Slice DoD `:67`, `:73-75`) — Vertragsebene
  ist eindeutig genug für einen Implementations-Agenten. Kein Befund.
- **AK-Konsistenz (Happy/Boundary/Negative):** Happy (`:815`, alle Kennungen
  kommen vor ⇒ 0/Exit 0) deckt sich mit der neuen „Vorkommen"-Definition;
  Boundary (`:816`, genau ein `diagram-id-undefined`/Exit 1 + Gegentest:
  nicht-gelistete `bash`-Fence **und** Vorkommen außerhalb jedes Fence
  unberührt) und Negative (`:817`, byte-identisch `DC-QA-02` + nichts
  geschrieben `DC-QA-03`) sind unverändert sauber. Die Boundary-Klausel
  „außerhalb jedes Fence bleibt … unberührt" ist konsistent zum Prüf-Scope
  (Modul liest nur Fence-**Inneres** gelisteter Sprachen). Kein Regress.
- **Link-Disziplin / Anker (DIAG-Sektion):** alle sechs in der DIAG-Sektion
  verwendeten Markdown-Links (`DC-FA-CLI-002`, `DC-FA-ID-001` ×2, `DC-QA-02`,
  `DC-QA-03`, `DC-FA-LINK-001`) zielen auf existierende `### `-Headings im selben
  Lastenheft (verifiziert: Zeilen 72/537/902/907/450); keine toten Anker. Kein
  Befund.
- **MR-006 / Hard Rule §3.4 (Spec-Straten nie abwärts):** der neue
  `DC-FA-DIAG-001`-Text verlinkt ausschließlich seitwärts/aufwärts auf andere
  `DC-FA-*`/`DC-QA-*`; **kein** Abwärts-Verweis auf ADR/Slice/Welle im
  Lastenheft-Körper. Die `slice-045`-Provenance lebt korrekt in der
  §7-Historie-Spalte (`:945`) — legitime Provenance, kein Stratums-Bruch. Die
  Nachbesserung hat hier nichts eingeschleppt. Kein Befund.
- **Keine neuen bare IDs:** die `ARC-NN`/`LH-*`-Erwähnungen leben in der
  §7-Historie-Prosa (`Anlass: … ARC-NN/LH-*-Kennungen`, `:945`) bzw. im Slice
  (planning-Schicht, von `ids` nicht erzwungen für Kennungen ohne `defined-in`
  im d-check-Repo) — das CR führt keine nackte, linkpflichtige d-check-Kennung
  ohne Link ein. Kein Befund.
- **Roadmap-/planning-check-Invariante (Regress-Verdacht):** §Aktuelle Welle
  steht auf „welle-34-diagram-ids — in Arbeit" mit `slice-045` in `in-progress/`
  (Invariante in-progress-Slice ⟺ aktive Welle erfüllt); der Link
  `slice-045-diagram-ids.md` ist gleiches Verzeichnis und existiert; der
  Tieflink `DC-FA-DIAG-001` aus der Roadmap (planning-Schicht, Abwärtslink
  **erlaubt**, anders als im Spec-Stratum) zielt auf den existierenden Anker.
  „Zuletzt abgeschlossen: welle-33" unverändert korrekt. Kein Regress.
- **Versions-/Historie-Konsistenz:** Kopf-Version `0.25.0` (`:3`), neue
  Historie-Zeile `0.25.0 | 2026-06-23 | … | slice-045` (`:945`) und die
  Slice-/Roadmap-Nennung „Lastenheft 0.25.0" stimmen überein; die
  Memory-Notiz, dass die Doc-Version ≠ Release-Tag ist, bleibt unberührt
  (kein Release-Anspruch im CR). Kein Befund.
- **Ablage-Regel (dieser Report):** unter `docs/reviews/**` — von `ids`/
  `codepaths` exemptiert, **nicht** von `links`/`anchors`; der einzige
  Markdown-Link (`2026-06-23-slice-045-r1.md`, gleiches Verzeichnis) existiert,
  alle übrigen Verweise sind Klartext-/Inline-Code-Pfade mit `Datei:Zeile`.
  Kein Anker-/Link-Bruch.

## Kategorie-Summary

| Kategorie | Anzahl (neu in R2) |
| --------- | ------------------ |
| HIGH      | 0                  |
| MEDIUM    | 0                  |
| LOW       | 0                  |
| INFO      | 0                  |

**R1-Findings-Status:** F-1 (MEDIUM) aufgelöst · F-2 (LOW) aufgelöst ·
F-3 (LOW) aufgelöst · F-4 (INFO) aufgelöst · F-5 (INFO) aufgelöst. Keine
neuen Findings; keine Regression.

## Verdikt

**ACCEPT.**

Die Nachbesserung schließt das blockierende **F-1 real**: „definiert" ist jetzt
operational eindeutig („eigenständiges Kennungs-Token außerhalb von Fences —
Heading **oder** Tabelle/Fließtext, bewusst nicht heading-zentriert") und deckt
die tabellen-definierten `ARC-NN` des genannten Anlasses ab; der Happy-Path-AK
ist dazu konsistent und gegen die reale Trigger-Quelle erfüllbar. F-2 (Glossar
trägt `diagrams`), F-3 (Allquantor zu „bislang einziges" entschärft mit
Quell-Verweis), F-4 (`spans`-Isolation als ADR-/`.a`-Auflage in Slice §4
benannt) und F-5 (Trigger blockgenau: `ARC` im Flowchart, `LH-FA-POL-004` im
Sequenzdiagramm) sind je verifiziert geschlossen. Regressions-Check ohne Befund:
Link-Disziplin und Anker intakt, MR-006/§3.4 gewahrt (kein Abwärts-Link im
Spec-Körper, Provenance in §7), keine neuen bare IDs, planning-check-Invariante
und Versions-Historie konsistent. Restpunkte der `.a`-/ADR-Ebene (Token-Grenz-
Mechanik, `spans`-Byte-Identität) sind korrekt auf die Implementierung deferiert
und im Plan verortet — nicht-blockierend, da erst im Code messbar.
