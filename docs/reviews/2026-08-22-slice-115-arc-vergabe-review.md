# Review-Report: slice-115 — `ARC-*`-Vergabe in der Architektur-Sicht + `diagrams` als Konsument

**Datum:** 2026-08-22 · **Review-Art:** Code-/Konfigurations-Review (geprüft gegen Slice-Plan, Wellendokument welle-80 D1/D3, Baseline v5.7.0 `grundlagen-source-precedence.md` §ID-Schema als Klammer + §Vergabe und `templates/spec/architecture.template.md`, `DC-FA-DIAG-001` samt Spezifikation §DC-FA-DIAG-001.a, `DC-FA-ID-001`/§DC-FA-ID-001.a, `DC-QA-03`, Hard Rules AGENTS §3.4/§3.7 + §4/§5, MR-000, MR-006, MR-025) mit eigenen Gegenproben am gebauten Image
**Gegenstand:** Commit `6181757` (Range `34d3c0d..6181757`) — elf `ARC-<NNN>` in `spec/architecture.md` (§1 neue Komponenten-Tabelle + Kasten-Label, §2 neue erste Spalte, §3 Fortsetzung), Modul `diagrams` opt-in in `.d-check.yml`, Verankerung in der Netzlos-Modulliste in `internal/adapter/driven/configyaml/gate_consistency_test.go`, `doc-check`-Beschreibung in `AGENTS.md` §4 und `harness/README.md` §Sensors; **vor** der Closure, kein Release
**Skill:** `.harness/skills/reviewer.md` @ 1.5.0 · **Modell-ID:** `claude-opus-5[1m]`
**Eingangs-Kontext:** Slice-Plan `docs/plan/planning/in-progress/slice-115-arc-vergabe-architektur.md` (§2 Vorgehen, §3 NICHT-Liste, §5 Risiken); Wellendokument `docs/plan/planning/welle-80-struktur-ids.md` (D1/D3, §3 Closure-Trigger, §6 Out-of-Scope); vendorte Baseline `.harness/baseline/v5.7.0/templates/spec/architecture.template.md` und `.harness/baseline/v5.7.0/regelwerk/grundlagen-source-precedence.md` §ID-Schema als Klammer + §Vergabe; `.harness/baseline/v5.7.0/regelwerk/grundlagen-harness-dateien.md` §Was ein Kommentar trägt und `grundlagen-traceability.md` §Herkunfts-Anker; Spezifikation §DC-FA-DIAG-001.a, §DC-FA-VER-001.a, §DC-FA-ID-001.a; `internal/hexagon/core/rules/diagrams.go` und `internal/adapter/driven/configyaml/configyaml.go` als Ist-Wahrheit des Moduls; `harness/conventions.md` §MR-000, `harness/conventions/MR-006-referenzrichtung-matrix.md`, `harness/conventions/MR-025-spiegel-vor-dem-editieren.md`; Vorgänger-Slice `docs/plan/planning/done/slice-114-spec-vergabe-spezifikation.md` und Vorgänger-Report `docs/reviews/2026-08-22-slice-114-spec-vergabe-review.md`. Nicht erhalten: DoD-Abhakung (Verifikations-Rolle). Kein `make`-Target im echten Repo — alle Proben liefen als Image-Lauf (`docker run --rm --network none -v "$SCRATCH":/repo:ro d-check:latest`) gegen eine `.git`-freie Baum-Kopie außerhalb des Repos, Exit je Lauf explizit in eine Datei umgeleitet und geprüft (Arbeitsregel BEO-007); Rückbauten per Dateikopie, md5 gegen das Original geprüft.

## Findings

### F-1 · MEDIUM

- **kategorie:** MEDIUM
- **quelle:** `MR-025` (Spiegel vor dem Editieren) / ADR-0024 („grün, sofern keine Accepted-ADR berührt") / die deklarierte Kopplung im Makefile-Kommentar selbst
- **pfad:** `Makefile:203-212` (`FOCUS_DISABLE`), Wirkung an `Makefile:79`, `Makefile:82`, `Makefile:201`, `Makefile:214`
- **befund:** Der Kommentar über `FOCUS_DISABLE` deklariert die Kopplung wörtlich — *„Spiegelt die .d-check.yml-modules-Liste; wächst die dort, hier nachziehen (ein neues Default-Modul liefe sonst mit … Über-Feuern)"* —, der Commit erweitert `modules` um `diagrams`, die Abwahl-Liste bleibt bei neun Modulen. Damit läuft `diagrams` in vier fokussierten Läufen mit, die nur ihr eines opt-in-Modul fahren sollen: `make gate-consistency` und `make planning-check` (beide in `make gates`), `make trace-check` (Push-/PR-CI, `.github/workflows/ci.yml:70`) und `make adr-check` (`pre-commit`-Hook mit `STAGED=1`, PR-CI `ci.yml:71`). Die Hilfezeile `Makefile:213` sagt für `adr-check` weiterhin „nur vcs"; gemessen ist das falsch — ein einziges `diagram-id-undefined` im Arbeitsbaum (auch ungestagtes WIP) macht den ADR-Immutable-Lauf rot, obwohl keine Accepted-ADR berührt ist, und den Traceability-Lauf, obwohl die Commit-Message in Ordnung ist.
- **verifizierbar:** ja — Negativ-Probe 9: der Lauf mit exakt der `FOCUS_DISABLE`-Flag-Kette meldet Exit 1 und `spec/architecture.md:37 ARC-042 diagram-id-undefined`; vor dem Commit hätte dieselbe Kette 0 Befunde geliefert (kein aktives Datei-Modul).
- **klasse:** modulliste-gewachsen-abwahlliste-nicht-nachgezogen

### F-2 · MEDIUM

- **kategorie:** MEDIUM (LOW-Klasse „latente Wartungsfalle", eine Stufe höher nach der Kontext-Eskalation des Reviewer-Skills: der Pfad ist ein Gate, und der `pre-commit`-Hook bindet jeden Commit an einen grünen `doc-check`)
- **quelle:** `DC-FA-DIAG-001` / Spezifikation §DC-FA-DIAG-001.a Schritte 1–3 (Ventil-Parität zu `ids`, §DC-FA-ID-001.a Schritt 4) / AGENTS §3.7 (Klasse *Grenze*)
- **pfad:** `.d-check.yml:143-157` (Kommentarblock) und `.d-check.yml:158-161` (der `diagrams:`-Block ohne `scope`)
- **befund:** Der Kommentar benennt zwei gemessene Grenzen; die dritte, die als einzige den Arbeitsalltag trifft, fehlt: das Muster gilt **repo-weit**. Ohne `diagrams.scope` prüft das Modul jede gescannte Datei, und anders als `ids` (dasselbe Muster, `exempt-paths: [CHANGELOG.md, "docs/reviews/**"]` plus Zeilen-Marker) hat `diagrams` weder `exempt-paths` noch eine Marker-Auswertung — `internal/hexagon/core/rules/diagrams.go:37-57` kennt keinen `ignoreMarker`. Ein `mermaid`-Beispiel mit einer dreistelligen `ARC`-Kennung außerhalb der Sicht — ein Lauf-Beleg in `docs/reviews/`, eine Modul-Illustration im Benutzerhandbuch, genau die Stellen, an denen dieses Repo Kennungen literal zitiert — ist damit ein harter Befund, den nur ein Ausschluss der ganzen Datei über `diagrams.scope.ignore` abstellt; der Commit ist dann bis zur Entfernung des Beispiels blockiert. Der Bestand ist heute unauffällig (drei Live-Dateien mit `mermaid`-Fences, keine mit dreistelliger `ARC`-Kennung), die Falle zündet beim nächsten Edit.
- **verifizierbar:** ja — Negativ-Probe 7: derselbe angehängte `mermaid`-Block in einem Report unter `docs/reviews/` liefert Exit 1 / `diagram-id-undefined`, mit **und** ohne `d-check:ignore` auf der Zeile.
- **klasse:** ventil-asymmetrie-zwischen-modulen-desselben-musters

### F-3 · LOW

- **kategorie:** LOW
- **quelle:** AGENTS §3.7 (ein Kommentar beschreibt, was da ist; Klasse *Abgrenzung*) / Spezifikation §DC-FA-VER-001.a Schritt 2
- **pfad:** `.d-check.yml:144-145`
- **befund:** Die Begründungszeile *„Die uebrigen Module sehen Fences nie — eine Kennung im Diagramm ist fuer sie unsichtbar"* stimmt für `ids`, nicht für „die übrigen Module": `versions` liest ausdrücklich **auch** Fenced-Code (§DC-FA-VER-001.a Schritt 2: „**alle** Roh-Zeilen — Prosa **und** Fenced-Code"; `internal/hexagon/core/rules/versions.go:19-21` nennt es „eine gescopte Ausnahme von der Fence-Opazität"), und `spans` hat den Fence-Zustand zum Gegenstand. Der Widerspruch steht in derselben Datei 30 Zeilen tiefer: der `versions`-Kommentar (`.d-check.yml:222`) sagt „auch in Fenced-Code, gescopte Fence-Ausnahme". Wer die Zeile beim nächsten Modul-Zugang liest, übernimmt eine Invariante, die das Repo an zwei Stellen bereits widerlegt.
- **verifizierbar:** nein — Kommentar-Aussage; die Gegenlage ist gegen Spezifikation §DC-FA-VER-001.a und `.d-check.yml:222` belegt, nicht gegen einen Gate-Exit.
- **klasse:** pauschale-abgrenzung-uebersieht-die-ausnahme-im-selben-file

### F-4 · LOW

- **kategorie:** LOW
- **quelle:** `MR-025` („Der Spiegel ist die Stelle, nicht die Datei"; Spiegel *Autoritäts-Doku*) / `DC-FA-TGT-001` (die Doku-Tabellen sind Autoritäts-Doku)
- **pfad:** `Makefile:134` (Hilfetext des `doc-check`-Targets), dazu `AGENTS.md:186` und `harness/README.md:74`
- **befund:** Dieselbe `doc-check`-Beschreibung existiert an drei Stellen; zwei sind nachgezogen, die dritte nicht: `Makefile:134` nennt weiterhin „Doku-Links, Anker, ID-Linkpflicht + Referenzmatrix … DC-FA-LINK/ANCH/ID/MTX" — vier von zehn aktiven Modulen, ohne `codepaths`, `spans`, `hostpaths`, `versions`, `structure`, `diagrams`. Auch die beiden nachgezogenen Zeilen bleiben unvollständig: sie nennen jetzt sieben Module und lassen `spans`, `hostpaths` und `versions` weiter ungenannt, während sie im selben Satz „netzloser Lauf aller Module außer `external`" behaupten. Ein Leser, der `make help` oder die Autoritäts-Tabelle als Inventar nimmt, unterschätzt den Umfang des Gates, das seinen Commit blockiert.
- **verifizierbar:** nein — Prosa-/Hilfetext-Drift; kein Gate liest die Beschreibungsspalte (`targets` vergleicht Target-**Namen**, nicht Texte).
- **klasse:** enumerations-spiegel-teilweise-nachgezogen

### F-5 · INFO

- **kategorie:** INFO
- **quelle:** Baseline `templates/spec/architecture.template.md` §5 / Slice-Plan §1 + §4 / Wellendokument §4
- **pfad:** `docs/plan/planning/welle-80-struktur-ids.md:64` (Slice-Zeile der Wellen-Tabelle), `docs/plan/planning/in-progress/slice-115-arc-vergabe-architektur.md:55,72`
- **befund:** Die Abweichung — §5 Fehlermodelle bleibt kennungslos — ist sachlich richtig und von der Vorlage gedeckt (deren §5-Tabelle führt `Fehlerquelle | Behandlung-Schicht | Logging`, keine ID-Spalte; die Baseline vergibt `ARC-*` an „Komponente oder Schnittstelle", und für Zielelemente ohne Kennung sieht sie ausdrücklich den `§`-Anker vor). Festgehalten ist sie ausschließlich in der Commit-Botschaft: Wellendokument §4 („§3 extern und §5 Fehlermodelle setzen fort") und Slice §1/§4 behaupten weiterhin Kennungen in §5. Beide Artefakte wandern nach `done/` und werden dort ohne die Botschaft gelesen.
- **verifizierbar:** nein — Planungs-Artefakt-Aussage; die Deckung durch die Vorlage ist gegen `.harness/baseline/v5.7.0/templates/spec/architecture.template.md` belegt.
- **klasse:** begruendete-abweichung-nur-in-der-botschaft

### F-6 · INFO

- **kategorie:** INFO
- **quelle:** `DC-FA-DIAG-001` / Muster-Wahl `\bARC-\d{3}\b` (Slice-Plan §2 Schritt 3)
- **pfad:** `.d-check.yml:160`
- **befund:** Die dreistellige Musterbreite mit beidseitiger Wortgrenze — richtig gewählt gegen den Präzedenz-Befund an `ARC-\d{2}` — hat eine ungenannte Kehrseite: eine vertippte Kennung außerhalb der Breite ist im Diagramm **still**. Gemessen bleiben `ARC-0123` und `ARC-42` im Kasten-Label bei Exit 0 / 0 Befunden; die abschließende `\b` verhindert, dass `ARC-0123` als Präfix-Token `ARC-012` gemeldet wird (das ist die gewollte Wirkung), und die Kennung selbst fällt durch kein Muster. Der Wächter fängt die verwaiste Kennung, nicht die verunglückte.
- **verifizierbar:** ja — Negativ-Probe 8: beide Token im `mermaid`-Fence der Sicht, Exit 0.
- **klasse:** musterbreite-macht-den-tippfehler-unsichtbar

## Negativbefunde (geprüft, ohne Befund)

Alle Läufe: `docker run --rm --network none -v "$SCRATCH":/repo:ro d-check:latest`, Ausgabe je Lauf in eine Datei, Exit explizit gelesen.

1. **Sollform (Leitfrage 3).** Baum-Kopie unverändert, Lauf P0 ⇒ `EXIT=0`, `d-check: 422 Datei(en) geprüft, 0 Befund(e)`. Die Zahl der Commit-Botschaft ist damit unabhängig reproduziert; alle zehn Module inklusive `diagrams` laufen dabei mit.

2. **Vergabe: Zählung, Lückenlosigkeit, Doppelfreiheit, Dokumentreihenfolge (Leitfrage 1).** `grep -o "ARC-[0-9]\{3\}" spec/architecture.md | sort | uniq -c` ⇒ elf verschiedene Kennungen, `ARC-001`…`ARC-011`, keine Lücke, keine Dublette; je dreimal für 001–007 (Fence, §1-Tabelle, §2-Tabelle), je einmal für 008–011. Die Vergabe-Reihenfolge im Dokument ist streng aufsteigend: Zeilen 30–36 (Fence), 50–56 (§1-Tabelle), 88–91 (§3). Sieben Kästen im Flowchart, sieben Zeilen in der Komponenten-Tabelle — die Vorlagen-Regel „eine Zeile je Kasten des Diagramms" ist eingehalten. Ohne Befund.

3. **§1 ↔ §2: dieselben sieben, keine neuen (Slice §5 Risiko 3, Leitfrage 1).** Die Kennungs-Menge der §2-Tabelle ist mengengleich mit der §1-Tabelle (`ARC-001`…`ARC-007`); §2 führt keine Kennung ein, die §1 nicht vergibt, und lässt keine aus. Die Reihenfolge in §2 (002…007, dann 001) ist die **bestehende** Zeilenfolge der Schichten-Tabelle, die der Commit nicht antastet — die Vergabe passiert in §1, §2 referenziert. Kein Konventions-Anker verlangt Monotonie in einer referenzierenden Tabelle (die Baseline-Vorlage ordnet ihre §2 nur deshalb aufsteigend, weil dort Schicht- und Vergabe-Reihenfolge zufällig zusammenfallen); die sechs `structure`-Chronologie-Regeln zielen auf andere Dateien/Abschnitte. Ohne Befund; das Risiko ist gemessen ausgegangen.

4. **Vollständigkeit gegen die Vorlage — trägt eine Sektion zu Unrecht keine Kennung (Leitfrage 1)?** Die Vorlage vergibt in §1 (Komponenten) und §3 (externe Berührungspunkte), referenziert in §2 und lässt §4 (Sequenzen) und §5 (Fehlermodelle) kennungslos — ihre §5-Tabelle hat keine ID-Spalte. Die gelieferte Datei hat genau §1–§5 und deckt beide Vergabe-Sektionen vollständig ab (7 Kästen, 4 externe Zeilen). Die Plan-Abweichung ist damit die vorlagen-konforme Variante ⇒ F-5 betrifft nur ihre Dokumentation. Ohne Befund an der Vergabe.

5. **Spaltenname `Kennung` statt `ID` (Leitfrage 1).** Kein Befund, sondern Haus-Stil mit Präzedenz: `spec/spezifikation.md` führt seit slice-114 in §3, §4 und §6 dieselbe erste Spalte als `Kennung` (Zeilen 2609, 2623, 2688), das Baseline-Regelwerk selbst betitelt seine Straten-Tabelle mit `Kennung`, und die Datei ist durchgehend deutschsprachig. Anzumerken bleibt, dass die Vorlage in §2 gar nicht `ID` schreibt, sondern `Komponente(n)` — die dortige Sonderregel („umfasst die Schicht mehrere Komponenten, bleibt die Spalte leer") ist hier gegenstandslos, weil jede Zeile genau eine Komponente ist. Ohne Befund.

6. **Hard Rule §3.4 auf den NEU geschriebenen Zeilen (Leitfrage 2).** Geprüft wurden ausschließlich die hinzugefügten Zeilen: der Kopf-Absatz „Kennungen" und die sieben Zeilen der Komponenten-Tabelle (die §2-/§3-Zeilen sind bis auf die vorangestellte Spalte byte-gleich zum Vorzustand). Keine Sprach-, Technologie- oder Modul-Pfad-Nennung: die Rollen-Spalte ist gegenüber §2 sogar entschärft (`ARC-005` sagt „rein lesender Zugriff auf die Versionsgeschichte" statt `.git`, `ARC-006` „Konfiguration dekodieren und validieren" statt `.d-check.yml`); „git" erscheint nur im **bestehenden** Komponenten-Namen „VCS-/git-Adapter", der schon vor dem Commit im Kasten-Label und in §2 stand. Keine Welle, kein Slice, kein Commit-Hash, kein Closure-Datum; das Datum in der Statuszeile ist der von der Vorlage vorgesehene Frische-Marker. Der neue Absatz sagt zudem ausdrücklich, was die Baseline an dieser Stelle verlangt (Struktur-ID, keine Anforderung, keine Zusage). Ohne Befund.

7. **Gegenprobe A und die drei Implementer-Behauptungen (Leitfrage 3).** Alle drei eigenständig nachgestellt, jeweils Rückbau per Dateikopie mit md5-Gleichheit gegen das Original (`7391f5fe4ac6776009997746b454caa0`):
   - **A — nicht definierte Kennung im Fence:** zusätzlicher Kasten `XXX["ARC-042 Fremd-Kasten"]` ⇒ `EXIT=1`, `spec/architecture.md:37 ARC-042 diagram-id-undefined`. Behauptung bestätigt.
   - **B — nur die §1-Tabellenzeile entfernt** (`ARC-003`): ⇒ `EXIT=0`, 0 Befunde. Die Kennung lebt über die §2-Zeile weiter; die Grenze (2) des Kommentars beschreibt die Wirklichkeit korrekt.
   - **B' — Komponente ganz aus der Prosa** (beide Tabellenzeilen weg, Kasten bleibt): ⇒ `EXIT=1`, `spec/architecture.md:32 ARC-003 diagram-id-undefined`. Behauptung bestätigt.
   Zusätzlich geprüft, dass die Sicht sich nicht selbst über den Fence definieren kann: `definedTokens` liest `defined-in` über `proseLines`, also mit entfernten Fences (`internal/hexagon/core/rules/diagrams.go:66-84`) — deshalb greift B' überhaupt. Ohne Befund an den Behauptungen.

8. **Muster-Ränder (Leitfrage 3).** `ARC-0123` und `ARC-42` als zusätzliche Kasten-Labels ⇒ `EXIT=0`, 0 Befunde (⇒ F-6). Die beidseitige Wortgrenze wirkt wie zugesagt: kein Fehl-Target `ARC-012` aus `ARC-0123`, kein Substring-Treffer. Die Musterwahl selbst (dreistellig, `\b`) ist korrekt und deckt, was der Commit behauptet.

9. **Geltungsbereich und Ventile (Leitfrage 3, dritte Grenze).** `mermaid`-Fence mit `ARC-042` an einen Report unter `docs/reviews/` angehängt ⇒ `EXIT=1` / `diagram-id-undefined`; derselbe Block mit `d-check:ignore` auf der Kennungs-Zeile ⇒ **weiterhin** `EXIT=1`. Gegenrichtung: eine **definierte** Kennung (`ARC-001`) im `mermaid`-Fence der Roadmap ⇒ `EXIT=0` — fremde Dateien dürfen die Kennung nennen, ohne dass `ids` eine Linkpflicht auslöst (Fences sind für `ids` unsichtbar). Bestands-Zensus der `mermaid`-Fences im Live-Baum: `spec/architecture.md`, `docs/plan/planning/in-progress/roadmap.md`, `docs/reviews/2026-07-18-slice-076-markdown-lexik-commonmark.md` — keiner der beiden letzteren trägt heute eine dreistellige `ARC`-Kennung, der Abhängigkeitsgraph der Roadmap arbeitet mit Wellen-Namen. Heute grün ⇒ F-2 ist eine Falle, kein Bruch.

10. **Fail-closed der `defined-in`-Bindung (BEO-004: welche Eingaben liest das Modul, die es nicht scannt?).** `spec/architecture.md` weggeschoben ⇒ `EXIT=2` mit `d-check: error: konfiguriertes ids-Target existiert nicht: spec/architecture.md`, ohne Prüfung — kein stilles Grün. Die `defined-in`-Datei ist die einzige Eingabe, die `diagrams` außerhalb seines Scans liest (`fsys.ReadFile`, nicht discovery-gebunden); sie liegt im Scan-Bereich, unterliegt aber dort keiner der Modul-Zusagen: was in ihrer Prosa steht, gilt als definiert, gleich ob es vergibt, erwähnt oder widerruft — genau die Grenze (2) des Kommentars. Ein zweiter Wirkungspfad wäre `scan.ignore` auf der Quelle (dann ungeprüft, aber weiter definierend); für dieses Repo ausgeschlossen (`ignore` listet nur `.harness/`-Pfade). Ohne Befund.

11. **Netzlos-Bindung und Fail-closed-Charakter des Tests (Leitfrage 4).** `diagrams` gehört in `netlessDocModules()`: das Modul liest ausschließlich über den Filesystem-Port (`diagrams.go`), kennt keine Revision und keinen Netzzugriff, und `forbiddenInNetless()` (external, sources, vcs) bleibt unberührt — die `DC-QA-03`-Aussage „netzloser Lauf aller Module außer `external`, kein Range" bleibt wahr, der Lauf P0 lief mit `--network none`. Der Test bleibt fail-closed in jedem Zweig: fehlende oder undekodierbare `.d-check.yml` ⇒ `t.Fatalf`; der Guard ist über `assertNetlessModules` geteilt, sodass Live-Prüfung und Mutations-Fälle denselben Pfad treffen; die Guard-Tabelle deckt „erstes Modul fehlt", „letztes Modul fehlt" (jetzt `diagrams`, über `full[:len(full)-1]`) und je ein gesetztes verbotenes Modul ab. Der Live-Test liest den Pfad `../../../../.d-check.yml` relativ zum Paket — korrekt. Der Testlauf selbst (`make test`) ist Sache der Verifikation. Ohne Befund.

12. **MR-025-Spiegel, selbst abgeleitet (Leitfrage 5).** Ableiter statt Gedächtnis: `grep` nach dem vorigen Vertreter derselben Art (den Modulnamen der `modules`-Liste, u. a. `structure`) über den ganzen Baum, dazu `grep` nach `doc-check` in der Live-Doku. Gefunden und geprüft: (a) `AGENTS.md:186` — nachgezogen; (b) `harness/README.md:74` — nachgezogen; (c) `Makefile:134` Hilfetext — nicht nachgezogen ⇒ F-4; (d) `Makefile:210-212` `FOCUS_DISABLE` — nicht nachgezogen ⇒ F-1; (e) `.d-check.closure.yml:17` führt `modules: []` und aktiviert ausschließlich über die Kommandozeile — kein Spiegel, korrekt nicht angefasst; (f) `README.md:60` / `README.de.md:59` und `docs/user/operations.md:23` / `docs/user/benutzerhandbuch.md:1773` listen `diagrams` als **Produkt**-Modul bereits seit v0.25.0 — sie spiegeln das Prüf-Profil dieses Repos nicht, korrekt unberührt; (g) `spec/spezifikation.md:170` zeigt ein generisches Config-Beispiel, keine Repo-Konfiguration; (h) `--print-config`/`--suggest-config`-Vorlage und Spezifikation §2-Schema: welle-80 §6 schließt Produkt-Änderung aus, der Diff berührt sie nicht; das `diagrams`-Schema (`fences`, `patterns`, `defined-in`, `scope`) existiert dort seit v0.25.0 unverändert; (i) `harness/conventions/MR-007-aufloesung-mr-003.md` nennt `make doc-check` als Geltungsbereich ohne Modul-Enumeration — nichts nachzuziehen. Der Datei-Zensus ist damit vollständig; die zwei Lücken liegen beide im `Makefile`, also in genau der Datei-Klasse, die `MR-025` beim ersten Anwenden schon einmal übersehen hat.

13. **Referenz-Richtung (Leitfrage 6, MR-006).** `grep -c "ARC-"` ⇒ `spec/spezifikation.md`: **0**; `spec/lastenheft.md`: 1 Treffer, und der ist außerhalb des Musters — die §7-Historie-Zeile zu v0.25.0 zitiert `ARC-NN` als **zweistelliges Fremdprojekt-Beispiel** (Anlassfall „belief-agent-Architektur"), keine Nennung einer d-check-Kennung und kein Link. Kein Abwärtsverweis Spezifikation → Architektur, keine Verletzung der `matrix`-Ordnung `lastenheft → spezifikation → architecture`. Die Sicht selbst nennt in den neuen Zeilen nichts Abwärtiges (kein ADR, keine Welle, kein Slice, kein Planning-Pfad); der Lauf P0 meldet null `matrix`-Befunde. Ohne Befund.

14. **Kommentar-Klassen des neuen Config-Blocks (Hard Rule §3.7).** `.d-check.yml:143-157` trägt Zusage („geprüft wird Existenz, nicht Linkpflicht"), Rang-Zeiger (`DC-FA-DIAG-001`), Kopplung (Definitionsquelle ist die Sicht: §1-Tabelle plus Kasten-Label) und zweimal Grenze; Herkunft steht als **ein** auflösbares Feld in der Baseline-Form (`DC-FA-DIAG-001, seit welle-80` — `grundlagen-traceability.md` §Herkunfts-Anker erlaubt genau diese Paarung, und der `structure`-Block darüber führt sie seit slice-114 identisch). Keine Slice-Nummer, kein Mess-Label, keine Review-Historie, keine Deliberation über Verworfenes. Inhaltliche Lücken der Grenzen-Aufzählung ⇒ F-2/F-3, die Form ist ohne Befund.

15. **Botschaft gegen Baum (BEO-009-Probe).** Die zählbaren Aussagen der Commit-Botschaft nachgerechnet: „elf Kennungen" ✓ (Probe 2), „§1 sieben Komponenten in einer neuen Komponenten-Tabelle" ✓, „§3 setzt mit vier externen Berührungspunkten fort" ✓, „§2 nennt dieselben sieben und führt keine neuen ein" ✓ (Probe 3), „Sollform 422 Dateien / 0 Befunde" ✓ (Probe 1), Gegenproben A/B/B' ✓ (Probe 7), „matrix-Messung: null ARC-Nennungen in der Spezifikation" ✓ (Probe 13), „AGENTS §4 und harness/README §Sensors nennen es" ✓. Nicht selbst nachgestellt: „`make test` grün" und Gegenprobe C (`diagrams` aus der Modulliste ⇒ `make test` rot) — ein Go-Test-Lauf war ausgeschlossen (kein Host-Go, kein `make` im echten Repo); die Rot-Richtung folgt zwingend aus dem gelesenen Guard (Probe 11). Die Botschaft benennt beide Abweichungen (§5 kennungslos, Spaltenname) ausdrücklich und trägt keine Struktur-ID im Betreff — konform zu AGENTS §5. Ohne Befund.

16. **Slice §5-Risiken (Leitfrage 7).** Risiko 1 (Lesbarkeit der Kasten-Labels): die Kennung steht als Präfix vor der Rolle, die Zeilenumbrüche (`<br/>`) und alle Kanten-Labels sind unverändert — die Form ist wie geplant ausgegangen. Risiko 2 (Regex-Härte): die konstruierte Gegenprobe existiert und greift (Probe 7 A), die Wortgrenzen sind gemessen (Probe 8). Risiko 3 (doppelte Wahrheit §1/§2): gemessen ausgegangen (Probe 3). Alle drei sind damit belegt beantwortbar; das Eintragen der Ausgänge ist Closure-Arbeit. Ohne Befund.

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 2 (F-1, F-2) |
| LOW | 2 (F-3, F-4) |
| INFO | 2 (F-5, F-6) |

## Verdikt: APPROVE MIT AUFLAGEN

Die Vergabe selbst ist sauber und belegbar: elf Kennungen, lückenlos, doppelfrei, in Dokumentreihenfolge, sieben Kästen zu sieben Tabellenzeilen, §2 mengengleich ohne Neuvergabe, §3 als Fortsetzung derselben Reihe. Keine Sektion ist übersehen, die die Vorlage bevergabt — die Abweichung gegenüber dem eigenen Plan (§5 kennungslos) ist die vorlagen-konforme Variante und wurde zugunsten der höherrangigen Quelle entschieden; der Spaltenname `Kennung` hat Präzedenz im Technik-Stratum. Die Hard Rule §3.4 hält auf allen neuen Zeilen, und die Rollen-Spalte der neuen Tabelle ist technologiefreier formuliert als der Bestand. Die Referenz-Richtung ist gemessen, nicht behauptet. Der Konsument tut, was der Commit sagt: alle drei Gegenproben sind unabhängig reproduziert, inklusive der ehrlich benannten grünen — dass das Modul Nennung nicht von Vergabe unterscheidet, steht als Grenze im Kommentar, statt als Zusage behauptet zu werden. Die Netzlos-Bindung ist richtig gesetzt und der Test in jedem Zweig fail-closed.

**Zwei Punkte gehören vor die Closure**, beide außerhalb der Vergabe: **F-1** ist eine deklarierte Kopplung, die dieser Commit bricht — die Abwahl-Liste des Makefiles spiegelt die Modul-Liste ausdrücklich und wurde nicht nachgezogen; die Folge ist Über-Feuern in vier fokussierten Läufen, darunter der `pre-commit`-Hook und zwei CI-Schritte, deren Hilfezeile weiterhin „nur vcs" behauptet. Gemessen, nicht hergeleitet (Probe 9). **F-2** ist die Grenze, die der sonst vorbildliche Kommentar nicht nennt: das Muster gilt repo-weit, und anders als beim identischen `ids`-Muster gibt es weder `exempt-paths` noch Zeilen-Marker — die erste `mermaid`-Illustration mit einer dreistelligen Kennung in einem Lauf-Beleg oder im Handbuch blockiert über den Hook jeden Commit. Beide sind klein zu schließen und beide betreffen genau das, womit die Welle ihren Wert begründet: einen Gate-Konsumenten, der ehrlich beschrieben ist.

**Nice-to-fix mit der Closure:** F-3 (die pauschale Fence-Aussage widerspricht dem `versions`-Kommentar derselben Datei) und F-4 (die dritte Fassung der `doc-check`-Beschreibung). F-5 gehört in die Closure-Notiz bzw. die Ergebnisnotiz der Welle, F-6 ist eine Notiz für den nächsten Modul-Zugang.
