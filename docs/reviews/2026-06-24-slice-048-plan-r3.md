# Review R3 — slice-048 (`versions`-Modul, Plan-/Doc-Review)

## Kopf-Metadaten

- **Gegenstand:** [`slice-048-versions-modul.md`](../plan/planning/done/slice-048-versions-modul.md)
  im aktuellen `HEAD` (`c6d4d8b`). Kein Code-Diff im Umfang.
- **Review-Art:** Plan-/Dokumenten-Review vor Implementierung.
- **Reviewer:** Codex, adversarial nach [`reviewer.md`](../../.harness/skills/reviewer.md)
  Version 1.0.0.
- **Datum:** 2026-06-24.
- **Eingangs-Kontext:** [`AGENTS.md`](../../AGENTS.md) §3/§5/§6,
  [`harness/README.md`](../../harness/README.md), [`harness/conventions.md`](../../harness/conventions.md)
  `MR-013`/`MR-014`, Baseline-Bundle `lab-regelwerk.zip` v1.3.0
  (Module 3, 5, 10), [`spec/lastenheft.md`](../../spec/lastenheft.md)
  `DC-FA-VER-001`/`DC-FA-CLI-002`/`DC-FA-CLI-010`,
  [`spec/spezifikation.md`](../../spec/spezifikation.md), [`ADR-0019`](../plan/adr/0019-versions-pin-fence-ausnahme.md),
  [`version.md`](../../version.md), Roadmap §Aktuelle Welle, R1/R2-Reports.
- **Sensoren:** `make planning-check`, `make doc-check`.

## Findings

### F-1 — LOW · Quelle: Maintainability / Doku-Drift · `spec/spezifikation.md:3`

Die technische Spezifikation enthält inzwischen die `versions`-Algorithmik, die
Schema-Keys und den Grund-Code `version-stale`, aber der Kopf steht weiterhin
auf „Letzte Änderung: 2026-06-23" und §7 enthält keinen slice-048-Eintrag. Die
Rang-2-Quelle ist damit inhaltlich aktuell, aber ihre Provenance wirkt auf den
Stand vor der `versions`-Ergänzung zurückgesetzt.

**verifizierbar:** nein — Prosa-/Historienkonsistenz; `make doc-check` bleibt
dafür erwartbar grün.

### F-2 — LOW · Quelle: Maintainability / Gate-Wartung · `version.md:4`

Das Release-Register erklärt, pro Release seien nur die Aktuell-Zeile, die neue
Verlaufszeile und der wandernde `<a id>`-Anker nachzuziehen, enthält im
Einleitungsblock aber zusätzlich einen festen Link auf `#v0.27.0`. Beim nächsten
Release-Bump bricht dieser Link ebenfalls, sobald der Anker zur neuen Version
wandert; die Pflegeanweisung unterschätzt damit den tatsächlichen
Release-Edit-Umfang.

**verifizierbar:** nein — aktueller `doc-check` ist grün; das Failure-Szenario
entsteht beim nächsten Release-Bump.

## Negativbefunde (geprüft, ohne Befund)

- **R2-Finding F-1 (`versions`-Schema):** `spec/spezifikation.md` enthält jetzt
  `versions.pin-pattern`, `versions.current-from` und `versions.exempt-paths`
  in der Schema-/Constraint-Tabelle. Geprüft, ohne Befund.
- **R2-Finding F-2 (only-current-anchor):** `version.md` beschreibt jetzt, dass
  nur die aktuelle Version den expliziten Versionsanker trägt und dass der Anker
  beim Release wandert. Geprüft, ohne Befund.
- **Lastenheft/Modul-Registry:** `DC-FA-CLI-002`, Glossar und Historie listen
  `versions`; `DC-FA-VER-001` enthält Happy/Negative/Boundary/Modul-aus.
  Geprüft, ohne Befund.
- **ADR-Kontext:** `ADR-0019` ist im ADR-Index als Proposed eingetragen und
  begründet die breitere Fence-Öffnung gegenüber `ADR-0018`. Geprüft, ohne
  Befund.
- **Lifecycle/Planning:** Roadmap §Aktuelle Welle zeigt auf `slice-048`, und
  `make planning-check` ist grün. Geprüft, ohne Befund.
- **Aktueller Doku-Sensorzustand:** `make doc-check` ist grün. Geprüft, ohne
  Befund.

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---:|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 2 |
| INFO | 0 |

## Verdikt

**ACCEPT mit LOW-Nacharbeit.** Die R2-Blocker sind behoben; die verbleibenden
Punkte betreffen Provenance und Release-Wartung, nicht die geplante
`versions`-Semantik.
