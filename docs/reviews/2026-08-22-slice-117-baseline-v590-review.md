# Review-Report: slice-117 — Baseline-Pin-Hebung `v5.7.0` → `v5.9.0` (zwei Stufen)

**Datum:** 2026-08-22 · **Review-Art:** Doku-/Konventions-Review (geprüft gegen Slice-Plan slice-117 §2/§3/§5, Wellendokument welle-81 §1/§3/§6, Hard Rules AGENTS §1/§3.3/§3.7, `MR-013` (Lifecycle-Move-Bündelung), `MR-021` (pin-gebundene Verweise), `MR-023` (self-contained Bundle-Layout), `MR-029` (der neue Pin-Nachtrag), die vendorte Vorlage `harness/conventions/MR-NNN-titel.template.md`, Beobachtungs-Register BEO-008) mit eigenen Gegenproben am gebauten Image und am Kurs-Repo
**Gegenstand:** Commit-Range `67a8d8b..e6a08fc` — `1b3be75` (Lifecycle-Move `MR-028` nach `harness/conventions/done/` samt Link-Tiefen-Fixes und Index-Zeilen-Umzug) und `e6a08fc` (v5.9.0-Bundle vendored, `v5.7.0`-Baum entfernt, neue Adaption `MR-029`, Pin in §Baseline und §Adoptierte Konventions-Quellen, Drei-Klassen-Zensus der pin-gebundenen Verweise, `ignore-refs`-Eintrag für die eingefrorene `MR-027`); **vor** der Closure, kein Release, kein Produkt-Code
**Skill:** `.harness/skills/reviewer.md` @ 1.6.0 · **Modell-ID:** `claude-opus-5[1m]`
**Eingangs-Kontext:** Slice-Plan `docs/plan/planning/in-progress/slice-117-baseline-v590-bump.md`; Wellendokument `docs/plan/planning/welle-81-zustandsfelder.md`; Vorgänger-Hebung `docs/plan/planning/done/slice-110-baseline-v570-bump.md` und ihr Report `docs/reviews/2026-08-21-slice-110-baseline-v570-review.md` (dort war die Klasse-2/3-Lücke die Auflage); `docs/plan/planning/observations.md` (BEO-008 zählt genau diese Klasse, Stand 2; BEO-007 als Arbeitsregel); `harness/conventions.md` §Baseline, §Adoptierte Konventions-Quellen, §Adaptions-Block; `harness/conventions/MR-013-lifecycle-move-buendelung.md`, `harness/conventions/MR-021-vendored-verweise-pin-gebunden.md`, `harness/conventions/MR-023-baseline-v500.md`, `harness/conventions/MR-029-baseline-v590.md`, `harness/conventions/done/MR-027-struktur-id-verzicht.md`, `harness/conventions/done/MR-028-baseline-v570.md`; `.d-check.yml` (`scan.ignore`, `ignore-refs`, `codepaths.exempt-paths`, `planning`); `tools/harness/fetch-baseline-cache.sh`; vendorter Baum `.harness/baseline/v5.9.0/` samt `SHA256SUMS` und `templates/harness/conventions/MR-NNN-titel.template.md`. Nicht erhalten: die DoD-Abhakung (Verifikations-Rolle). Kein `make`-Target im echten Repo — alle Produkt-Proben liefen als Image-Lauf gegen `.git`-freie Baum-Kopien außerhalb des Repos, Exit je Lauf explizit in eine Datei umgeleitet und gelesen (Arbeitsregel BEO-007); Rückbauten per Dateikopie, Sollform-Lauf danach wiederholt. Der entfernte Vorgänger-Baum wurde für die Delta-Messung aus `67a8d8b` in eine Kopie außerhalb des Repos ausgecheckt; als unabhängiges Orakel diente zusätzlich das lokal vorliegende Kurs-Repo (nur lesende `git`-Kommandos).

## Findings

### F-1 · MEDIUM

- **kategorie:** MEDIUM
- **quelle:** `MR-029` (eigener Körper, Bullet *Alter Baum entfernt, Historie via Tombstone*) / Slice-Plan §2 Schritt 3 Klasse 1 (Pfad-Verweise) / BEO-008
- **pfad:** `harness/conventions/MR-029-baseline-v590.md:48-49`
- **befund:** Der Bullet behauptet `.harness/baseline/v5.9.0/` sei entfernt. Entfernt ist der Vorgänger-Baum; `.harness/baseline/v5.9.0/` ist genau der Baum, den derselbe Commit anlegt und auf den alle 26 lebenden Pfad-Verweise jetzt zeigen. Die Aussage widerspricht dem Titel und dem §Adaption-Rumpf derselben Datei und macht den Rest des Bullets in sich unlesbar: „der einzige eingefrorene Link-Bestand **darauf** — die aufgelöste `MR-027`" bezieht sich auf einen Pfad, den `MR-027` nicht trägt (`MR-027` verlinkt zweimal `.harness/baseline/v5.7.0/regelwerk/…`), und begründet damit einen `ignore-refs`-Eintrag, dessen `refs`-Glob auf den *anderen* Tag lautet. Es ist die Über-Hebung derselben Klasse, die der Zensus dieses Slice fangen soll: der Pfad-grep der Klasse 1 hat die Version in einer Aussage **über die Vergangenheit** mitgehoben. Zum Vergleich steht die wortgleiche Vorgänger-Formulierung eine Hebung früher korrekt auf dem *alten* Tag (`harness/conventions/done/MR-028-baseline-v570.md:44-45` nennt den `v5.6.0`-Baum). Der Eintrag ist zugleich die im selben Bullet-Block deklarierte „Checkliste für den Nachfolger".
- **verifizierbar:** nein — kein Gate liest die Prosa-Aussage gegen den Verzeichnisbestand (Negativ-Proben 1 und 2 bleiben stumm, der Pfad in der Zeile existiert ja). Belegt per Verzeichnis-Listing (`.harness/baseline/` enthält genau `v5.9.0`), per `git show 1b3be75 --stat` (Baum-Umbenennung `v5.7.0 => v5.9.0`) und per Textvergleich mit `harness/conventions/done/MR-028-baseline-v570.md:44-45`, Negativ-Probe 9.
- **klasse:** pfad-grep-hebt-die-aussage-ueber-den-entfernten-baum-mit

### F-2 · MEDIUM

- **kategorie:** MEDIUM
- **quelle:** `MR-029` §Adaption („**gemessen statt gezählt**") / Slice-Plan §1 („das inhaltliche Delta ist in der Welle gemessen") / Präzedenz: Auflage F-2 des Vorgänger-Reports `docs/reviews/2026-08-21-slice-110-baseline-v570-review.md` (Bundle-Zählung ehrlich machen)
- **pfad:** `harness/conventions/MR-029-baseline-v590.md:25-29`
- **befund:** Die Delta-Angabe geht nicht auf und teilt nach der falschen Achse. Behauptet: 33 geänderte Bundle-Dateien, „26 davon **nur** im Quell-URL-Stempel", inhaltlich „vier Regelwerks-Dateien … und **sieben** Templates" — das sind 26 + 4 + 7 = 37 von 33. Nachgemessen über beide Bäume: 33 Dateien unterscheiden sich, davon tragen **23** ausschließlich die eine geänderte Stempel-Zeile (22 `regelwerk/`-Dateien mit ihrer `<!-- Quelle: … -->`-Kopfzeile plus `templates/harness/conventions.template.md` mit der Download-URL), inhaltlich ändern sich **10** Dateien: vier `regelwerk/`- und **sechs** Template-Dateien. Die genannten 26 und 7 sind in Wahrheit die Aufteilung nach *Baum* (26 `regelwerk/`- und 7 `templates/`-Dateien unterscheiden sich), nicht nach Änderungsart. Die Einzelangaben der vier Regelwerks-Dateien stimmen dagegen aufs Zeichen (`grundlagen-harness-dateien` +34/−1, `modul-03-spec` +11, `modul-06-roadmap` +5/−5, der Index). Das siebte Template trägt keine Regel, sondern nur den Tag in der Download-URL. Wer die Zahl als Umfangsangabe für die Folge-Slices liest — die MR ist ausdrücklich der Delta-Datensatz, „die Regel verkörpern und anwenden die Folge-Slices" —, sucht in `templates/harness/conventions.template.md` nach einer Regel, die dort nicht steht, und unterschätzt zugleich die Zahl der inhaltlich betroffenen Dateien um drei. Dieselben Zahlen stehen wortgleich in `docs/plan/planning/welle-81-zustandsfelder.md:19-21` (außerhalb dieser Range entstanden, aber vom MR-Körper übernommen). Es ist die zweite Hebung in Folge, in der die Bundle-Zählung nachgebessert werden muss.
- **verifizierbar:** nein — kein Gate misst eine Prosa-Zahl. Belegt durch zwei unabhängige Messungen: (a) Datei-für-Datei-`diff` des Vorgänger-Baums aus `67a8d8b` gegen `.harness/baseline/v5.9.0/` (33 Unterschiede, davon 23 mit genau einer geänderten Zeile), (b) `git diff --stat v5.7.0..v5.9.0 -- lab/regelwerk lab/templates` im Kurs-Repo: exakt 10 Dateien, 4 Regelwerk + 6 Templates, +86/−23. Negativ-Proben 5 und 6.
- **klasse:** delta-zahlen-teilen-nach-baum-statt-nach-aenderungsart

### F-3 · LOW

- **kategorie:** LOW
- **quelle:** `harness/conventions.md` §Aufgelöste Adaptionen, eigene Sektionsregel: „nur ID und **Nachfolger**, damit die Kette auffindbar bleibt, ohne gelesen zu werden"
- **pfad:** `harness/conventions.md:146`
- **befund:** Die neue Zeile für `MR-028` trägt in der Spalte *aufgelöst durch* keinen Nachfolger, sondern eine Ankündigung: „die nächste Pin-Hebung (der eigene Auflösungs-Trigger des Eintrags)". Die Zeile entstand in `1b3be75`, als der Nachfolger noch nicht existierte; `e6a08fc` legt `MR-029` an, füllt die Zelle aber nicht nach. Damit endet die Pin-Kette in der Tabelle: `MR-011` → `MR-012` → `MR-016` → `MR-023`, `MR-026` → `MR-028` → *(nichts)*. Die Zeile darüber zeigt die Sollform (`MR-026` nennt `MR-028` als Link). Wer die Kette auditiert — der Wellen-Closure-Trigger-Audit über alle drei Artefaktklassen tut das —, findet den fünften Nachtrag von hier aus nicht; auffindbar ist er nur über die §Baseline-Prosa oder über das Öffnen von `MR-029`, also genau über den Weg, den die Sektionsregel vermeiden will.
- **verifizierbar:** nein — die Zelle enthält keinen Link, also kann kein `links`/`anchors`-Befund entstehen (Negativ-Probe 1 bleibt bei 0 Befunden). Belegt per Zeilenvergleich gegen die Nachbarzeile `harness/conventions.md:145` und gegen die Sektionsregel `harness/conventions.md:133-134`.
- **klasse:** aufgeloeste-zeile-ohne-nachfolger-zeiger

### F-4 · LOW

- **kategorie:** LOW
- **quelle:** vendorte Vorlage `.harness/baseline/v5.9.0/templates/harness/conventions/MR-NNN-titel.template.md` §Regeln dieser Datei („`Löst auf` und `Ausgelöst durch Baseline-Stand` **nur, wenn dieser Eintrag einen früheren ablöst**" — dort als Pflichtpaar) / Slice-Plan §2 Schritt 2 („Nachfolge-Adaption **aus der vendorten Vorlage** anlegen")
- **pfad:** `harness/conventions/MR-029-baseline-v590.md:18-19` und `:64-66`
- **befund:** `MR-029` löst `MR-028` ausdrücklich ab („ersetzt `MR-028` nach dessen eigenem Auflösungs-Trigger"), führt aber weder das Feld `Löst auf:` noch `Ausgelöst durch Baseline-Stand:`. Die Ablösung steht stattdessen zweimal als Fließtext im `Adaption`- und im `Auflösungs-Trigger`-Feld, jeweils als **Pfad**-Link auf die Eintrags-Datei (`done/MR-028-baseline-v570.md`) — genau die Form, vor der die Vorlage im selben Satz warnt: „der Verweis geht auf die Index-Zeile, nicht auf die Eintrags-Datei: die wandert bei Auflösung nach `done/` und ein Pfad-Link bricht genau dann". Hier hält der Pfad, weil `MR-028` seinen Endzustand erreicht hat; die Form ist trotzdem die abgeratene, und der Baseline-Stand, der die Ablösung ausgelöst hat, ist als Feld nicht abfragbar. Der Bestand ist konsistent daneben: `MR-026` und `MR-028` führen die beiden Felder ebenfalls nicht — die Abweichung ist damit nicht neu, wird mit diesem Neuzugang aber fortgeschrieben und ist der Vorlage gegenüber nirgends als Haus-Form deklariert.
- **verifizierbar:** nein — kein Gate prüft die Feld-Menge einer `MR-*`-Datei gegen die vendorte Vorlage (Negativ-Probe 1 bleibt stumm). Belegt per Feld-Inventar über alle 29 `MR-*`-Dateien und per Textvergleich mit der Vorlage, Negativ-Probe 10.
- **klasse:** ableitungs-felder-fehlen-obwohl-der-eintrag-abloest

### F-5 · LOW

- **kategorie:** LOW
- **quelle:** `MR-021` §Geltungsbereich (die Aufzählung, die deklariert, **wo** pin-gebundene Verweise leben) / BEO-008 (die Spiegel einer Pin-Hebung sind drei Klassen)
- **pfad:** `harness/conventions/MR-021-vendored-verweise-pin-gebunden.md:6-9`
- **befund:** Der Geltungsbereich nennt als lebende Träger „(aktuell `harness/README.md` §Guides/§Sensors, `docs/plan/planning/README.md`, §Adoptierte Konventions-Quellen)". Gemessen tragen acht Dateigruppen mit zusammen 26 Pfad-Verweisen den Pin: `harness/README.md` (5), `harness/conventions.md` (4), `AGENTS.md` (4), `.harness/skills/reviewer.md` (2), neun aktive `MR-*`-Dateien (je 1), `docs/plan/planning/README.md` (1), `docs/plan/planning/in-progress/roadmap.md` (1) und der Slice-Plan (1). Die Datei ist in `e6a08fc` angefasst worden (Zeile 4 retargetet, Zeile 17 als Klasse-3-Ellipse gehoben); die veraltete Aufzählung zwei Zeilen darüber blieb stehen. Wer beim nächsten Bump die Regel liest, die die Pin-Bindung *deklariert*, bekommt drei von acht Trägern genannt — die vollständige Liste steht nur im `Geltungsbereich` von `MR-023` und `MR-029`, also in den Einträgen, die beim nächsten Mal gerade abgelöst werden. Das ist die Mechanik, aus der BEO-008 entstanden ist.
- **verifizierbar:** nein — Prosa-Aufzählung, kein Gate. Belegt per `git grep`-Zählung aller `.harness/baseline/v5.9.0/`-Vorkommen außerhalb des vendorten Baums (26 Treffer in 16 Dateien), Negativ-Probe 4.
- **klasse:** geltungsbereich-enumeration-nennt-drei-von-acht-traegern

### F-6 · LOW

- **kategorie:** LOW
- **quelle:** Slice-Plan §2 Schritt 3 Klasse 3 („Prosa-/Ellipsen-Pins in MR-Körpern und Fließtext … jede Klasse einzeln durchgehen und das Ergebnis je Klasse notieren") / `MR-029` §Hebungs-Zensus („Klasse 3 traf genau **eine** lebende Stelle … und eine Sektionsregel der Roadmap")
- **pfad:** `.d-check.yml:337`
- **befund:** Der Kommentar über dem `planning`-Block nennt die Form, die das Modul misst, mit einem Versions-Pin: „Offene-Wellen-Form (Baseline v5.7.0, slice-108/110)". Die Schwester-Aussage in `docs/plan/planning/in-progress/roadmap.md:15` — „Regeln dieser Sektion: … §Roadmap-Struktur (v5.7.0)" — ist im selben Commit auf `v5.9.0` gehoben worden, diese nicht. Damit sagen zwei Stellen, die dieselbe Baseline-Sektion beschreiben, zwei verschiedene Stände; und der genannte Stand ist der einzige, dessen Text im Repo nicht mehr nachschlagbar ist (der Baum ist entfernt), obwohl `modul-06-roadmap.md` §Offene Wellen in `v5.9.0` inhaltlich fortgeschrieben wurde (+5/−5, Bijektions- und Kardinalitäts-Absatz neu). Der Zensus deklariert für Klasse 3 zwei lebende Fundstellen; ob diese dritte als historisch eingestuft wurde oder unbemerkt blieb, ist nirgends notiert — und genau die Unterscheidung ist der Zweck des klassenweisen Protokolls.
- **verifizierbar:** nein — Klasse 3 ist gate-blind; Negativ-Probe 3 zeigt das an der URL-Hälfte derselben Klasse (fünf lebende Kurs-URLs auf den alten Tag zurückgedreht: Exit 0, 0 Befunde). `.d-check.yml` liegt zudem außerhalb der `codepaths.roots`. Belegt per Vergleich der beiden Kommentar-/Prosa-Stellen und per `git diff` der Bundle-Datei `regelwerk/modul-06-roadmap.md` über beide Stufen.
- **klasse:** prosa-pin-in-der-config-nicht-im-klassen-protokoll

## Negativbefunde (geprüft, ohne Befund)

Alle Produkt-Läufe: `docker run --rm --network none -v "$SCRATCH":/repo:ro d-check:latest`, Ausgabe je Lauf in eine Datei, Exit explizit gelesen. `$SCRATCH` ist eine `.git`-freie Kopie des jeweiligen Commit-Baums außerhalb des Repos.

1. **Sollform-Lauf `e6a08fc` (P1).** Unveränderte Baum-Kopie ⇒ `EXIT=0`, `d-check: 431 Datei(en) geprüft, 0 Befund(e)`. Alle zehn konfigurierten Module liefen mit; die Zahl der Commit-Botschaft ist damit unabhängig reproduziert. Der `planning`-Lauf (`--enable planning` plus die zehn `--disable`-Schalter des Fokus-Satzes) ⇒ `EXIT=0`, 0 Befunde. **Per-Commit-Grün auch für das erste Glied:** derselbe Lauf gegen die Baum-Kopie von `1b3be75` ⇒ `EXIT=0`, `430 Datei(en) geprüft, 0 Befund(e)` — der Move-Commit ist für sich grün, die Kette hat keinen roten Zwischenstand. Ohne Befund.
2. **Klasse 1 ist gate-gedeckt (Negativ-Probe).** Ein lebender Pfad-Verweis in `harness/README.md` auf den entfernten Baum zurückgedreht ⇒ `EXIT=1`, genau ein `target-missing` auf `../.harness/baseline/v5.7.0/regelwerk/modul-13-quality-gates.md`. Die Pin-Kopplung aus `MR-021` ist damit für Klasse 1 gate-erzwungen, wie die Regel behauptet. Rückbau, Sollform wiederhergestellt (`EXIT=0`, 0 Befunde). Ohne Befund.
3. **Klasse 2 ist gate-blind (Negativ-Probe).** Alle fünf lebenden Kurs-URLs mit dem Tag (`releases/tag/`, `releases/download/`, `tree/` in `harness/conventions.md:27/47/52`, dazu `AGENTS.md:32` und `harness/README.md:60`) auf `v5.7.0` zurückgedreht ⇒ `EXIT=0`, `0 Befund(e)`. Kein Modul deckt diese Klasse; der Zensus ist die einzige Kontrolle, und die Slice-§5-Risikoaussage ist damit gemessen statt behauptet. Rückbau, Sollform wiederhergestellt. Ohne Befund.
4. **Klassen-Zensus nachgezogen, alle drei Klassen selbst gegrept.** (1) 26 Pfad-Verweise auf `.harness/baseline/v5.9.0/` in 16 lebenden Dateien, kein einziger lebender Verweis auf einen älteren Baum: Referenzen auf `v1.4.0`, `v5.0.0`, `v5.6.0` und `v5.7.0` liegen ausschließlich in `done/`-Dokumenten, immutablen ADRs, Review-Reports und den `ignore-refs`-Einträgen selbst. (2) Fünf lebende Kurs-URLs, alle auf `v5.9.0`; die beiden übrigen Tag-URLs im Bestand (`docs/reviews/2026-07-19-…` mit `v1.4.0`, `harness/conventions/done/MR-014-…` mit `v1.3.0`) sind historisch und korrekt stehengeblieben. (3) Prosa-Pins: die Ellipse `…/v5.9.0/…` in `MR-021:17` und die Roadmap-Sektionsregel sind gehoben; `AGENTS.md:170` („seit dem v5.6.0-Bump, geschärft mit dem v5.7.0-Bump"), `docs/plan/adr/0055-…:79/171`, `spec/lastenheft.md:2818`, die `done/`-Slices der welle-79 und die Historie-Zeilen der Roadmap nennen den alten Tag als Vergangenheitsaussage und sind zu Recht unberührt. Zwei maschinelle Gegenproben: **kein** Markdown-Link im Repo, dessen Linktext eine andere Version nennt als sein Ziel (einziger Treffer ist ein Produkt-Versions-Zitat in einem Review-Report von 2026-07-17, ohne Baseline-Bezug), und **keine** Zeile mit zwei verschiedenen Baseline-Tag-Pins — die Halb-Hebung, die der Commit an sich selbst gefunden hat, ist die letzte ihrer Art. Zwei Befunde aus diesem Zensus stehen als F-1 und F-6; im Übrigen ohne Befund.
5. **Vendoring-Integrität gegen das Manifest.** `sha256sum -c SHA256SUMS` im vendorten Baum ⇒ `EXIT=0`, 51 von 51 `OK`. Manifest-Deckung beidseitig geprüft: die Menge der Dateien im Baum (51, ohne das Manifest selbst) ist mit der Menge der Manifest-Zeilen **identisch** — keine Datei ohne Prüfsumme, keine Prüfsumme ohne Datei. Die Aussage „51 Dateien beide Bäume plus `SHA256SUMS`" der Commit-Botschaft ist damit unabhängig belegt, ebenso die DoD-Angabe zu `--verify`. Ohne Befund.
6. **Vendoring-Authentizität gegen den Upstream-Tag.** Vergleich des vendorten Baums gegen `lab/regelwerk` und `lab/templates` am Kurs-Tag `v5.9.0` (nur lesende `git archive`/`diff`-Kommandos im lokal vorliegenden Kurs-Repo): identische Datei-Menge (51/51), 23 Dateien byte-identisch, 28 Dateien unterscheiden sich in **genau** der Zeile, die das Release-Bundle derivativ umschreibt — der relative Quellzeiger wird zur absoluten, auf den Tag gepinnten URL (`<!-- Quelle: … -->` in den `regelwerk/`-Dateien, `releases/latest/download/` → `releases/download/v5.9.0/` in `templates/AGENTS.template.md` und `templates/harness/conventions.template.md`). Keine einzige Abweichung darüber hinaus: es ist wirklich nur vendored worden, an den Bundle-Bäumen wurde nicht Hand angelegt. Zusätzlich Currency: `v5.9.0` ist der höchste Tag des Kurs-Repos (drei ungetaggte Commits danach betreffen `lab/team-sim` und die Roadmap) — die `--check-latest`-Behauptung „Pin ist der neueste Release" hält. Ohne Befund.
7. **Slice-§5-Risiko „zwei Stufen auf einmal" materiell geprüft.** Der Delta-Diff wurde je Stufe einzeln gefahren: Stufe 1 (`v5.7.0..v5.8.0`) berührt sechs Dateien, Stufe 2 (`v5.8.0..v5.9.0`) sieben. Gegenprobe auf transiente Regeln — Zeilen, die Stufe 1 einfügt und Stufe 2 wieder entfernt — ergibt **genau eine** Zeile, die Zwischenstands-Angabe des Regelwerks-Index („Stand: Kurs-Welle 83"). Es gibt keine Regel, die in `v5.8.0` entstand und in `v5.9.0` verschwand; die Zwei-Stufen-Blindstelle ist gemessen leer. Ohne Befund.
8. **Der entfernte Baum: Auflösung und Skopus des Ventils.** Alle verbliebenen Nennungen von `.harness/baseline/v5.7.0` sind klassifiziert: **zwei Markdown-Links** (beide in `harness/conventions/done/MR-027-struktur-id-verzicht.md:4` und `:6`) und **sechs Inline-Code-Nennungen** in fünf Review-Reports. Die Begründung des `ignore-refs`-Eintrags ist damit wahr — die Reports nennen den Pfad tatsächlich nur in Inline-Code, und `codepaths.exempt-paths` führt `docs/reviews/**` bereits. Das Ventil ist **load-bearing und minimal**: entfernt man den Eintrag, meldet der Lauf `EXIT=1` mit **genau zwei** `target-missing`-Befunden, beide auf `MR-027` — nichts anderes hängt daran. Und es ist **quell-skopiert, nicht global**: derselbe tote Pfad, testweise in `harness/conventions/done/MR-026-baseline-v560.md` eingefügt, wird gemeldet (`EXIT=1`, ein `target-missing`). Beide Rückbauten geprüft, Sollform wiederhergestellt. Ohne Befund.
9. **Commit-Schnitt und Lifecycle-Move (`MR-013`, AGENTS §3.3).** `1b3be75` ist ein reiner Move plus die Link-Tiefen-Fixes der bewegten Datei (Git meldet `similarity index 74 %`, die Botschaft deklariert den `git mv` ausdrücklich — die von AGENTS §3.3 verlangte Kompensation für den gesunkenen Rename-Score) plus die gekoppelte Index-Zeile: `MR-028` wandert von *Aktive* nach *Aufgelöste Adaptionen*, **beide** `<a id>`-Anker wandern mit, und der Nachfolger-Zeiger der `MR-026`-Zeile wird auf den neuen `done/`-Pfad nachgezogen. Nichts Übriges: keine Inhaltsänderung am `MR-028`-Rumpf jenseits der `../` → `../../`-Tiefen, kein Pin, kein Bundle. Die Bündelung der Index-Zeile ist notwendig (ohne sie wäre der Commit `target-missing`-rot) und liegt im Sinn von `MR-013`; die Präzedenz `f845e8b` hat denselben Umfang noch in **einem** Commit erledigt, hier ist er sauberer getrennt. Anker-Auflösung gegengeprüft: die eingefrorenen Verweise auf `#mr-028` (sechs Fundstellen in `done/`-Slices, Wellen-Results, Roadmap-Drift-Log, Beobachtungs-Register, Wellendokument und Slice-Plan) und auf den Voll-Slug (§Baseline-Kette `harness/conventions.md:39`) lösen weiter auf — Negativ-Probe 1 mit aktivem `anchors`-Modul ist der Beleg. Zur Baum-Neuvendorierung im selben Commit wie die Verweis-Hebung: Git zeigt sie als Umbenennung `v5.7.0 => v5.9.0`, AGENTS §3.3 zielt aber auf die `git log --follow`-Verfolgbarkeit repo-eigener Artefakte; ein derivativer Snapshot wird nicht verfolgt, und die Trennung wäre zwingend rot (entfernter Baum ohne gehobene Verweise). Ohne Befund; ein Befund steht in F-3.
10. **MR-Form von `MR-029` gegen die vendorte Vorlage.** Nummer ist die nächste freie des dichten Zählraums (aktiv bis `MR-025`, aufgelöst bis `MR-028`, kein `MR-029` im Bestand). `Status`, `Datum`, `Geltungsbereich`, `Ersetzt-Baseline-Regel`, `Adaption`, `Begründung`, `Auflösungs-Trigger` sind vorhanden und gefüllt; das `Ersetzt-Baseline-Regel`-Feld trägt begründet einen Strich (Pin-Fortschreibung im Layout des Vorgängers, kein Fork — die Vorlage verlangt „genau eine Regel", nicht „mindestens eine", und `MR-023` trägt das Layout weiter). Der Auflösungs-Trigger ist sauber und selbst-referenziell konsistent (die nächste Pin-Hebung ersetzt den Eintrag; `MR-023` bleibt daneben aktiv, weil es das Layout trägt, nicht den Pin-Wert) — er ist derselbe, der in diesem Slice für `MR-028` eingetreten ist, die Ablösungs-Mechanik ist also an sich selbst belegt. Die Index-Zeile in §Aktive Adaptionen trägt den Voll-Slug- **und** den Kurz-Anker. Die inhaltlichen Delta-Aussagen der vier Regelwerks-Dateien sind gegengemessen und stimmen; die Aussage „alle tragen **eine** Regel" hält auch für das Technik-Stratum-Detail des Kurs-Deltas (die Spezifikations-Historie ohne `Version`-Spalte) — `spec/spezifikation.md:2696` führt bereits nur `| Datum | Änderung |`, es entsteht kein ungenannter Nachzug. Zwei Form-Befunde stehen als F-2 und F-4; im Übrigen ohne Befund.
11. **Scope-Treue gegen Slice §3 (NICHT-Liste).** Die Range fasst 17 Dateien außerhalb des vendorten Baums an: `.d-check.yml`, `.harness/skills/reviewer.md`, `AGENTS.md`, `docs/plan/planning/README.md`, `docs/plan/planning/in-progress/roadmap.md`, `harness/README.md`, `harness/conventions.md`, neun aktive `MR-*`-Dateien plus die neue `MR-029`. **Kein** Kopf-Zustandsfeld ist angefasst (die Roadmap-Kopfzeile `**Status:** Aktiv. **Letzte Änderung:**` steht unverändert, ebenso die der Spezifikation und des Beobachtungs-Registers), `docs/plan/planning/observations.md` ist überhaupt nicht berührt, keine Drift-Log-Zeile und keine `Stand`-Zelle geändert, `spec/` unberührt, kein Produkt-Code, kein Release, keine Version. Die neue Regel wird vendored und zitiert, aber nicht angewandt — genau wie §3 und §6 des Wellendokuments es verlangen. Die Diffs der übrigen Dateien sind ausnahmslos Ein-Token-Hebungen `v5.7.0` → `v5.9.0`; `AGENTS.md:170` (Hard-Rule-Herkunft „seit dem v5.6.0-Bump, geschärft mit dem v5.7.0-Bump") ist als Vergangenheitsaussage korrekt stehengeblieben. Ohne Befund.
12. **Skript und Layout.** `tools/harness/fetch-baseline-cache.sh` ist in der Range nicht angefasst — konsistent mit der MR-Aussage „kein Layout-Wechsel, dasselbe Materialisierungs-Skript". Das Pfadschema `.harness/baseline/<tag>/{regelwerk,templates}/` ist unverändert, `scan.ignore` steht tag-agnostisch auf `.harness/baseline/**` und brauchte keine Hebung. Ohne Befund.

## Kategorie-Summary

| Kategorie | Anzahl | Findings |
| --------- | ------ | -------- |
| HIGH      | 0      | — |
| MEDIUM    | 2      | F-1, F-2 |
| LOW       | 4      | F-3, F-4, F-5, F-6 |
| INFO      | 0      | — |

## Verdikt

**APPROVE mit Auflagen** — keine HIGH; die beiden MEDIUM sind Text-Korrekturen an einem einzigen Artefakt und blockieren die Closure, nicht den Ansatz.

Der Kern des Slice ist sauber und unabhängig belegt: das Bundle ist authentisch vendored (51/51 gegen das Manifest, 51/51 gegen den Upstream-Tag, jede Abweichung nachweislich die derivative Stempel-Zeile des Release-Builds), der Vorgänger-Baum ist restlos entfernt, jeder verbliebene Verweis darauf ist entweder eingefroren und quell-skopiert ausgenommen oder eine Inline-Code-Nennung in einem ohnehin ausgenommenen Report, das Ventil ist load-bearing und beweisbar nicht global, alle 26 lebenden Pfad-Verweise und alle fünf lebenden Kurs-URLs stehen auf dem neuen Pin, es gibt **keine** Halb-Hebung mehr im Bestand (zwei maschinelle Gegenproben), beide Commits sind je für sich `doc-check`-grün, der Commit-Schnitt trennt Move und Inhalt sauberer als die Präzedenz, und die Scope-Grenze des Slice ist exakt eingehalten. Der Drei-Klassen-Zensus hat gegenüber der Vorgänger-Hebung sichtbar getragen: die Klasse, die dort als Auflage nachgezogen werden musste, ist hier vom Verfahren selbst gefangen worden.

**Auflagen vor der Closure:**

- **F-1 (MEDIUM):** `harness/conventions/MR-029-baseline-v590.md:48` sagt, der aktuelle Baum sei entfernt. Der Eintrag ist der Pin-Datensatz **und** die deklarierte Checkliste für die nächste Hebung; die Zeile macht in derselben Passage die Tombstone-Begründung unlesbar.
- **F-2 (MEDIUM):** die Delta-Zahlen in `harness/conventions/MR-029-baseline-v590.md:25-29` gehen nicht auf (26 + 4 + 7 von 33) und teilen nach Baum statt nach Änderungsart; gemessen sind es 23 Stempel-Dateien und 10 inhaltliche (4 Regelwerk, 6 Templates). Es ist die zweite Hebung in Folge, in der die Bundle-Zählung nachgebessert werden muss.

**Steering-Loop-Signal** nach `reviewer.md` §Kontext-Eskalation: F-1, F-5 und F-6 sind dreimal in einem Lauf dieselbe Klasse — eine Versions-Angabe in **Prosa**, die der Pfad-grep entweder mitnimmt, obwohl sie über die Vergangenheit spricht (F-1), oder liegen lässt, obwohl sie eine lebende Aussage ist (F-6), plus eine Bestands-Aufzählung, die niemand mitzieht (F-5). BEO-008 zählt bisher die Nicht-Hebung; die Über-Hebung ist die Gegenrichtung derselben Beobachtung und mit F-1 erstmals belegt. Für den Register-Lese-Schritt der Welle: der Zähler ist fällig, und die bei 3× benannte mechanische Form — Abgleich der Tag-Muster in URLs und Prosa gegen den §Baseline-Pin, Bauform des `versions`-Moduls — müsste beide Richtungen messen, nicht nur die vergessene.

**Nicht geprüft (Rollengrenze):** die DoD-Abhakung des Slice-Plans und der `make gates`-Lauf im echten Repo — beides ist Verifikations-Territorium; die hier gefahrenen Image-Läufe sind Review-Gegenproben, kein Gate-Nachweis.
