# Review — slice-026 Implementierung (`--repair`)

## Kopf-Metadaten

- **Datum:** 2026-06-18
- **Gegenstand:** Working-Tree-Diff der slice-026-Umsetzung (Reparatur-
  Patch `--repair`) — `core/repair.go`, `report.Repair`, CLI-Flags,
  Spec/Doku.
- **Reviewer:** Claude (Agent), Skill `.harness/skills/reviewer.md` v1.0.0.
- **Eingangs-Kontext:** Slice-Plan `slice-026-repair.md`; Anforderungen
  DC-FA-CLI-008 (Haupt), DC-FA-CLI-007 (FixCandidateFor), DC-FA-CLI-003/004;
  Hard Rules DC-QA-02 (Determinismus), DC-QA-03 (read-only); ADR-0005
  (Import-Regeln). `make gates` grün (Coverage 93,9 %).

## Findings

### (in-flight gefixt) Über-matchende Kennung → falsch platzierte Ersetzung

- **Quelle:** DC-FA-CLI-008 (Konservativ-Disziplin) · **Pfad:**
  `core/repair.go` `bareOccurrences`
- **Befund:** Die literale Token-Suche hätte `ADR-0001` *innerhalb* von
  `ADR-00012` ersetzt → kaputter Patch („grün ≠ richtig"-Pfad). Das
  verletzt „konservativ = eindeutig".
- **Resolution:** Wortgrenzen-Prüfung (`wholeToken`) ergänzt; eingebettete
  Vorkommen werden übersprungen (Test `TestRepairEdits_Overmatch_…`).

### INFO-1 — Best-Guess-Reichweite

- **Quelle:** DC-FA-CLI-008 · **Pfad:** `repair.go` `uniqueBasenames`
- **Befund:** Die breite Stufe matcht nur Markdown-Dateien im Scan-Bestand
  mit **eindeutigem** Basisnamen; mehrdeutige oder nicht-md-Ziele liefern
  keinen Edit (kein Raten ins Leere). Bewusst, dokumentiert.

### INFO-2 — Titel im Link-Ziel

- **Quelle:** DC-FA-CLI-008 · **Pfad:** `repair.go` `destSpan`
- **Befund:** Bei `target-missing` mit Link-Titel (`(pfad "Titel")`) ersetzt
  der Best-Guess das ganze Ziel-Feld (Titel entfiele). Edge der breiten,
  **review-pflichtigen** Stufe — fängt der Mensch ab.

### INFO-3 — Coverage 94,5 % → 93,9 %

- **Quelle:** Maintainability · **Pfad:** `core/repair.go`
- **Befund:** Fehler-/Overlap-Randpfade (`applyRepls`-Overlap-Guard,
  `RepairEdits`-IO-Fehler) sind nicht voll exerziert. Über der Schwelle
  (93 %).

## Negativbefunde (geprüft, ohne Befund)

- **`git apply`-Reinheit (DC-FA-CLI-008):** Round-Trip-Test mit echtem
  `git init` + `git apply` + erneutem Lauf (0 Befunde) — der Patch ist
  sauber anwendbar.
- **Konservativ = eindeutig:** Inline-Code-Vorkommen werden NICHT
  repariert (positionserhaltendes Blanking; Test), über-matchende
  Kennungen ebenfalls nicht (Wortgrenze).
- **Marker-Disziplin:** review-pflichtig-Markierung der breiten Stufe geht
  auf stderr; der stdout-Patch bleibt frei von Fremdzeilen (Test).
- **Determinismus (DC-QA-02):** 10× byte-identisch, auch mehrere gleiche
  Kennungen je Zeile (Test).
- **Kombinations-Verbote (DC-FA-CLI-004):** `--repair --json` und
  `--doctor --repair` → Exit 2 (Test).
- **read-only (DC-QA-03):** Patch nur auf stdout, kein In-place.
- **Hexagon (ADR-0005):** `make arch-check` grün; `repair.go` importiert
  nur stdlib + Port; Renderer im Adapter.

## Kategorie-Summary

| HIGH | MEDIUM | LOW | INFO |
|---|---|---|---|
| 0 | 0 | 0 | 3 |

(zzgl. 1 in-flight behobener Konservativ-Defekt — Über-Match.)

## Verdikt

**Freigegeben.** Keine HIGH/MEDIUM/LOW offen; das Konservativ-Risiko
(Über-Match) wurde im selben Stand mit Wortgrenzen-Prüfung geschlossen.
INFO-Punkte als dokumentierte Best-Guess-Grenzen akzeptiert. `make gates`
grün. Closure kann erfolgen; welle-15 ist damit vollständig.
