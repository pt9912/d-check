# Review-Report: slice-118 — Die Zustandsfeld-Regel verkörpern (Briefing §3.7, Reviewer-Skill 1.7.0)

**Datum:** 2026-08-22 · **Review-Art:** Doku-/Konventions-Review (geprüft gegen Slice-Plan slice-118 §2/§3/§5, Wellendokument welle-81 §1/§4/§6, Hard Rules AGENTS §1/§3.3/§3.7, `MR-000` (Baseline-Aussage), `MR-013` (Lifecycle-Move-Bündelung), `MR-015` (das Briefing routet, es spiegelt nicht), `MR-029` (der v5.9.0-Pin-Nachtrag), die vendorten Ziel-Formen `.harness/baseline/v5.9.0/templates/AGENTS.template.md` §3.7 und `.harness/baseline/v5.9.0/templates/.harness/skills/reviewer.template.md` §Klassifikation, Kanon `.harness/baseline/v5.9.0/regelwerk/grundlagen-harness-dateien.md` §Was ein Kommentar trägt und `.harness/baseline/v5.9.0/regelwerk/modul-06-roadmap.md` §Roadmap-Struktur/§Das Beobachtungs-Register) mit eigenen Gegenproben am gebauten Image
**Gegenstand:** Commit `68bafad` (Range `d2efe9c..68bafad`) — `724b1f4` (reiner Lifecycle-Move slice-118 `open/` → `in-progress/` samt Ruhe-Marker-Flip und Pfad-Verweisen, `MR-013`) und `68bafad` (`AGENTS.md` §3.7 um den Zustandsfeld-Absatz erweitert und umbenannt, `.harness/skills/reviewer.md` 1.6.0 → 1.7.0 mit dem neuen HIGH-Eintrag *Zustandsfeld trägt Chronik*); **vor** der Closure, kein Release, kein Produkt-Code
**Skill:** `.harness/skills/reviewer.md` @ 1.7.0 · **Modell-ID:** `claude-opus-5[1m]`
**Besonderheit:** der Skill ist in diesem Lauf **zugleich Werkzeug und Gegenstand** — geprüft und angewandt wurde die neue Fassung 1.7.0 (Kategorien-Anker inklusive des neuen HIGH-Eintrags, Anti-Pattern, sechsfeldriges Output-Schema mit `klasse`, Negativbefund-Pflicht, Ablage).
**Eingangs-Kontext:** Slice-Plan `docs/plan/planning/in-progress/slice-118-zustandsfeld-regel.md` (§2 Vorgehen samt Spiegel-Liste, §3 NICHT-Liste, §5 Risiken); Wellendokument `docs/plan/planning/welle-81-zustandsfelder.md` (gemessenes Delta, vier Treffer im Bestand); Vorgänger-Slice `docs/plan/planning/done/slice-117-baseline-v590-bump.md` und dessen Report `docs/reviews/2026-08-22-slice-117-baseline-v590-review.md`; Präzedenz `docs/plan/planning/done/slice-113-struktur-id-konvention.md` (dieselbe Reihenfolge „Regel zuerst") und `docs/plan/planning/done/slice-116-adr-neuzugangs-regel.md` (Präzedenz der Skill-Version-Hebung); Folge-Slices `docs/plan/planning/open/slice-119-kopf-zustandsfelder.md` und `docs/plan/planning/open/slice-120-register-und-drift-log.md`; `harness/conventions.md` §Baseline/§MR-000/§Adaptions-Block, `harness/conventions/MR-015-agents-md-routet.md`, `harness/conventions/MR-029-baseline-v590.md`; `harness/README.md` §Guides/§Sensors; `docs/plan/planning/observations.md`; `.d-check.yml` und `.d-check.closure.yml`. Nicht erhalten: die DoD-Abhakung (Verifikations-Rolle). Kein `make`-Target im echten Repo — alle Proben liefen als Image-Lauf (`d-check:latest`, netzlos, read-only) gegen eine `.git`-freie Baum-Kopie außerhalb des Repos; Exit je Lauf explizit gelesen (Arbeitsregel BEO-007).

## Findings

### F-1 · MEDIUM

- **kategorie:** MEDIUM
- **quelle:** Kanon `.harness/baseline/v5.9.0/regelwerk/grundlagen-harness-dateien.md` §Was ein Kommentar trägt (*Dieselbe Regel für Zustandsfelder*) / Ziel-Form `.harness/baseline/v5.9.0/templates/AGENTS.template.md` §3.7 / `MR-000`
- **pfad:** `AGENTS.md:155`
- **befund:** Die neue Überschrift sagt „Kommentare **und Zustandsfelder** tragen eine der fünf Klassen" und prädiziert damit die fünf Klassen (Zusage · Kopplung · Abgrenzung · Rang-Zeiger · Grenze) über beide Gegenstände. Ein Zustandsfeld trägt keine davon: Der Kanon überträgt ausdrücklich nur die **zwei Tests** („Die zwei Tests oben gelten unverändert" — Adressat und Zeitform) und gibt dem Zustandsfeld eine eigene Form, „den Zustand und den Beleg — als auflösbaren Anker". Genau das sagt auch der Rumpf desselben Abschnitts zwei Absätze weiter (`AGENTS.md:169-170`) — die Überschrift widerspricht ihrem eigenen Körper. Die Ziel-Form löst es anders: die Vorlagen-Überschrift bleibt beim Kommentar („Ein Kommentar beschreibt, was da ist") und der erste Rumpf-Satz zieht den Geltungsbereich nach („Gilt für Code, Konfiguration und Skripte — und für Zustandsfelder (unten)"). `AGENTS.md` §3 ist der Hard-Rule-Index, den jede Session liest; wer nur die Überschriftenzeile aufnimmt — die Kurzform ist der Zweck der Liste —, sucht für eine `Stand`-Zelle nach der passenden der fünf Klassen, findet keine, und hat zwei mögliche Fehlschlüsse zur Wahl: die Zelle in eine Kommentar-Klasse pressen oder die Regel für Zustandsfelder als unanwendbar verwerfen. Der Slice liefert die Regel, **bevor** slice-119 und slice-120 sie anwenden — der Titel ist damit die Fassung, gegen die beide Folge-Slices arbeiten.
- **verifizierbar:** nein — kein Gate liest Überschriften-Semantik; `structure` führt für `AGENTS.md` keine Abschnitts-Invariante (nur `targets`/`gate-undocumented` binden §4). Negativ-Probe 1 bleibt bei 0 Befunden, obwohl der Titel steht. Belegt per Textvergleich Überschrift ↔ Rumpf (`AGENTS.md:155` gegen `:169-170`) und gegen die Ziel-Form.
- **klasse:** hard-rule-titel-behauptet-mehr-als-die-regel

### F-2 · LOW

- **kategorie:** LOW
- **quelle:** `AGENTS.md` §3.7 §Bestandsgrenze (repo-eigene Zusatz-Aussage, in der Ziel-Form nicht vorgesehen) / Slice-Plan §3 („Keine Anwendung … slice-119, slice-120")
- **pfad:** `AGENTS.md:179-185`
- **befund:** Die Bestandsgrenze grandfathert „vor der Einführung bzw. vor dieser Schärfung geschriebene **Kommentare**" und nennt ausschließlich Kommentar-Beispiele (Test-Kommentare, ältere Config-Kommentare mit Slice-Nummer) — während die Herkunfts-Klammer im selben Absatz jetzt auch die v5.9.0-Schärfung auf Zustandsfelder trägt. Damit steht im Briefing nicht, ob der bestehende Zustandsfeld-Bestand grandfathered ist: vier Kopfzeilen, acht `Stand`-Zellen des Beobachtungs-Registers und die Schließungs-Zeilen des Drift-Logs sind sämtlich älter als die Regel. Die Antwort steht nur im Wellendokument (sie werden geräumt, nicht grandfathered) — also in einem Planungs-Artefakt, das nach der Welle nach `done/` wandert, nicht im Briefing, das jede Session liest. Failure-Szenario: ein Agent fasst `docs/plan/planning/observations.md` für eine unabhängige Closure an, liest §3.7, sieht die Chronik-Zelle, die er ohnehin gerade editiert, und kann aus dem Briefing nicht entscheiden, ob „geräumt wird beim nächsten Anfassen der Zeile" für ihn gilt — er räumt außerhalb seines Slice-Skopus oder lässt einen Verstoß in einer Zeile stehen, die er gerade angefasst hat.
- **verifizierbar:** nein — kein Gate kennt den Begriff „grandfathered". Belegt per Wortlaut (`AGENTS.md:179-180` nennt nur „Kommentare") gegen `docs/plan/planning/welle-81-zustandsfelder.md:35-42` und `docs/plan/planning/open/slice-120-register-und-drift-log.md:70`.
- **klasse:** bestandsgrenze-deckt-nur-die-halbe-regel

### F-3 · INFO

- **kategorie:** INFO
- **quelle:** Kanon `.harness/baseline/v5.9.0/regelwerk/grundlagen-harness-dateien.md` §Was ein Kommentar trägt (*Dieselbe Regel für Zustandsfelder*, Eröffnungssatz)
- **pfad:** `AGENTS.md:167-168` und `.harness/skills/reviewer.md:32-33`
- **befund:** Beide neuen Texte eröffnen mit einer Aufzählung — „eine `Stand`- oder `Status`-Zelle in Roadmap, Beobachtungs-Register oder Meilenstein-Tabelle" bzw. „eine `Stand`-/`Status`-Zelle (Roadmap, Beobachtungs-Register, Meilenstein-Tabelle)" —, die sich als Skopus liest. Der Kanon eröffnet allgemein („Ein Feld, das einen *Zustand* trägt") und führt dieselben drei nur als Apposition; seine Kopfzeilen-Regel nennt zusätzlich das Reconciliation-Register. Gemessene Folge im eigenen Bestand: `harness/conventions.md:27` trägt `**Stand:** v5.9.0 (…)`, dessen Klammer sechs frühere Pin-Hebungen nacherzählt — nach dem Kanon-Test („nicht als Chronik, wie der Zustand zustande kam") ein Chronik-Feld, das weder in der Vier-Treffer-Zählung des Wellendokuments noch im Skopus der beiden neuen Texte vorkommt; `docs/user/benutzerhandbuch.md:4` `**Stand:**` liegt aus demselben Grund draußen. Gegenlesart, die dies auf INFO hält: die Klammer besteht aus auflösbaren `MR-*`-Ankern, also aus der Beleg-Form, die der Kanon zulässt — und die Verengung ist **nicht** eigenmächtig, die Ziel-Form `.harness/baseline/v5.9.0/templates/AGENTS.template.md:167-170` führt dieselben drei Klassen wortgleich. Ein Reconciliation-Register führt d-check nicht (reiner GF-Betrieb), die Lücke ist dort heute leer.
- **verifizierbar:** nein — kein Gate liest Zustandsfeld-Semantik (Negativ-Probe 1 läuft mit sämtlichen Chronik-Zellen im Baum auf 0 Befunde). Belegt per `grep` über alle `**Stand:**`-/`**Status:**`-Felder und Tabellen-Spalten außerhalb des vendorten Baums.
- **klasse:** aufzaehlung-liest-sich-als-skopus-statt-als-beispiele

## Negativbefunde (geprüft, ohne Befund)

1. **Treue zur Baseline — die drei Aussagen sind vollständig da.** Der neue Absatz trägt alle drei Aussagen des Kanons, jede satzweise gegengelesen: (a) **Zellen-Form** — „Sie nennt den Zustand und den Beleg als auflösbaren Anker, nicht die Chronik, wie es dazu kam" (`AGENTS.md:169-170`) gegen „Es nennt den Zustand und den Beleg — als auflösbaren Anker …, nicht als Chronik, wie der Zustand zustande kam"; (b) **Drift-Log-Beschränkung** — „trägt nur Umplanungen, keine Schließungen und keine erreichten Meilensteine (die stehen im Closure-Log bzw. in der Status-Spalte)" (`:171-172`) gegen `modul-06-roadmap.md` §Roadmap-Struktur („Eine Schließung ist keine Umplanung, ein erreichter Meilenstein auch nicht; für den sagt die `Status`-Spalte der Meilenstein-Tabelle *erreicht* mit Datum und Beleg"); (c) **Register-Kopfzeile** — „trägt **keine** Kopfzeile `Status: Aktiv. Letzte Änderung: <Datum>` — sein Zustand ist sein Inhalt, sein Änderungsdatum hält `git`" (`:173-175`) gegen den Kanon-Bullet *Die Kopfzeile eines lebenden Registers ist derselbe Fall*. Auch der Zwischensatz „ist ein Zustands-Artefakt wie ein Kommentar, nur im Rumpf" steht so im Kanon. Ohne Befund.
2. **Treue zur Baseline — nichts Hinzuerfundenes.** Jede Aussage des Absatzes hat eine Kanon-Entsprechung; auch die Zuweisung „(die stehen im Closure-Log bzw. in der Status-Spalte)", die im Kanon-Abschnitt selbst nur halb steht, ist über `modul-06-roadmap.md` §Roadmap-Struktur voll gedeckt (Abgeschlossene Wellen = „das einzige Closure-Log der Roadmap"; Meilenstein-`Status` = *erreicht* mit Datum und Beleg). Keine Verschärfung und keine Lockerung gegenüber dem Kanon gefunden. Ohne Befund.
3. **Die Ausnahme ist korrekt wiedergegeben und richtig platziert.** Der Kanon hängt sie an den Kopfzeilen-Fall („Ein Datum, das ein benannter Trigger pflegt, ist kein solcher Kopf — der Unterschied ist der Trigger, nicht die Zeile"), nicht an die Zellen-Regel; beide neuen Texte tun dasselbe. Das Briefing nennt ein Beispiel (Frische-Marker der Architektur-Sicht), der Skill beide des Kanons (zusätzlich der Wellen-Stand des Regelwerks) — und das zweite ist in diesem Repo real: `.harness/baseline/v5.9.0/regelwerk/README.md:3` trägt `**Stand:** Kurs-Welle 86 · 2026-08-22.`, sichtbar in jedem Bump-Diff. Der Skill nimmt damit genau den Fall vorweg, an dem ein Reviewer sonst falsch meldete. Ohne Befund.
4. **MR-015 — routet der Absatz, oder spiegelt er?** Geprüft gegen `AGENTS.md` §1 („trägt Hard Rules und Pointer … und dupliziert deren Inhalt nicht"). Der Absatz ist mit elf Zeilen etwa zweieinhalbmal so lang wie die vier Zeilen der Ziel-Form, weil er den Kopfzeilen-Fall aufnimmt, den die Vorlage in §3.7 gar nicht führt. Das ist hier trotzdem Routing und keine Nacherzählung: er schließt mit „Kanon: [Baseline §Was ein Kommentar trägt]" auf den auflösenden Link, er lässt die Herleitungen des Kanons (Leser-Modell, Adressaten-/Zeitform-Test, die Orte-Aufzählung, das Kopien-Driften) sämtlich weg, und der Kopfzeilen-Fall ist die Aussage, gegen die slice-119 im selben Wellen-Lauf arbeitet — ohne sie stünde die Regel für den größten Bestandsposten nicht im Briefing. Die Grenze ist knapp, aber nicht überschritten: gespiegelt sind Aussagen, nicht Begründungen. Ohne Befund.
5. **HIGH ist die richtige Kategorie, und die Stelle stimmt.** Die Ziel-Form `.harness/baseline/v5.9.0/templates/.harness/skills/reviewer.template.md:51-56` führt *Zustandsfeld trägt Chronik* im HIGH-Block, unmittelbar nach *Kommentar trägt keine der Kommentar-Klassen* — genau diese Reihenfolge hat `.harness/skills/reviewer.md:32-44` übernommen. d-check führt HIGH als **einen** Bullet mit fettgesetzten Anker-Namen (Haus-Form seit 1.5.0); der Neuzugang setzt sich in derselben Form fort und schließt mit derselben Herkunfts-Klammer („neuer HIGH-Eintrag seit 1.7.0, Auflösungs-Trigger: permanent") wie der 1.5.0-Eintrag. Ohne Befund.
6. **Anwendbar ohne Nachschlagen, und beide Ausprägungen sind gedeckt.** Der Eintrag nennt die Artefaktklassen, die Prüffrage („erzählt, **wie** der Zustand entstand, statt Zustand und Beleg als auflösbaren Anker zu nennen"), die zweite Ausprägung („oder ein Drift-Log protokolliert Schließungen und erreichte Meilensteine und wird damit ein zweites Closure-Log"), den Kopfzeilen-Fall samt Begründung, die Nicht-Melde-Ausnahme und den ausdrücklichen Satz „**Kein Gate fängt das**; die Prüfung ist ein Urteil, kein `grep`". Ich habe ihn in diesem Lauf angewandt, ohne den Kanon dafür zu öffnen — er trägt. Ohne Befund.
7. **Die Nicht-Melde-Ausnahme erzeugt kein Schlupfloch.** Sie ist doppelt verriegelt: das Attribut ist **benannt** (ein bloß behaupteter Trigger fällt durch), und die zwei genannten Instanzen — Frische-Marker der Sicht, Wellen-Stand des Regelwerks — sind konkrete Artefakte, keine Kategorie. Rest-Risiko benannt statt behauptet: ein Autor könnte einen Trigger selbst deklarieren, um eine Chronik-Kopfzeile zu retten; die zwei Beispiele grenzen den Raum ein, und der Kanon-Link löst die Frage in einem Hop auf. Ohne Befund.
8. **„Kein Gate fängt das" ist keine halluzinierte Gegenrichtung — es ist gemessen.** Die Sollform-Probe läuft mit dem vollständigen Chronik-Bestand im Baum (`docs/plan/planning/observations.md` mit acht `Stand`-Zellen bis 3 011 Zeichen, das Drift-Log mit Schließungs-Zeilen, vier Kopf-Zustandszeilen) auf **432 Datei(en) / 0 Befund(e)**. `structure` führt für die betroffenen Dateien nur `table-order`-Regeln, `.d-check.closure.yml` nur Closure-Note-Muster. Die Aussage des Skills ist damit belegt, nicht behauptet — die Gegenrichtung (ein Gate behaupten, wo keines steht) wäre HIGH gewesen. Ohne Befund.
9. **Versions- und Datums-Konsistenz.** `.harness/skills/reviewer.md:3` steht auf `1.7.0` / `2026-08-22`; die Hebung ist minor für einen neuen Anker, dieselbe Stufung wie 1.5.0 → 1.6.0 für den MEDIUM-Neuzugang in slice-116. Spiegel der Skill-Version im Repo per `grep` abgeleitet: **keiner** ist lebend — die Guides-Zeile `harness/README.md:61` nennt den Skill ohne Version, `harness/conventions/MR-029-baseline-v590.md:13` nennt ihn als Pfad-Verweis ohne Version, und alle Zahlen-Nennungen (`1.5.0`, `1.6.0`) stehen in `done/`-Slices und Review-Reports, also in Vergangenheits-Aussagen. Ohne Befund.
10. **Scope-Treue gegen §3 des Slice-Plans.** Der Inhalts-Commit fasst genau zwei Dateien an (`AGENTS.md`, `.harness/skills/reviewer.md`, +30/−3). Die vier bekannten Treffer sind unverändert, jeder einzeln nachgesehen: die drei Kopfzeilen `docs/plan/planning/in-progress/roadmap.md:3`, `docs/plan/planning/observations.md:3`, `spec/spezifikation.md:3` und die der Sicht `spec/architecture.md:3` stehen wortgleich; `docs/plan/planning/observations.md` ist in der ganzen Range nicht berührt (also keine `Stand`-Zelle); das Drift-Log `docs/plan/planning/in-progress/roadmap.md` §Historische Trigger-Verschiebungen ist unverändert. Kein Produkt-Code, kein Release, keine Version, kein Sensor — die NICHT-Liste hält. Ohne Befund.
11. **Der Lifecycle-Move im Range-Vorlauf ist sauber.** `724b1f4` ist ein reiner `git mv` (Slice-Body byte-identisch) samt den beiden Pfad-Verweisen in `docs/plan/planning/welle-81-zustandsfelder.md:67` und `docs/plan/planning/done/slice-117-baseline-v590-bump.md:167` — Bündelung nach `MR-013`, getrennt vom Inhalts-Commit nach AGENTS §3.3. Die einzige Roadmap-Änderung ist das Entfernen des Ruhe-Markers „Nichts in Arbeit."; das ist die von `modul-06-roadmap.md` §Roadmap-Struktur verlangte Marker-Hälfte (Marker genau dann, wenn `in-progress/` leer ist), nicht ein Eingriff in ein Zustandsfeld. Der Zeiger auf die offene Welle bleibt daneben stehen — die Zwei-Hälften-Form aus welle-79. Ohne Befund.
12. **Spiegel-Vollständigkeit, selbst abgeleitet statt aus dem Plan übernommen.** `grep` über `*.md`/`*.yml`/`Makefile` nach `fünf Klassen`, `Kommentar trägt`, `Kommentar-Klassen`, `Zustandsfeld`, `Chronik`, `reviewer.md`, `Reviewer-Skill` außerhalb `.harness/baseline/**` und `docs/reviews/**` ergibt als lebende Nenn-Stellen: `AGENTS.md` §3.7 (geändert), `.harness/skills/reviewer.md` (geändert), `harness/README.md:61` (Guides-Zeile, versionsfrei und rollenbeschreibend — trägt die Regel nicht), `harness/conventions.md` (nennt „Kommentar" überhaupt nicht), `harness/conventions/MR-029-baseline-v590.md:36` (beschreibt, was die **Vorlage** bekommt, und erklärt den Konformitäts-Abgleich ausdrücklich zur Sache der Folge-Slices), `.harness/skills/closure-note-reviewer.md` (kein Bezug zu Kommentar-Klassen oder Zustandsfeldern), `.d-check.yml` (nennt `AGENTS.md` nur als Autoritäts-Datei für §4-Targets). Keine Stelle steht nach diesem Commit unvollständig da. Ohne Befund.
13. **Der Überschriften-Umbenennung folgt kein toter Anker.** `grep` nach `AGENTS.md#3`-Verweisen findet Links auf §3.1, §3.3, §3.4, §3.5, §3.6 — **keinen** auf §3.7, weder im lebenden Bestand noch in immutablen ADRs oder `done/`-Slices. Gegenprobe 2 zeigt, dass ein solcher Verweis gemeldet worden wäre. Ohne Befund.
14. **Kein Scheinbefund-Risiko im Bestand (Risiko §5 des Plans).** Alle `Stand`-/`Status`-Felder und -Spalten außerhalb des vendorten Baums durchgesehen: ADR-Kopf und ADR-Index-Spalte (`**Status:** Accepted`), `MR-*`-Kopf, Lastenheft-Kopf, RTM-`Status`-Spalte in `spec/spezifikation.md:588` und `docs/user/benutzerhandbuch.md:637`, `Status`-Spalten in Review-Reports, `**Ausgang:**`-Felder der Slice-Pläne — keines liegt im Skopus des neuen Ankers (drei benannte Artefaktklassen plus lebende Register-Kopfzeile), und keines ist ein Kandidat für einen Fehl-HIGH. Die Meilenstein-Tabelle der Roadmap ist leer (`— keine offenen —`), das Closure-Log führt keine `Status`-Spalte. Der einzige Grenzfall geht in die Gegenrichtung und steht als F-3. Ohne Befund.
15. **Die Commit-Botschaft behauptet keine Probe, die nicht stattfand (BEO-009).** „Probe: make doc-check grün (432 Dateien / 0 Befunde)" ist am Review-Stand unabhängig reproduziert — Gegenprobe 1 liefert dieselbe Zeile und Exit 0. Auch die inhaltlichen Behauptungen der Botschaft („Kein Bestand ist hier angefasst", „Kein Sensor") halten der Prüfung stand (Negativbefunde 8 und 10). Ohne Befund.
16. **Reihenfolge-Präzedenz eingehalten.** slice-113 hat in welle-80 dieselbe Form gefahren („Die Regel steht, bevor die erste Kennung vergeben wird"); slice-118 zieht sie hier durch, und der Start-Trigger („slice-117 in `done/`") ist nachweislich eingetreten. Ohne Befund.

## Gegenproben (Exit explizit)

Alle Läufe gegen eine `.git`-freie Baum-Kopie von `68bafad` außerhalb des Repos, netzlos und read-only:

```
git archive 68bafad | tar -x -C <scratch>/review-118
docker run --rm --network none -v "<scratch>/review-118":/repo:ro d-check:latest
```

1. **Sollform-Lauf am Review-Stand** → `DCHECK-EXIT=0`, Ausgabe `d-check: 432 Datei(en) geprüft, 0 Befund(e)`. Reproduziert die Commit-Botschaft aufs Zeichen und belegt zugleich Negativbefund 8 (der volle Chronik-Bestand liegt im Baum und bleibt stumm).
2. **Gegenprobe alter Überschriften-Slug** — je eine Zeile mit dem alten (`#37-kommentare-tragen-eine-der-fünf-klassen`) und dem neuen Slug in `docs/plan/planning/README.md` angehängt → `NEG-EXIT=1`, genau ein Befund: `docs/plan/planning/README.md:28 … anchor-missing` für den **alten** Slug; der neue bleibt stumm. Beweist beides: das Ventil hätte einen toten Verweis gemeldet, und es gibt keinen.
3. **Rückbau-Lauf** nach Wiederherstellung der Datei → `RESTORE-EXIT=0`, wieder 432/0.

## Kategorie-Summary

| Kategorie | Anzahl | Findings |
| --------- | ------ | -------- |
| HIGH      | 0      | — |
| MEDIUM    | 1      | F-1 |
| LOW       | 1      | F-2 |
| INFO      | 1      | F-3 |

## Verdikt

**APPROVE mit Auflage** — keine HIGH; das eine MEDIUM ist eine Überschriften-Korrektur an einer einzigen Zeile und blockiert die Closure, nicht den Ansatz.

Der Kern des Slice ist sauber: der Zustandsfeld-Absatz trägt alle drei Kanon-Aussagen vollständig und erfindet keine vierte, die Trigger-Ausnahme ist korrekt wiedergegeben und am richtigen Fall verankert, der HIGH-Eintrag steht in der Kategorie und an der Stelle, an der die Ziel-Form ihn führt, ist ohne Nachschlagen anwendbar (in diesem Lauf angewandt), deckt beide Ausprägungen, benennt seine Gate-Losigkeit ehrlich — und diese Ehrlichkeit ist gemessen, nicht behauptet. Die Scope-Grenze hält exakt: keine der vier Bestands-Flächen ist angefasst, die einzige Roadmap-Änderung ist der pflichtige Ruhe-Marker-Flip des Lifecycle-Moves. Die Spiegel-Fläche ist unabhängig abgeleitet und vollständig. Der neue Anker erzeugt im heutigen Bestand nachweislich keinen Scheinbefund — das benannte Risiko aus §5 ist eingelöst.

**Auflage vor der Closure:**

- **F-1 (MEDIUM):** `AGENTS.md:155` prädiziert die fünf Kommentar-Klassen über Zustandsfelder und widerspricht damit dem eigenen Rumpf und dem Kanon, der für Zustandsfelder ausdrücklich nur die zwei Tests überträgt. Die Überschrift ist die Kurzform, gegen die slice-119 und slice-120 arbeiten werden.

**Zur Abwägung, nicht als Auflage:** F-2 (die Bestandsgrenze schweigt zum Zustandsfeld-Bestand) und F-3 (die Aufzählung liest sich als Skopus; `harness/conventions.md:27` bleibt außerhalb der Vier-Treffer-Zählung) sind beide **Eingangsmaterial für slice-119/slice-120** — F-2 entscheidet sich mit der Räumung ohnehin, F-3 ist eine Zensus-Frage der Welle, nicht ein Defekt dieses Commits.

**Kein Steering-Loop-Signal** nach `reviewer.md` §Kontext-Eskalation: die drei Findings gehören drei verschiedenen Klassen an; keine wiederholt sich in diesem Lauf.

**Nicht geprüft (Rollengrenze):** die DoD-Abhakung des Slice-Plans und der `make gates`-Lauf im echten Repo — beides ist Verifikations-Territorium; die hier gefahrenen Image-Läufe sind Review-Gegenproben, kein Gate-Nachweis.
