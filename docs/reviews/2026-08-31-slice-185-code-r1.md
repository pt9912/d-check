# Review slice-185 — `doc-usage` im `--print-mk`-Fragment

**Review-Art:** Code (gegen Slice-Plan, Anforderung, Hard Rules — keine DoD-Abhakung)
**Gegenstand:** `78ff14e..HEAD` (4 Commits): `4db8e2b` (docs(spec), eigenständige Vorab-Korrektur `doc-structure` in der Aufzählung) · `ecf5ec3` (docs(plan), Slice angelegt) · `39258a4` (docs(plan), Lifecycle-Move `open/`→`in-progress/`) · `8e75841` (feat(cli), die eigentliche Implementierung)
**Skill:** `.harness/skills/reviewer.md` @ v1.13.0 · **Modell:** claude-sonnet-5 · **Datum:** 2026-08-31
**Eingangs-Kontext:** Slice-Plan `docs/plan/planning/in-progress/slice-185-print-mk-doc-usage.md`; `DC-FA-CLI-010` (Lastenheft 0.80.0, erweitert), `DC-FA-CLI-001` (exponiert, unverändert), `DC-FA-CLI-010.a` (Spezifikation); `AGENTS.md` §3 (alle Unterpunkte) und §5; Vorläufer-Reviews `2026-06-23-slice-047-r1.md`, `2026-06-23-slice-044-r1.md`, `2026-06-23-slice-044-r2.md`, `2026-06-21-slice-038-print-mk.md`; Beobachtungs-Register `BEO-002` (Zähler 5), `BEO-020` (Zähler 6), `BEO-023` (Zähler 7). Kein ADR berührt (additiv, wie die Vorgänger `doc-doctor`/`doc-repair`/`doc-help`).

### Eigener Lauf

| Lauf | Ausgabe |
|---|---|
| `make test` | alle Pakete `ok`, inkl. `TestCLI185_PrintMK_UsageTarget` · Exit 0 |
| `make build` dann `docker run d-check:latest --help` | Exit 0, `wc -c` stdout = **0**, stderr = **2488** — deckt sich wortgleich mit der Botschaft/Historie-Zeile |
| `docker run d-check:latest --print-mk > d-check.mk` + `make -n -f <frag> doc-usage` | `docker run --rm --network none -v "…:/repo:ro" ghcr.io/pt9912/d-check:v0.0.0-dev --help` — parst und expandiert korrekt |
| `grep -c '%'` über das erzeugte Fragment | 0 — kein `fmt`-Renderer-Artefakt |
| **Umkehr-Probe (eigene, gegen `BEO-023`):** `doc-usage`-Block in `print_mk.go` temporär entfernt, `make test` | `FAIL github.com/pt9912/d-check/internal/adapter/driving/cli` — genau der neue Test kippt; Datei danach exakt zurückgesetzt (`git diff --stat print_mk.go` leer) |
| `make arch-check` | `gesamt: 0 Befund(e)` |
| `make doc-check` (Arbeitsbaum vor diesem Report) | `d-check: 631 Datei(en) geprüft, 0 Befund(e)` |
| `make gates` | `[gates] baseline-verify + workflow-pins + doc-check + lint + test + arch-check + coverage-gate + semgrep + gate-consistency + planning-check green` · Exit 0 (zehn benannte Glieder in der Schlusszeile — deckt sich mit der Feat-Botschaft) |

`make fullbuild`, `make record-gates`, `make image-scan` bewusst nicht gefahren (Nachweis-Datei bzw. Netz; nicht Gegenstand dieses Reviews).

---

## Urteil

**ACCEPT** — 0 HIGH · 2 MEDIUM · 0 LOW · 0 INFO.

Die Implementierung ist sauber und eng am Vorgänger-Muster (`doc-doctor`/`doc-repair`/`doc-help`,
slice-047): ein bestehender Modus wird ein Fragment-Target, `@`-echo-unterdrückt, ohne
`--enable`/`--disable`, mit korrektem `%`-freiem Template und exakt sieben `fmt`-Verben. Die
`stderr`-Eigenschaft von `--help` ist real gemessen (0/2488 Byte), nicht behauptet, und die
Umkehr-Probe (`BEO-023`) beißt tatsächlich — nachgestellt und bestätigt. Die Aufzählungs-Spiegel
(`BEO-002`) sind vollständig nachgezogen: Lastenheft, Spezifikation, Code-Kommentar, Handbuch und
sogar `operations.md` (dort sogar über die eigene Spiegel-Liste des Plans hinaus verbessert, durch
Delegation an das erzeugte Fragment statt einer weiteren statischen Aufzählung). Ein einziger
MEDIUM-Befund: eine unverifizierte Exklusivitäts-Aussage in der Spezifikation, zweimal geschrieben.

---

## Findings

### M-1 · `doc-usage` ist nicht „das einzige der dreizehn" ohne `--enable`/`--disable` — sechs weitere Targets tragen dieselbe Eigenschaft

- **kategorie:** MEDIUM
- **quelle:** Maintainability (Muster wie `BEO-011`, Ausprägung a — Exklusivitäts-Aussage ohne Nachbar-Zählung)
- **pfad:** `spec/spezifikation.md:750-753` (§`DC-FA-CLI-010.a` Punkt 5, `doc-usage`-Aufzählungspunkt) und `spec/spezifikation.md:3130` (Historie-Zeile 2026-08-31)
- **befund:** Beide Stellen behaupten wortgleich, `doc-usage` trage „als einziges der dreizehn **keine** `--enable`/`--disable`-Wahl". Tatsächlich tragen von den dreizehn Targets nur sechs (`doc-immutable`, `doc-commits`, `doc-planning`, `doc-tracked`, `doc-targets`, `doc-structure`) eine `--enable`/`--disable`-Wahl; die übrigen sieben (`doc-check`, `doc-trace`, `doc-complete`, `doc-doctor`, `doc-repair`, `doc-usage`, `doc-help`) tragen sie alle nicht — nachgezählt gegen `internal/adapter/driving/cli/print_mk.go:75-129` (`mkTemplate`). `doc-usage` ist damit eines von sieben, nicht das einzige. Der Rest des Bullet-Punkts (Modus-vs.-Regelmodul-Unterscheidung, `stderr`, Mount-Zusage) ist inhaltlich korrekt und selbst gemessen bestätigt — nur die Exklusivitäts-Formel ist falsch.
- **verifizierbar:** ja — `grep -c '\-\-enable' internal/adapter/driving/cli/print_mk.go` liefert 6, nicht 12; ein Diff der Zeilen ohne `--enable` zählt sieben `.PHONY`-Blöcke.
- **klasse:** exklusivitäts-aussage-ungezählt

### M-2 · Die Commit-Botschaft von `39258a4` zitiert `MR-013` für einen Übergang, den dessen Geltungsbereich-Feld nicht nennt

- **kategorie:** MEDIUM
- **quelle:** Prüffrage 9 (Quelle über ihren Geltungsbereich hinaus zitiert), Reviewer-Skill-Anker „lies das Geltungs-Feld, nicht den Titel"
- **pfad:** Commit-Botschaft `39258a4` (*„slice-185 beansprucht — open/ nach in-progress/ (MR-013, slice-185)"*) gegen `harness/conventions/MR-013-lifecycle-move-buendelung.md:6-9` (Feld **Geltungsbereich**)
- **befund:** Das **Geltungsbereich**-Feld von `MR-013` nennt wörtlich drei Dinge: `AGENTS.md` §3.3, „der Slice-Lifecycle `docs/plan/planning/in-progress/` → `…/done/`" (**eine** Richtung, explizit benannt) und „die MR-/Wellen-Lifecycle-Moves" (`conventions/` → `conventions/done/`, flaches Wellendokument → `done/`). Der Übergang `open/` → `in-progress/`, den `39258a4` bündelt und mit `MR-013` kennzeichnet, steht in keinem der drei Teile. `AGENTS.md` §3.3 selbst führt die passende Passage unter der Überschrift „**Ausnahme Slice-Lifecycle-Move (`in-progress/` → `done/`)**" — ebenfalls nur diese eine Richtung. Der Move-Commit selbst ist mechanisch richtig gebaut (reiner `git mv` plus der Roadmap-Flip, ohne den `make planning-check` auf diesem Commit rot liefe — dieselbe Kollisionslogik, die `MR-013` für die Gegenrichtung dokumentiert), aber die **zitierte Quelle** deckt genau diesen Fall nicht. Das ist kein Einzelfall dieses Diffs: mindestens acht frühere „`…` beansprucht — open/ nach in-progress/ (MR-013, …)"-Commits desselben Repos folgen demselben Muster (`e8401c7`, `b2530f7`, `a062fe8`, `3458610`, `6b485a6`, `e390c2b`, `776c0dd`, `4a91a41`) — die Zitierung ist etablierte Praxis, nicht ein einmaliger Ausrutscher, und trägt trotzdem nicht gegen das Geltungsbereich-Feld.
- **verifizierbar:** ja — der Feldinhalt ist wörtlich zitierbar (`sed -n '6,9p' harness/conventions/MR-013-lifecycle-move-buendelung.md`); die acht Präzedenz-Commits sind über `git log --grep=beansprucht --oneline` auffindbar.
- **klasse:** quelle-jenseits-geltungsbereich

---

## Negativbefunde (geprüft, ohne Befund)

- **Zahlen-Konsistenz der Aufzählung (`BEO-002`):** `grep -rn "zwölf"` außerhalb `done/`/`.harness/baseline/` findet keine übrig gebliebene Zwölf-Nennung zu dieser Anforderung; `.PHONY:`-Zeilen in `print_mk.go` zählen exakt 13; Lastenheft-Beschreibung, beide Akzeptanzkriterien, Out-of-Scope-Klausel, Spezifikations-Punkt-5-Zahl, Code-Kommentar und Handbuch-§4.16 nennen alle „dreizehn"/13 konsistent. Kein Spiegel übersehen.
- **`fmt`-Verben/`%`-Verbot:** genau 7 `%s` innerhalb der `mkTemplate`-Konstante (Zeilen 49–129), passend zu den 7 Argumenten des `fmt.Sprintf`-Aufrufs (Version + sechs `disableAllExcept`); das erzeugte Fragment enthält kein literales `%`. Kein `%`-Hazard durch den neuen Block.
- **`stderr`-Eigenschaft real gemessen:** `docker run d-check:latest --help` ⇒ 0 Byte stdout, 2488 Byte stderr, Exit 0 — deckungsgleich mit Code-Kommentar, Lastenheft-Historie und Spezifikations-Historie. Kein behaupteter, sondern ein reproduzierter Wert.
- **Mount-Kurzschluss-Zusage:** `--help`/`-h` löst in `parseOptions` (`cli.go:325-331`) über `flag.ErrHelp` aus und kehrt mit `done=true` zurück, **bevor** `Run` `earlyGenerators`/`openRoot` erreicht (`cli.go:559-572`) — die Behauptung „Kurzschluss vor der Wurzel-Auflösung" ist Code-treu, nicht nur Prosa.
- **Umkehr-Probe (`BEO-023`):** temporäres Entfernen des `doc-usage`-Blocks aus `mkTemplate` lässt genau `TestCLI185_PrintMK_UsageTarget` (Paket `cli`) rot laufen, alle anderen Pakete bleiben grün — der Test beißt tatsächlich, keine Attrappe. Datei nach der Probe exakt auf den committeten Stand zurückgesetzt.
- **`doc-help` bleibt unverändert Make-Ebene:** kein Edit an dessen Recipe/Annotation; der Test prüft explizit das Nebeneinander beider Targets.
- **Kein ADR nötig (§3.6, Slice-Kopf-Einordnung):** additive Fragment-Erweiterung eines bestehenden Modus, keine Schwellen-Senkung, keine neue Import-Kante (`make arch-check` → 0 Befunde), keine Gate-Lockerung.
- **`AGENTS.md` §3.1 (Docker/make-only):** kein Host-Werkzeug im Diff; alle eigenen Läufe oben gehen über `make`-Targets bzw. Docker.
- **§3.2 (Suppression-Verbot):** keine `nolint`-Direktive im Diff.
- **§3.3 (`git mv` + Inhaltsänderung getrennt):** `39258a4` ist ein reiner `git mv` (`R100`, Slice-Datei byte-identisch) plus der gekoppelte Roadmap-Flip (Entfernen des Ruhe-Markers „Nichts in Arbeit" — zwei gelöschte Zeilen in `roadmap.md`); ohne diese Bündelung wäre der Move-Commit für sich `planning-check`-rot (in-progress/ trägt einen Slice, Roadmap noch auf Ruhe-Marker) — die Mechanik selbst ist richtig gebaut. Die Frage, ob `MR-013` die **Quelle** ist, die genau diesen Übergang deckt, ist nicht Sache dieser Zeile — siehe **M-2**.
- **§3.4 (Spec-Straten nie abwärts):** die neuen Lastenheft-/Spezifikations-Absätze und Historie-Zeilen verweisen ausschließlich same-stratum (`#dc-fa-cli-010…`, `#dc-fa-cli-001…`); keine ADR-, Slice-, Welle- oder Commit-Hash-Nennung im Anforderungskörper.
- **§3.7 (Kommentarklassen):** der neue Go-Kommentar (`print_mk.go:119-122`) trägt eine Zusage (gemessenes Verhalten + Begründung der `@`-Unterdrückung) und keine Review-Historie, keine Slice-Nummer, keine Deliberation über Verworfenes.
- **§3.8 (Modul verspricht nur über die Scan-Menge):** der Generator ist kein Regelmodul und liest keine Eingabe außerhalb dessen, was er selbst ausgibt — keine neue Ziel-Achse.
- **Negativtest zu neuem Vertrag (Prüffrage 13):** vorhanden und beißend (`TestCLI185_PrintMK_UsageTarget`, inkl. der eigenen Umkehr-Probe oben).
- **Kein stiller Grün-Pfad (Prüffrage 1):** kein Gate-/Skript-Verhalten geändert; der neue Codepfad ist reine Template-Erweiterung ohne neue Bedingungen.
- **Kommentar-/Zustandsfeld-Bestand sonst unverändert:** `roadmap.md`-Diff entfernt ausschließlich den Ruhe-Marker (Zustand, kein Chronik-Text); kein neues `Status:`-Feld angelegt.
- **Slice-Kopf:** `**Lifecycle:**` statt `**Status:**`, `**Verantwortlich:**` bei Beanspruchung gesetzt, `**Berührte Spec-Stellen:**` mit Kennung (`DC-FA-CLI-010`, `DC-FA-CLI-010.a` Punkt 5) statt bloßem `§N` — konform.

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 2 |
| LOW | 0 |
| INFO | 0 |

## Verdikt

**ACCEPT.** Zwei MEDIUM-Befunde, keiner blockierend.

M-1 ist eine unverifizierte Exklusivitäts-Aussage in `spec/spezifikation.md` — sie berührt
kein Gate, keinen Code-Pfad und keine Akzeptanzkriterien, aber vor Closure zu korrigieren: die
Spezifikation ist die technisch verbindliche, fortschreibbare Quelle (Source Precedence
Rang 2), und eine falsche „als einziges"-Behauptung dort ist genau die Klasse, die `BEO-011`
bereits vier Mal registriert hat. **Nach Abschluss dieses Reviews vom Auftraggeber bestätigt
behoben:** beide Stellen tragen jetzt eine gezählte Aussage (sechs von dreizehn mit
Fokus-Liste, sieben ohne) statt der Exklusivitäts-Formel; `make gates` danach grün. Der
Befund bleibt hier stehen als Lauf-Beleg des Zustands zum Review-Zeitpunkt, nicht als
offener Punkt.

M-2 ist eine Selbstkorrektur dieses Reports: eine frühere Fassung dieser Datei behauptete in
ihrer eigenen §3.3-Negativbefund-Zeile, `MR-013`s Geltungsbereich schließe Slice-Lifecycle-Moves
„allgemein" ein. Das Geltungsbereich-**Feld** von `MR-013` (nicht sein Titel, nicht der
erläuternde Fließtext) nennt wörtlich nur eine Richtung — „der Slice-Lifecycle
`docs/plan/planning/in-progress/` → `…/done/`" — plus die MR-/Wellen-Lifecycle-Moves; `AGENTS.md`
§3.3 führt dieselbe eine Richtung unter der Überschrift „Ausnahme Slice-Lifecycle-Move
(`in-progress/` → `done/`)". Die Commit-Botschaft von `39258a4` zitiert `MR-013` aber für
`open/` → `in-progress/` — einen Übergang, den das Feld nicht nennt. Die Mechanik des Moves
selbst bleibt unstrittig richtig (ohne die Bündelung liefe `make planning-check` auf dem
Move-Commit rot); nur die zitierte Quelle trägt die Aussage nicht. Dieselbe Zitierform findet
sich in mindestens acht früheren Commits desselben Repos — etablierte Praxis, die die
Geltungsbereich-Frage nicht beantwortet, sondern nur wiederholt. Reparaturvorschlag gehört
nicht in dieses Finding (Reviewer-Skill „Kein Lösungsvorschlag im Befund").
